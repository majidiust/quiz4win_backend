[2026-06-18] [A-01] [BUILD] **`public-hosts` — added live & upcoming host-collection endpoints.** Extended `supabase/functions/public-hosts/index.ts` with two new read-only, JWT-free routes for the customer/host apps: `GET /public-hosts/live` (hosts on air right now, each with their embedded `live_shows`) and `GET /public-hosts/upcoming` (hosts with future shows, each with embedded `upcoming_shows` sorted soonest-first + `upcoming_shows_count`). Both use a PostgREST inner-join on `games` so only hosts that actually have a matching live/upcoming game are returned, paginated (limit≤50), and gated to `application_status='approved'`. `live` and `upcoming` are reserved path keywords (host IDs are UUIDs, so no collision with `/public-hosts/:id`). Additive only — existing routes unchanged (R-16 backward-compatible). No migration required; reuses existing anon RLS on `show_hosts` + `games`. R-01: no PII returned. **Deploy:** `docker compose up -d --build --force-recreate api`.

[2026-06-18] [A-01] [BUILD] **New `public-hosts` Edge Function — customer-app host directory & profile API.** Created `supabase/functions/public-hosts/index.ts` with five read-only, JWT-free routes for the customer app: `GET /public-hosts` (list hosts, filterable by country/language/search, sortable, paginated), `GET /public-hosts/:id` (full public profile including is_live flag and upcoming_shows_count), `GET /public-hosts/:id/live` (current live show the host is presenting, if any), `GET /public-hosts/:id/upcoming` (paginated upcoming assigned shows), `GET /public-hosts/:id/history` (paginated completed shows, most-recent-first). Only `application_status='approved'` hosts are exposed (defence in depth on top of the existing `show_hosts_select_active` RLS policy that already gates anon access to `status='active'`). R-01 compliance: phone, auth_user_id, and all lifecycle timestamps are excluded from every response. No migration required — both `show_hosts` and `games` already have anon-accessible RLS policies. **Deploy:** `docker compose up -d --build --force-recreate api`.

[2026-06-17] [A-01] [FIX] **Host app landing page — replaced placeholder copy with real data from quiz4win.com/become-host.** Previous version used invented statistics (€850/show, <1 hr approval, 10k+ players). Real figures sourced from the public marketing site: 2.4k shows/month, 180+ active hosts, 47 countries; per-show fee $150–$1,000 (scaled by audience size); revenue share 1%–5% of participant entries. Hero headline updated to the site's own tagline ("Step on the stage. Get paid every show."). Sub-headline, steps, and benefit copy updated to match the site's messaging (production support, cross-promo, transparent payouts). Added an earnings breakdown panel replacing the vague scarcity strip. Bottom CTA updated to reflect real stats. R-07 compliant. **Deploy:** `docker compose up -d --build --force-recreate host`.

[2026-06-17] [A-01] [REFACTOR] **Restructured the host Profile (Settings) page into a classified hub + inner detail pages.** The `/settings` page (the bottom-nav "Profile" tab) crammed everything onto one screen: avatar header, three nav rows, a full editable profile form (country, phone, languages, tagline, about), six unlabelled social-URL inputs, and sign-out. Reorganised into a clean menu: **(1)** `host-app/src/app/(app)/settings/page.tsx` — now a hub showing the avatar/name/status header plus three labelled sections — **Account** (Edit profile, Social profiles), **Verification & payouts** (Verification files, Payout wallets), **General** (Notifications) — each rendered as a tappable `Row` with icon, title, subtitle, and chevron; sign-out at the bottom. Removed the inline `Input`/`Textarea`/`updateProfileAction` form and the now-unused `LANGS` constant from this file. **(2)** New `settings/profile/page.tsx` — personal details form (country, phone, languages grid, tagline, about) with `back="/settings"`. **(3)** New `settings/social/page.tsx` — the six social links, now each properly **labelled** (Instagram, Telegram, YouTube, TikTok, X, Website), `type="url"`, with `back="/settings"`. **(4)** `settings/actions.ts` — made `updateProfileAction` **partial-safe**: it now only PATCHes fields actually present in the submitted form (`formData.has(k)`), gates `languages` behind a hidden `has_languages` marker (so "none selected" ≠ "not on this form"), and honours a hidden `redirect` field so each inner page returns to itself with the success/error banner. This prevents an inner page's form from wiping fields it doesn't display. No backend/API change — both inner pages reuse the existing `PATCH /host/me` route. `host-app tsc --noEmit` exit 0; no IDE diagnostics. **Deploy:** `docker compose up -d --build --force-recreate host` (front-end only).

[2026-06-17] [A-01] [BUILD] **Host payout (withdrawal) feature — full implementation.** Hosts can now request a payout of at least $10 from their wallet balance; both hosts and admins have list/history views, detail pages, and admin action buttons. Changes: **(1) Migration** `supabase/migrations/20260617000000_host_withdrawals.sql` — creates `host_withdrawals` table (host_id FK→show_hosts, payment_method_id FK→host_payment_methods, amount NUMERIC(12,2) CHECK≥10, status pending/processing/completed/rejected, crypto snapshot columns, admin fields); host SELECT-only RLS policy; extends `notifications.type` CHECK to add `host_withdrawal`. **(2) Edge Function** `supabase/functions/host/index.ts` — added three routes: `GET /host/withdrawals` (list with status/limit filters), `POST /host/withdrawals` (validates approved host + active payment method + amount≥10 + no existing pending/processing + sufficient balance; atomically debits `profiles.wallet_balance`, appends `transactions` row type=`host_payout`, inserts withdrawal record, sends notification), `GET /host/withdrawals/:id` (detail). **(3) Host-app** — `host-app/src/app/(app)/withdrawals/page.tsx` (server page: shows balance card, request form, history list); `withdrawals/request-form.tsx` (client: active-method selector, amount input with min/max validation, note, submit via `useFormStatus`); `withdrawals/actions.ts` (`requestWithdrawalAction` server action). Updated `wallet/page.tsx` to add "Request payout →" link. **(4) Admin** — added `approveHostWithdrawal`, `rejectHostWithdrawal` (refunds balance, inserts refund transaction, notifies host), `completeHostWithdrawal` (records TX hash, notifies host) server actions to `admin/src/lib/actions/finance.ts`; created `admin/src/app/(app)/finance/host-withdrawals/page.tsx` (list with status tabs + stats), `host-withdrawals/[id]/page.tsx` (detail: request card, host card, destination/crypto card, history table), `host-withdrawals/[id]/host-withdrawal-actions.tsx` (approve/complete/reject dialogs); added "Host Payouts" entry under Finance in `admin/src/lib/nav.ts`; added Withdrawals tab to `admin/src/app/(app)/hosts/[id]/page.tsx`. Both `tsc --noEmit` exit 0. **Deploy:** `docker compose up -d --build --force-recreate host admin` + redeploy `host` Edge Function + apply migration via `db-maintainer`.

[2026-06-17] [A-01] [REFACTOR] **Cleaned up the Verification Files upload section (host-app).** The "Upload a new file" card used a raw native `<select>` and `<input type="file">`, which rendered with inconsistent default browser chrome (messy "No file chosen" text, mismatched select arrow) that clashed with the glass UI. Replaced the inline form in `files/page.tsx` with a new `UploadForm` client component (`host-app/src/app/(app)/files/upload-form.tsx`): **(1)** a styled document-type picker — a 2-column grid of selectable pills (icon + label + check), each emitting the value via a hidden `file_type` input; **(2)** a tap/drag dropzone that, once a file is chosen, shows a preview card (image thumbnail or type icon, filename, formatted size) with a remove (X) button — drag-and-drop syncs the dropped file back into the real hidden `<input type="file">` via `DataTransfer` so the form still posts it; **(3)** a submit button that reflects `useFormStatus().pending` and is disabled until a file is selected. Upload still posts to the existing `uploadFileAction` server action (unchanged mechanism). Removed the now-unused `Button`/`uploadFileAction` imports and `TYPES` constant from `page.tsx`. `host-app tsc --noEmit` exit 0; no IDE diagnostics. **Deploy:** `docker compose up -d --build --force-recreate host`.

[2026-06-17] [A-01] [BUILD] **In-app preview for host Verification Files.** Clicking a file on `/files` (host-app) previously rendered `<a href={f.url}>` — but non-avatar host files (selfie, ID, intro video, screenshots) are stored **private** in S3, so the raw `url` 403s. Added a proper in-app viewer: **(backend)** new route `GET /host/me/files/:id/url` in the host Edge Function — verifies the file belongs to the requesting host, then returns a short-lived presigned GET URL (10 min) via `presignGet` for private files, or the public URL for avatars. **(host-app)** new `getFilePreviewUrl(id)` server action (calls the route with the httpOnly session token, never exposing it to the browser) + new `FilePreview` client component that opens a modal with the correct viewer by MIME type: `image/*` → `<img>`, `video/*` → `<video controls>`, `application/pdf` → `<iframe>`, else an "Open in new tab" link. The viewer also offers an "Open in new tab" link for every file. Wired into `files/page.tsx` (replaced the plain Preview anchor). `host-app tsc --noEmit` exit 0 (deno not installed locally — route mirrors existing in-file patterns). **Deploy:** redeploy the `host` Edge Function + `docker compose up -d --build --force-recreate host`.

[2026-06-17] [A-01] [FIX] **Avatar "Take photo" now opens a real in-app camera.** The "Take photo" option in `AvatarPickerSection` clicked a hidden `<input type="file" capture="user">`. The HTML `capture` attribute is unreliable — on desktop and many mobile browsers it silently opens a plain file picker instead of the camera. Replaced it with an in-app live camera using `navigator.mediaDevices.getUserMedia`: full-screen `<video>` preview (mirrored for the front camera), a capture button that centre-crops the current frame to a square via `<canvas>` → `canvas.toBlob` → `File` (passed to the existing `handleFile` upload path), plus cancel and switch-camera (front/back) controls. Stream tracks are stopped on capture, cancel, and unmount. The hidden file input is retained as a fallback when the camera API is unavailable (insecure context / unsupported browser) or permission is denied. `tsc --noEmit` exit 0. **Deploy:** `docker compose up -d --build --force-recreate host`. NOTE: getUserMedia requires a secure context (HTTPS) — already satisfied on `host.quiz4win.com`.

[2026-06-17] [A-01] [BUILD] **Dedicated /onboarding/avatar step + flicker fix.** Two improvements to the host onboarding flow: **(1) Flicker fix** — The apply wizard's step 4 ("Photo") combined an inline avatar picker with a review card, which appeared as a brief/confusing intermediate screen before the video upload page. Fixed by removing the Photo step entirely (and its `AvatarPicker` component, `uploadAvatarTempAction` import, and `avatar_url` hidden field). Step 4 is now "Review" (shows `ReviewCard` only). After submitting the wizard, `applyAction` now redirects to `/onboarding/avatar` instead of `/onboarding/intro-video`. **(2) Mandatory avatar step** — Created `host-app/src/app/onboarding/avatar/page.tsx` (server component: fetches `/host/me` + `getUploadToken`) and `client.tsx` (`OnboardingAvatarClient`: wraps `AvatarPickerSection` with an `onChanged` callback, tracks `hasAvatar` state, and enables a Continue → `/onboarding/intro-video` button only once an avatar is set). Added optional `onChanged?: (url: string) => void` prop to `AvatarPickerSection` (wired into both predefined-avatar and file-upload success paths). Backend gate: `GET /host/onboarding-state` now fetches `avatar_url` from the host row; routing order is `approved → /dashboard`, `no avatar → /onboarding/avatar`, `no intro video → /onboarding/intro-video`, else `/onboarding/status` — approved hosts always bypass all onboarding checks. Status page updated to redirect pending non-suspended hosts to `/onboarding/avatar` before `/onboarding/intro-video`. `tsc --noEmit` exit 0. **Deploy:** `docker compose up -d --build --force-recreate host` + redeploy `host` Edge Function.

[2026-06-17] [A-01] [BUILD] **Host lifecycle email notifications — all events wired end-to-end.** Added full email coverage for every host lifecycle event using the existing Brevo transactional API. All sends are best-effort (fire-and-forget, never block the primary action). Changes: **(1) `supabase/functions/_shared/email.ts`** — added `hostWelcomeTemplate` (post-OTP welcome), `hostUploadReceivedTemplate` (file uploaded), `hostOnboardingCompleteTemplate` (first intro video = onboarding done), `adminHostEventTemplate` (internal admin notification). **(2) `admin/src/lib/admin-auth/email-brand.ts`** — added `hostStatusChangedEmailTemplate` (approve/reject/suspend/reactivate) and `hostFileReviewedEmailTemplate` (file approve/reject), both using the existing `renderBrandEmail` shell. **(3) `supabase/functions/auth/index.ts`** — after successful signup OTP verification, fires `hostWelcomeTemplate` to host and `adminHostEventTemplate` to `ADMIN_EMAIL`. **(4) `supabase/functions/host/index.ts`** — (a) imports new templates; (b) after `POST /host/apply` fires application-received email to host + admin notification; (c) updated `dispatchHost` signature to accept `hostEmail: string | null`; (d) after `POST /host/me/avatar` fires upload-received to host + admin; (e) after `POST /host/me/files` fires upload-received (or onboarding-complete if first intro_video) to host + admin. **(5) `admin/src/lib/actions/hosts.ts`** — imports `sendEmail` + template functions; added `getHostEmailInfo(db, hostId)` helper (joins `show_hosts` → `profiles.email`); in `setHostStatus` fires `hostStatusChangedEmailTemplate` to host; in `reviewHostFile` fires `hostFileReviewedEmailTemplate` to host. Admin `tsc --noEmit` exit 0. **Deploy:** `docker compose up -d --build --force-recreate host admin` + redeploy `auth` and `host` Edge Functions.

[2026-06-17] [A-01] [FIX] **Admin Docker build failed — `Module not found: Can't resolve '@/components/detail'`.** Root cause: the earlier "admin host detail page" work committed the consumers (`hosts/[id]/page.tsx` imports `@/components/detail`; `host-media.tsx` calls `getHostFileSignedUrl`) but the two dependencies were never staged: `admin/src/components/detail.tsx` was untracked (`??`) and `admin/src/lib/actions/hosts.ts` (adds `getHostFileSignedUrl`) was modified-uncommitted. Local `tsc`/dev passed because the files exist on disk, but the deploy server pulls from git and the build context lacked them → webpack module-not-found. Fix: committed `admin/src/components/detail.tsx` + `admin/src/lib/actions/hosts.ts`. Also committed `host-app/next.config.ts` (the `serverActions.bodySizeLimit: "50mb"` change that was likewise left uncommitted). Verified `admin` `tsc --noEmit` exit 0. **Deploy:** `docker compose up -d --build --force-recreate admin host`.\n\n[2026-06-17] [A-01] [BUILD] **Predefined 3-D avatar grid in settings avatar picker.** Extended `host-app/src/app/(app)/settings/avatar-picker.tsx`: bottom sheet now has three options — "Choose an avatar" (opens scrollable 5-column grid of 40 CDN images: 5 × `3d_` + 35 × `memo_` from alohe/avatars via jsDelivr, MIT licensed), "Take photo", "Choose from gallery". Selecting a predefined avatar sets an optimistic preview then calls `PATCH ${API_URL}/host/me` with `{ avatar_url }` using the existing `uploadToken` — no S3 upload needed since these are already publicly hosted. Added `showGrid` state + back-navigation. No prop or page changes needed. tsc clean (exit 0). R-07 compliant. **Deploy:** `docker compose up -d --build --force-recreate host`.

[2026-06-17] [A-01] [BUILD] **Telegram-style avatar picker in host settings + admin media fix (presigned URLs + inline video/image viewers).** (1) host-app settings page: added `getUploadToken` server action to `settings/actions.ts`; created `settings/avatar-picker.tsx` client component — clicking the avatar circle opens a bottom-sheet dialog with "Take photo" (camera, `capture="user"`) and "Choose from gallery" options, immediately previews the chosen image in the circle, then uploads directly browser→api.quiz4win.com/host/me/avatar using the server-side token (httpOnly bypass, same pattern as intro-video); updated `settings/page.tsx` to use `AvatarPickerSection` and removed the old form-based upload. (2) admin host detail page: pre-compute presigned URLs for intro_video, selfie, and id_document files server-side (via `presignGet(s3_key, 3600)`) so admin sees files immediately on page load without any click; updated `host-media.tsx` with `IntroVideoPlayer` that accepts a `src` prop (auto-plays inline on load, fallback load-on-demand), and new `InlineImageViewer` that shows selfie/ID doc inline with "Full size" link; updated page JSX to pass `src` props and render selfie/ID-doc sections in the sidebar. tsc clean on both apps (exit 0). R-07 compliant. **Deploy:** `docker compose up -d --build --force-recreate host admin`.\n\n[2026-06-17] [A-01] [FIX] **Intro-video upload on 401 now fully signs out + redirects to /signin instead of showing "Session expired" message.** Per user request: a dead-end "Session expired — please sign in again" banner is poor UX. Updated `host-app/src/app/onboarding/intro-video/page.tsx` so that when the direct upload `fetch` to `/host/me/files` returns 401 (or `unauthorized`), the page stops the camera tracks and calls the shared `signOutAction` (from `(app)/settings/actions.ts`) which revokes the refresh token server-side (`POST /auth/signout`), deletes every `sb-*-auth-token` cookie, and `redirect()`s to `/signin` — giving a clean re-login instead of a stuck error. The "Session expired" string was removed from the error map. tsc clean (exit 0). **Deploy:** `docker compose up -d --build --force-recreate host`. R-07 compliant.

[2026-06-17] [A-01] [FIX] **Intro-video upload "Session expired — please sign in again" — browser could not read the httpOnly session token.** Root cause: the previous 413 fix had the intro-video page read the access token in the browser via `createSupabaseBrowserClient().auth.getSession()`, but the session cookie is written server-side with `httpOnly: true` (host-app/src/lib/session-cookie.ts), so browser JS (`document.cookie`) cannot see it → `getSession()` returned null → the direct upload `fetch` to `/host/me/files` was sent with NO Authorization header → backend 401 `unauthorized` → mapped to "Session expired". Fix (2 files): **(1)** added a `getUploadToken()` server action in `host-app/src/app/onboarding/intro-video/actions.ts` that reads the access token from the server-side session (`createSupabaseServerClient().auth.getSession()`, where the httpOnly cookie IS accessible) and returns it. **(2)** `host-app/src/app/onboarding/intro-video/page.tsx` now calls `getUploadToken()` instead of the browser Supabase client to obtain the bearer token; the large video still uploads directly browser→api.quiz4win.com (preserving the bypass of the Next.js server-action 1 MB body limit). Replaced the `createSupabaseBrowserClient` import with `getUploadToken`. tsc clean (exit 0); no IDE diagnostics. NOTE on the related "lands on onboarding without entering credentials" report: that is the *persisted* 30-day session cookie being read by middleware (the user is already authenticated from a prior login) — it is resolved by signing out (clears the cookie) and requires the latest host build to be deployed. **Deploy:** `docker compose up -d --build --force-recreate host`. R-07 compliant.

[2026-06-17] [A-01] [BUILD] **Full three-condition auth/onboarding state machine.** Added GET /host/onboarding-state backend endpoint that returns email_verified, has_application, onboarding_complete, application_status, host_status, and a `next` routing hint — enforcing the rule: Email Verified → Onboarding Completed → Admin Approved before granting dashboard access. Added email_not_verified (403) guard on all host routes except apply/avatar-temp/onboarding-state. Updated (app)/layout.tsx to use the new endpoint as the single routing authority (sentinel: next==="/dashboard"). Updated signin action to redirect to /verify-otp on email_not_confirmed instead of showing a dead-end error. tsc clean; R-03, R-07 compliant.

[2026-06-17] [A-01] [FIX] **Intro-video upload 413 — bypass server action with direct browser→backend upload.** Root cause: `experimental.serverActions.bodySizeLimit` in `next.config.ts` is unreliable in Next.js standalone/Docker mode (`node server.js`) — a confirmed upstream issue (github.com/vercel/next.js/discussions/77505). Even though `host-app/next.config.ts` correctly sets `"50mb"`, the standalone runner may still enforce the default 1 MB limit. Fix: rewrote the `upload()` function in `host-app/src/app/onboarding/intro-video/page.tsx` to upload **directly from the browser to `api.quiz4win.com/host/me/files`** using the Supabase browser-client session token (`createSupabaseBrowserClient().auth.getSession()`), completely bypassing the Next.js server-action body pipeline. The video never transits through the Next.js server, so the Next.js limit is irrelevant. The `uploadIntroVideoAction` server action is no longer called from this page (it remains in `actions.ts` for potential reuse). CORS on the backend already allows browser requests (api.quiz4win.com serves `Access-Control-Allow-Origin`). Host-app passes `tsc --noEmit` (exit 0). **Deploy:** `docker compose up -d --build --force-recreate host`.\n\n[2026-06-17] [A-01] [BUILD] **Host-app visual redesign — match quiz4win.com/become-host palette (pink/fuchsia/teal, glass pill header, gradient button, rounded cards).** The host-app previously used an amber/violet studio theme that did not match the marketing site. Updated 9 files to align with the become-host page design language: **(1) `globals.css`** — replaced `--color-q4w-primary` amber and secondary violet vars with pink/fuchsia/teal; updated radial background glows from amber to pink and teal. **(2) `components/ui/button.tsx`** — primary variant now uses `bg-gradient-to-r from-pink-300 via-fuchsia-300 to-teal-300` with a pink glow shadow (`shadow-[0_10px_40px_-10px_rgba(236,72,153,0.55)]`), `text-black font-semibold`; secondary gets glass + `border-white/10`; danger becomes `bg-rose-500/20 border border-rose-400/30 text-rose-200`. **(3) `components/ui/input.tsx`** — labels now use `text-[11px] uppercase tracking-widest text-white/55`; the field wrapper is now a rounded glass pill (`border-white/10 bg-white/[0.04] focus-within:border-white/30`) with a transparent inner `<input>`; Textarea updated to match. **(4) `components/ui/card.tsx`** — radius bumped to `rounded-3xl`; added `border border-white/10`; `CardSubtitle` uses `text-white/45` instead of the var. **(5) `(auth)/layout.tsx`** — header replaced with a floating glass-pill (`rounded-2xl border border-white/10 bg-white/[0.04] backdrop-blur-xl`) containing a gradient "Quiz4Win" wordmark and a "Host Portal" badge, mirroring the site's nav. **(6) `(auth)/signin/page.tsx`, `signup/page.tsx`, `verify-otp/page.tsx`** — Card wrapper removed; each page is now a `rounded-3xl` glass panel with a soft glow ring (`from-pink-500/30 via-fuchsia-500/20 to-teal-400/30 blur-sm`), a kicker label `text-[11px] uppercase tracking-[0.18em] text-white/45`, a bold heading, and muted sub-text; error/info banners updated to rose/emerald; "Sign in"/"Start over" links use `text-pink-300`. **(7) `onboarding/status/page.tsx`** — pulsing orb changed from amber to pink/fuchsia gradient; Step component active state uses `border-pink-400/50 bg-pink-500/15 text-pink-300` instead of amber; rejected-screen CTA uses the gradient primary button; all muted text updated to `text-white/N` fractions. **(8) `(app)/dashboard/page.tsx`** — stat-card icons updated (`Gamepad2` → `text-pink-300`, `Wallet` → `text-teal-300`, `Mail` → `text-fuchsia-300`, `ClipboardList` → `text-teal-300`); chip highlights updated to pink/fuchsia/teal; next-show link uses `border-white/10`; "All" link uses `text-white/40`. Host-app passes `tsc --noEmit` (exit 0); no IDE diagnostics. **Deploy:** `docker compose up -d --build --force-recreate host`.

[2026-06-17] [A-01] [FIX] **Incomplete onboarding wrongly shown as "Profile submitted" — pending hosts without an intro video are now resumed into onboarding on every login.** Symptom (user): "I did not submit anything but the application is in 'Profile Submitted' status; if I didn't complete onboarding, every login should show the onboarding flow." Root cause: a `pending` `show_hosts` row is created the instant `POST /host/apply` succeeds (name + profile), but nothing tracked whether the *full* onboarding (incl. the intro video) was finished. `(app)/layout.tsx` redirects every non-approved host to `/onboarding/status`, which unconditionally rendered the "under review" PendingScreen for `pending` — so a host who applied but never recorded their intro was parked there permanently instead of being made to finish. There is no DB column for onboarding completion; the intro video lives in `host_uploaded_files` (`file_type='intro_video'`). Fix (3 files, no migration): **(1) Backend `supabase/functions/host/index.ts` — GET `/host/me`** now returns a computed `onboarding_complete` boolean (`true` only when an `intro_video` file exists for the host, via a `count:exact, head:true` query). **(2) `host-app/.../onboarding/status/page.tsx`** — routing rewritten: approved→`/dashboard`, suspended→SuspendedScreen, rejected→RejectedScreen, and any not-yet-approved host whose `onboarding_complete` is false is `redirect()`ed to `/onboarding/intro-video` to finish — on every login; the PendingScreen ("under review") now renders only once onboarding is genuinely complete. The misleading "Upload intro video" link was relabelled "Re-record intro video". **(3) `host-app/.../onboarding/intro-video/page.tsx`** — the intro video is now a **required** step: removed the "Skip for now" button + `skip()` handler (which previously routed to `/onboarding/status` and, under the new gate, would have created a redirect loop), and updated the subtitle to state it is the final required step. Host-app passes `tsc --noEmit` (exit 0); no IDE diagnostics. **Deploy:** `docker compose up -d --build --force-recreate host` (host-app) and redeploy the `host` Edge Function for the `/host/me` change.\n\n[2026-06-17] [A-01] [BUILD] **Admin host detail page: surface all onboarding data + reusable detail structure.** The admin Hosts detail page (`admin/src/app/(app)/hosts/[id]/page.tsx`) previously showed only a subset of profile fields, did not display the host's avatar, and linked the intro video to an empty `url` (private files store `url=""` — `uploadObject` returns `publicUrl:null` for `private` visibility, so the link was broken). Changes: **(1) Reusable primitives — `admin/src/components/detail.tsx` (new):** `DetailSection` (titled card w/ optional lucide icon + header actions), `DetailGrid`/`DetailField` (label-above-value, for wide dl grids), `DetailRow` (label-left/value-right, sidebars), `SummaryCell` (stat cell). Replaces the inline `Row`/`SumCell` helpers that were local to the host page and the ad-hoc `<dl>` blocks duplicated on the host-applications page. **(2) Presigned-URL action — `getHostFileSignedUrl(fileId)` in `admin/src/lib/actions/hosts.ts`:** `requireAdmin([...MOD,'finance'])`, zod-validates the uuid, returns the direct `url` for public-read rows (avatars) or `presignGet(s3_key, 300)` for private rows (intro_video/selfie/id_document/…). R-15.4 compliant (s3_key is source of truth; URL minted on read). **(3) Client media — `host-media.tsx` (new):** `HostFileLink` (opens a file in a new tab via on-click presign) and `IntroVideoPlayer` (loads the signed URL on demand and plays inline in a 16:9 `<video controls>`). **(4) Page rewrite:** added an avatar identity block (Avatar + name + tagline) and an "Onboarding profile" `DetailSection` surfacing every onboarding field collected by the apply wizard (country, languages, phone, About/bio, all 6 social links, and the playable intro video); split timeline + earnings into their own `DetailSection`s; the Files tab now uses `HostFileLink` so private files (intro video) are actually viewable. Admin passes `tsc --noEmit` (exit 0); no IDE diagnostics. Deploy: `docker compose up -d --build --force-recreate admin`.

[2026-06-17] [A-01] [FIX] **Intro-video upload 413 — host-app missing server-action body-size limit.** Symptom: `POST https://host.quiz4win.com/onboarding/intro-video` (the Next.js server-action invocation for `uploadIntroVideoAction`) failed; the recording never reached the backend. Root cause: Next.js caps **server action** request bodies at **1 MB** by default, so the multi-MB recording (~1 Mbps × up to 2 min ≈ 10–15 MB) was rejected with 413 "Body exceeded 1 MB limit" before our action code ran. nginx already allowed `client_max_body_size 50M` (deploy/nginx/host.quiz4win.com.conf), and the backend `MAX_FILE_BYTES` is 25 MB — only the host-app Next config lacked the override. admin had already fixed the identical issue (`admin/next.config.ts` → `serverActions.bodySizeLimit: "50mb"`). Fix: added `experimental.serverActions.bodySizeLimit: "50mb"` to `host-app/next.config.ts` (alongside existing `optimizePackageImports`), mirroring admin and matching the nginx cap. Affects the intro-video upload and the settings/onboarding avatar uploads (same server-action path). Host-app passes `tsc --noEmit`. **Deploy (REQUIRED — next.config is baked at build time):** `docker compose up -d --build --force-recreate host`.

[2026-06-17] [A-01] [BUILD] **Intro-video page: comprehensive host guide added.** `host-app/src/app/onboarding/intro-video/page.tsx` previously gave only a one-line subtitle + teleprompter. Added two static constants `STEPS` (4 numbered on-camera steps: introduce yourself → host the question → build suspense → reveal the answer) and `TIPS` (4 best-practice tips: lighting, steady eye-level framing, clarity/under-2-min, re-record freely). Rendered as a new guide `Card` (numbered list + amber "Tips" callout with a `Lightbulb` icon) shown **only** during `idle`/`previewing` phases so it disappears once recording starts, keeping the recording view focused on the teleprompter; expanded the header subtitle to explain the clip's purpose. No behaviour/recording-logic change. Host-app passes `tsc --noEmit`; no IDE diagnostics. Deploy: `docker compose up -d --build --force-recreate host`.

[2026-06-17] [A-01] [BUILD] **Onboarding gate, graphical status screen, and intro-video robustness overhaul.** Three interrelated improvements in host-app: **(1) `(app)/layout.tsx` — onboarding gate.** The app-group layout now checks `host.application_status` after `not_a_host` redirect. Any host whose status is not `approved` (pending, rejected, suspended, or unknown) is redirected to `/onboarding/status` instead of reaching the main app. Previously, pending hosts saw the full dashboard with only a small amber banner. **(2) `/onboarding/status/page.tsx` (new) — graphical application status screen.** Server-side page; fetches `/host/me` and routes: `approved` → `/dashboard` (race-free), `pending` → animated "under review" screen with pulsing orb + 3-step progress tracker (submitted ✓ / under review 🔄 / approved ○), refresh + "Upload intro video" links; `rejected` → red icon, rejection notes card, "Submit a new application" button; `suspended` → locked-icon screen. **(3) `/onboarding/apply/page.tsx` — pending redirect.** Changed from `redirect("/dashboard")` to `redirect("/onboarding/status")` for pending hosts who re-visit the apply page. **(4) `/onboarding/intro-video/page.tsx` — full rewrite, 4 bugs fixed + layout redesign.** Bug fixes: (a) **useEffect cleanup bug** — the old single `useEffect([recordedUrl])` ran stream teardown every time `recordedUrl` changed (including `setRecordedUrl(null)` during re-record), killing the camera mid-session; now split into three effects: unmount-only stream cleanup, recordedUrl-change blob-URL revocation, and phase-driven timer. (b) **Side effects inside state updater** — `setRevealed(true)` and `stopRecording()` were called inside a `setElapsed` functional updater (impure), which breaks React strict-mode and can fire twice; moved to a dedicated `useEffect([elapsed, phase])`. (c) **Timer not driven by phase** — timer setup was inside `startRecording()` requiring manual cleanup with refs; now a `useEffect([phase])` starts/stops the interval automatically. (d) **No camera preview on load** — user saw a black screen with no feedback; now auto-requests camera on mount and shows a live 16:9 preview immediately. Layout redesign: compact 16:9 video (not the old tall 3:4) + teleprompter card + controls all visible on screen without scrolling. Skip/upload now redirects to `/onboarding/status` (not `/dashboard`). Host-app passes `tsc --noEmit`; no IDE diagnostics. **Deploy:** `docker compose up -d --build --force-recreate host`.\n\n[2026-06-17] [A-01] [FIX] **host-app signup OTP: form truncated the 8-digit code → false "Code expired".** Supabase Auth (hosted) issues an 8-digit email OTP, but `verify-otp/page.tsx` hard-capped the input to 6 (`maxLength={6}`, `pattern="[0-9]{6}"`), silently dropping the last 2 digits. GoTrue then rejected the truncated token with "Token has expired or is invalid", which the auth edge fn maps (any "expired" substring) to `otp_expired` → UI showed "Code expired" even though the code was simply incomplete. Fix (host-app only, no backend/config change): **(1) `verify-otp/page.tsx`** — input now accepts 6–8 digits (`maxLength={8}`, `pattern="[0-9]{6,8}"`), relabelled "Verification code", placeholder/help text de-hardcoded from "6-digit". **(2) `verify-otp/actions.ts`** — token now stripped to digits only (`replace(/\D/g,"")`) so an autofill/paste with spaces still validates. **(3) `signup/page.tsx` + `signup/actions.ts`** — wording/comment changed from "6-digit code" to "verification code" (length is a Supabase Auth dashboard setting, not fixed in app). NOTE: to force a 6-digit code instead, change Supabase → Authentication → Email → OTP length to 6 (dashboard, no code change). Host-app passes `tsc --noEmit`. Deploy: `docker compose up -d --build --force-recreate host`.

[2026-06-17] [A-02] [RULE] **R-15 added — "All User File Uploads Go Through the S3 Helper" (Rules.md rev 5).** Codifies the previously-implicit upload convention: every user file (avatars, KYC docs, host intro videos, screenshots, attachments) MUST be stored in DigitalOcean Spaces via `supabase/functions/_shared/s3.ts` (`uploadObject`/`presignGet`/`deleteObject`/`buildPublicUrl`) — no direct `@aws-sdk/client-s3` use, no base64-in-DB, no container-fs storage. Sub-rules: **R-15.1** server-side validation (JWT first, ≤25 MB `MAX_FILE_BYTES`→413, MIME allow-list `ALLOWED_FILE_MIME`→415, logical `FILE_TYPES` allow-list) — never trust extension; **R-15.2** server-generated owner-scoped collision-proof keys (`hosts/<host_id>/<file_type>/<ts>-<uuid>.<ext>`, `avatars/<auth_user_id>/<ts>-<uuid>.<ext>`, `kyc/<user_id>/<doc>.<ext>`) — never client-supplied; **R-15.3** visibility tiers — `public-read` only for avatars/profile pictures, `private` (default) for KYC/intro videos/screenshots/attachments (presigned URLs at read); **R-15.4** persist the `s3_key` as source of truth, mint URLs on read, delete object on row delete.

[2026-06-17] [A-01] [BUILD] **Host profile picture upload (onboarding + settings) + 2-minute onboarding intro video — all to S3 per R-15.** Backend `supabase/functions/host/index.ts`: (1) new `POST /host/avatar-temp` — onboarding-wizard avatar upload that runs *before* a `show_hosts` row exists (keys `avatars/<auth_user_id>/…`, image-only `AVATAR_MIME`, `public-read`, returns public URL); (2) `POST /host/apply` now persists an optional `avatar_url` (only when supplied, so a re-apply never wipes it); (3) new `POST /host/me/avatar` — direct profile-picture replace for any non-suspended host (incl. pending, so admins see it during review), updates `show_hosts.avatar_url`, `public-read`; (4) `POST /host/me/files` relaxed so `intro_video` + `avatar` types may be uploaded while still **pending** (other types still require approved), and `avatar` files store `public-read` while everything else stays `private`. Host-app: **`onboarding/apply/actions.ts`** — `uploadAvatarTempAction` (→`/host/avatar-temp`), `avatar_url` added to apply body, post-apply redirect now goes to `/onboarding/intro-video` (was `/dashboard`). **`onboarding/apply/wizard.tsx`** — new "Photo" step (5th) with `AvatarPicker` (preview + `useTransition` upload) and the Review card moved onto it; hidden `avatar_url` field. **`onboarding/intro-video/{page.tsx,actions.ts}`** (new) — client recorder: teleprompter shows a sample question + 4 options, the correct answer auto-reveals 10 s after recording starts; `MediaRecorder` webm/mp4, 2-minute auto-stop, ~1 Mbps to stay under 25 MB; preview/re-record; `uploadIntroVideoAction`→`POST /host/me/files` (`file_type=intro_video`, private); "Skip for now" → dashboard. **`(app)/settings/{page.tsx,actions.ts}`** — profile card shows avatar (or placeholder) + upload form; `uploadAvatarAction`→`/host/me/avatar`; `Host` iface gains `avatar_url`. **Rule compliance:** R-15 (all uploads via `uploadObject`, server-side size/MIME/type validation, server-generated keys, avatars `public-read`/video `private`), R-03 (JWT validated), R-06, R-14 (host-app reads/writes via the host Edge Function, no Supabase SDK). Host-app passes `tsc --noEmit`; edge function + all touched files: no IDE diagnostics. **Deploy (HUMAN):** `docker compose up -d --build --force-recreate api host` (no migration — `show_hosts.avatar_url` already exists).

[2026-06-17] [A-01] [BUILD] **Dashboard: "My Shows" card + studio color palette.** (1) New "My Shows" card on the host-app dashboard: filters `upcomingGames` for `host_assignment_status==="accepted"`, shows confirmed count, links to `/games?tab=upcoming`. Only visible for approved hosts when count > 0. (2) Studio palette in `globals.css`: background → `#09070E` (near-black stage depth), primary → `#D4860A` (warm amber-gold spotlight), primary-2 → `#9333EA` (vivid violet stage light), muted → `#6E6882`, text → `#F2EFFE`. Background gradient: overhead amber-gold spotlight glow at top + dual violet corner fills — dramatic broadcast-studio backdrop. `themeColor` in `layout.tsx` and `manifest.webmanifest` updated to match new bg.

[2026-06-17] [A-01] [BUILD] **Invitations page now surfaces direct admin assignments (unifies the two assignment channels in one host-app screen).** Context: a host can be offered a game two ways — an explicit `host_invitations` row (admin "Invite") OR a direct assignment (`games.host_id` set + `host_assignment_status='pending'` via the admin host-picker). Previously the `/invitations` page only rendered `host_invitations`; direct assignments were visible only under `/games?tab=upcoming`. Changes (host-app, no backend change — `/host/games/upcoming` already returns `host_id`+`host_assignment_status`): **(1) `invitations/page.tsx`** — now fetches `/host/invitations` AND `/host/games/upcoming` in parallel; `pendingAssignments` = upcoming games with `host_assignment_status==='pending'` render as Accept/Reject cards ("Assigned by admin" badge + optional reject note) above the `host_invitations` cards; empty-state + header count (`totalPending`) include both. **(2) `invitations/actions.ts`** — new `acceptAssignmentAction`/`rejectAssignmentAction` calling the existing `POST /host/games/:id/accept|reject` endpoints, revalidating `/invitations` + `/games` + `/dashboard` and redirecting back to `/invitations`. **(3) `dashboard/page.tsx`** — the Invitations card now always links to `/invitations` (was smart-linking to `/games?tab=upcoming`), since that page now shows assignments too; the combined `pendingNotifications` badge is unchanged. **Rule compliance:** R-06 (no reverse imports), R-14 (no Supabase SDK — host-app reads via the host Edge Function). Host-app passes IDE typecheck (no diagnostics). Deploy: `docker compose up -d --build --force-recreate host`.

[2026-06-17] [A-01] [FIX] **dashboard: always show "Apply for shows" + correct Invitations count.** Two issues in `host-app/src/app/(app)/dashboard/page.tsx`: (1) "Apply for shows" card was hidden when `availableCount === 0` — now always visible to approved hosts with context-aware subtitle ("No shows open right now" vs count). (2) "Invitations" count only counted `host_invitations` with `status='sent'`; games where admin directly assigned the host (`host_assignment_status='pending'`) were invisible. Fix: added `host_assignment_status` to the dashboard `Game` interface, compute `pendingAssignments` from the already-fetched `upcoming` games, combine with `pendingInvites` for a single `pendingNotifications` count; badge turns amber when > 0. The card also smart-links to `/games?tab=upcoming` when there are pending assignments (so the host can act immediately) and falls back to `/invitations` otherwise. Deploy: `docker compose up -d --build --force-recreate host`.

[2026-06-17] [A-01] [BUILD] **surface host assignment status identically in admin panel + host app.** Per spec "both the Host App and Admin Panel must always display the same assignment status based on the backend state". The host-side Accept/Reject card already existed (`host-app/.../games/[id]/page.tsx`); this adds the missing status visibility, all driven from the backend `games.host_assignment_status` (no UI-inferred/cached state). **(1) `admin/src/components/status-badge.tsx`** — added `accepted`→`success` and `unassigned`→`muted` tones, an optional `label` prop, and an exported `hostAssignmentLabel()` helper (`pending`→"Pending host confirmation", `accepted`→"Accepted", `rejected`→"Rejected", `unassigned`→"Unassigned"). **(2) `admin/src/app/(app)/games/page.tsx`** — games list query now selects `host_id, host_name, host_assignment_status`; new **Host** column renders the host name + assignment-status badge (or "Unassigned"). **(3) `admin/src/app/(app)/games/[id]/page.tsx`** — Host info section renders the assignment-status badge under the host name. **(4) `host-app/src/app/(app)/games/page.tsx`** — `Game` interface gains `host_id`/`host_assignment_status`; the Upcoming tab cards now show a "Pending host confirmation" (amber) or "✓ Approved" badge from the backend status (the `/host/games/upcoming` endpoint already returns `host_assignment_status`). **Rule compliance:** R-06 (no reverse imports), R-14 (no Supabase SDK added — admin uses existing server client, host-app uses PostgREST via the host Edge Function). Admin + host-app pass IDE typecheck (no diagnostics). **Deploy (HUMAN):** `docker compose up -d --build --force-recreate admin host`.



[2026-06-17] [A-01] [FIX] **host-app showed stale "Approved" badge after admin unassigned a host.** Repro: host requests a game → admin approves (`host_game_requests.status='approved'`, game `host_id` set) → admin unassigns the host → game correctly returns to the host-app "Available" tab but still rendered the "✓ Approved" badge. Root cause: the badge in `host-app/src/app/(app)/games/page.tsx` is built from any request with status `pending|approved`, but neither unassign path (`assignGameHost(null)` in `hosts.ts`, `updateGame(host_id:null)` in `games.ts`) cleared the `approved` `host_game_requests` row — it stayed approved (and also kept counting against `check_host_schedule_conflict`/INV-17). Fix is at the DB layer for full coverage: **Migration `20260617020000_close_host_offers_on_unassign.sql`** extends the existing `close_stale_host_offers_on_assign()` trigger (which only handled the assign direction, host_id NULL→NOT NULL) to also handle the **removal/replace direction** (host_id NOT NULL→NULL or switch to a different host): the previous host's own `pending|approved` `host_game_requests` row → `cancelled`, and their `sent|accepted` `host_invitations` row → `expired`. Includes a one-time backfill cancelling/expiring rows already left stale by the pre-fix paths (any pending/approved request or sent/accepted invitation whose game no longer has that host). No app-code change needed — the trigger fires on every `UPDATE OF host_id`. Docs: `docs/host-logic.md` §4d (new) + §10 file index updated. **Rule compliance:** R-05 (lifecycle tables, not the financial ledger — status updates permitted), R-06 (no reverse imports), R-12 (migration via `db-maintainer` only). **Deploy (HUMAN):** `docker compose up -d --force-recreate db-maintainer`.



[2026-06-17] [A-01] [FIX] **host assignment status consistency — request-approval lands accepted, direct-assign lands pending.** `admin/src/lib/actions/hosts.ts`: (1) `claimGameHost()` (used by `reviewHostRequest` approve) now sets `host_assignment_status='accepted'` — a host who *requested* a game and is approved goes straight to "Start hosting" (no redundant accept step). (2) `assignGameHost()` direct-assign now sets `host_assignment_status='pending'` (host must Accept/Reject) and clear/unassign sets `host_name=null` + `host_assignment_status='unassigned'`. The primary UI paths `createGame`/`updateGame` already set `pending` correctly. NOTE: games assigned **before** this feature were backfilled to `accepted` by `20260617000000` (by design) — to test the accept/reject flow on such a game, unassign then re-assign the host. Admin passes `tsc --noEmit`. Deploy: `docker compose up -d --build --force-recreate admin`.



[2026-06-17] [A-01] [FIX] **host-app: missing host_id in API response — fixed assigned hosts seeing "Apply" form.** Updated SELECT statements in `supabase/functions/host/index.ts` for `/host/games/upcoming` and `/host/games/history` to include `host_id` and `host_assignment_status`. This allows `host-app/src/app/(app)/games/[id]/page.tsx` to correctly resolve `isAssigned = game.host_id === host.id`, which now properly renders the **Accept assignment / Reject assignment** card and suppresses the redundant application form for already-assigned hosts. Deploy: `docker compose up -d --build --force-recreate api`.



[2026-06-17] [A-01] [BUILD] **`requires_host` flag — exclude no-host games from the host-app available list.** Adds a per-template / per-game boolean that controls whether a game needs a human host; automated / AI-presented games can now be hidden from the host application so hosts cannot apply for / be offered them. **(1) Migration `20260617010000_game_requires_host.sql`** — adds `requires_host BOOLEAN NOT NULL DEFAULT TRUE` to both `game_templates` and `games` (DEFAULT TRUE preserves current behaviour — existing live games keep appearing in the host app). **(2) Migration `20260617010100_generate_game_copies_requires_host.sql`** — reinstalls `generate_game_from_template()` to copy `v_tpl.requires_host` onto each generated game (all other logic verbatim from `20260617000100`). **(3) `supabase/functions/host/index.ts`** — `GET /host/games/available` now adds `.eq("requires_host", true)` so only games that need a host are listed. **(4) `admin/src/lib/actions/games.ts`** — `GameSchema` gains `requires_host: z.boolean().optional()` (flows into the insert/update via the existing spread). **(5) `admin/src/lib/actions/templates.ts`** — `TemplateSchema` gains `requires_host: z.boolean().default(true)`. **(6) Admin UI** — a "Requires a host" toggle added to `create-game-dialog.tsx`, `edit-game-form.tsx` (`EditableGame` extended), `create-template-dialog.tsx`, and `edit-template-form.tsx` (`EditableTemplate` extended); edit forms read the value from the `*`-selected row. **Rule compliance:** R-06 (no reverse imports), R-12 (both migrations via `db-maintainer` only), R-14 (no Supabase SDK added). Admin passes `tsc --noEmit`. **Deploy (HUMAN):** (a) `docker compose up -d --force-recreate db-maintainer` (applies 20260617010000 + 20260617010100), (b) `docker compose up -d --build --force-recreate api admin`.



[2026-06-17] [A-01] [BUILD] **Host fee / commission + direct-assignment accept-reject flow (human-approved: numeric fields, payout-at-settlement model, reject-returns-to-pool, email+in-app notifications).** End-to-end host compensation + assignment-decision feature spanning the DB, Edge Function, and both Next.js apps. **(1) Migration `20260617000000_host_fee_commission_assignment.sql`** — adds `host_fee NUMERIC`, `host_commission_pct NUMERIC`, `show_host_fee BOOLEAN`, `show_host_commission BOOLEAN` to both `games` and `game_templates`; adds `games.host_assignment_status TEXT NOT NULL DEFAULT 'unassigned'` (CHECK `unassigned|pending|accepted|rejected`); backfills existing host-assigned games to `accepted` so current hosts keep their "Start hosting" CTA; installs `record_host_earning(p_game_id)` (computes host_fee + total-income × commission%, inserts a **pending** `host_earnings` row — admin still approves the credit, preserving INV-16 / R-05 append-only) fired by an `AFTER UPDATE OF prizes_distributed_at` trigger so payout is captured atomically at settlement. **(2) Migration `20260617000100_generate_game_copies_host_fee.sql`** — `generate_game_from_template()` now copies the four compensation columns onto each generated game. **(3) `supabase/functions/host/index.ts`** — `available`/`upcoming`/`history` SELECTs carry the four new columns (+`host_assignment_status` on `upcoming`); new `maskFeeFields()` nulls `host_fee`/`host_commission_pct` when the matching `show_*` flag is false and strips the control flags before returning (admin can hide compensation from the host). New `POST /host/games/:id/accept` (pending→accepted, in-app `host_invite` notification + best-effort admin email) and `POST /host/games/:id/reject` (returns game to pool: clears `host_id`/`host_name`, sets `unassigned`, admin email with optional reason). Invitation-accept path also stamps `host_assignment_status='accepted'`; stream-session live gate now blocks while status is `pending`. **(4) `admin/src/lib/actions/games.ts`** — `GameSchema` extended with the four fields; `createGame` writes `host_assignment_status='unassigned'`, sets `pending` + fires `notifyHostAssigned()` when a host is assigned; `updateGame` sets `pending` on (re)assign / `unassigned` on removal and notifies the newly-assigned host (in-app `host_invite` + branded email linking to the host portal). **(5) `admin/src/lib/actions/templates.ts`** — `TemplateSchema` extended (defaults: fee 0, commission 0, both `show_*` true); persisted via existing spread insert/update. **(6) Admin UI** — host fee/commission numeric inputs + show-to-host toggles added to `create-game-dialog.tsx`, `edit-game-form.tsx` (`EditableGame` type extended), `create-template-dialog.tsx`, and `edit-template-form.tsx` (`EditableTemplate` type extended). **(7) Host-app** — `games/[id]/page.tsx` renders Host fee / Commission tiles only when present (i.e. admin enabled visibility), and shows an Accept/Reject card when `host_assignment_status='pending'` (reject takes an optional reason); "Start hosting" CTA now gated on `accepted`. New `acceptGameAction`/`rejectGameAction` server actions in `games/actions.ts`. **Notification type:** uses existing `host_invite` category (no new CHECK value needed; `host_assignment` is NOT permitted by `notifications_type_check`). **Rule compliance:** R-02 (NUMERIC matches existing schema convention), R-05 (earnings row inserted once as pending; `transactions`/credit still admin-approved, never UPDATE/DELETE), R-09 (assignment claim stays a single conditional UPDATE), R-10 (A-05 financial/game-logic territory — human pre-approved the four decisions), R-12 (both migrations via `db-maintainer` only), R-14 (no Supabase SDK added to host service). Both apps pass `tsc --noEmit`. **Deploy (HUMAN):** (a) `docker compose up -d --force-recreate db-maintainer` (applies 20260617000000 + 20260617000100), (b) `docker compose up -d --build --force-recreate api admin host`.



[2026-06-17] [A-01] [FIX] **host: failed_to_list_games + missing manifest/favicon.** (1) `supabase/functions/host/index.ts` — `GET /host/games/available` selected `host_payout` which is not a column on `games` (it only appears in the wallet transaction-type enum); removed it. `GET /host/games/history` selected `participant_count` which does not exist — the column is `total_participants`; corrected. (2) Added `host-app/public/manifest.webmanifest` (name, short_name, start_url, display standalone, theme/bg color, icon refs) — resolves all 404s for `https://host.quiz4win.com/manifest.webmanifest` that were spamming the console. (3) Added `host-app/public/favicon.ico` (minimal 16×16 purple ICO) — resolves `favicon.ico 404`. Deploy: `docker compose up -d --build --force-recreate host`.


[2026-06-09] [A-01] [FIX] **admin host picker — `total_shows_hosted` does not exist; surface API errors + clearer empty states.** Two coordinated fixes from a single field-name typo that cascaded into both apps. **(1) admin host picker "Failed to load hosts"** — root cause: `listApprovedHosts()` in `admin/src/lib/actions/hosts.ts` selected and ordered by `total_shows_hosted`, but the actual `show_hosts` column (from the original `20260522120000_initial_schema.sql`) is **`shows_hosted`**. PostgREST returned the unknown-column error, the server action propagated `{ ok: false }`, and the Dialog displayed the generic "Failed to load hosts". Fix: rename the SELECT + ORDER BY + interface field to `shows_hosted` everywhere, propagate `error.message` back through the action's return type, and log `console.error("[hosts] listApprovedHosts failed:", error.message)` server-side for future debugging. Picker UI now wraps the server action in try/catch and renders the real message (e.g. `Failed to load hosts: column "x" does not exist`) so future schema drift is immediately visible. **(2) admin picker empty state** — when no rows match (e.g. fresh deployment where no host application has been approved yet) the picker now renders an inline "No approved hosts yet · Review applications →" link to `/host-applications` instead of a dead-end "No approved hosts yet" message. **(3) host-app games tab "did not load"** — root cause: the previous commit silently swallowed every API failure (`games = []`), so a 500/timeout/auth error rendered exactly like an empty-but-successful response. Fix: `host-app/src/app/(app)/games/page.tsx` now derives a `listErr` string from `listRes.status` + `listRes.error` (mapping 401 to "Your session expired — please sign in again" and ignoring 403 host_not_approved since that's expected on the read endpoints when a host hasn't been approved yet). The error renders above the list as a rose-tinted banner. Empty-state copy was also expanded: the Upcoming tab now reads "No shows assigned to you yet. You'll see games here once an admin approves your request or assigns you directly." with an inline "Browse available shows →" deep-link to `/games?tab=available`. The Available tab clarifies that admins schedule the live shows hosts can apply for. **No backend change** — `/host/games/upcoming` / `/host/games/available` / `/host/games/requests` were never broken; the user was seeing the legitimate empty state (no games assigned to their pending host account) and the swallowed-error UI. No R-11 surfaces touched. Verified end-to-end with `npx next build --no-lint` on both `admin/` and `host-app/`. Deploy: `docker compose up -d --build --force-recreate admin host`.


[2026-06-09] [A-01] [BUILD] **host-app — application-tracking tab + "Requested ✓" badge + dashboard discovery card.** The flow (browse Available → apply → admin reviews) was already wired end-to-end, but the host had no way to see WHICH games they'd already applied for or what state each application was in. Polished the experience: (1) **`host-app/src/app/(app)/games/page.tsx`** — added a 4th tab "Requests" that lists every host_game_request with status pill (pending/approved/rejected/cancelled), withdraw button for pending, and an admin-note line for rejected ones. The Requests tab title carries a small pending-count badge. The Available tab now parallel-fetches `/host/games/requests` and decorates each card with a `✓ Requested` / `✓ Approved` pill when the host has already applied; cards with no existing request now show a "Tap to apply →" affordance hint. The Available tab also surfaces `prize_pool` on cards when `host_payout` isn't set, so hosts have at-a-glance signal for which shows are worth applying to. (2) **`host-app/src/app/(app)/games/[id]/page.tsx`** — detail page now parallel-fetches the host's requests and: shows a "Request pending review" card with Withdraw button when a pending request exists (the apply form is suppressed); shows a small "Your earlier request was rejected/cancelled" hint above the apply form for closed-but-replayable applications; shows an amber "Approval required" notice when the host is approved=false and tries to view an available game. (3) **`host-app/src/app/(app)/games/actions.ts`** — `cancelRequestAction` now revalidates `/games` with the `"layout"` scope so both the games list and any open detail page pick up the new state, and redirects with an error param when the API rejects (e.g. status already changed). (4) **`host-app/src/app/(app)/dashboard/page.tsx`** — adds two new discovery cards: a primary-tinted "Apply for shows · N open" card surfaced only for approved hosts when `/host/games/available` returns >0 (deep-links to `/games?tab=available`), and a "My applications · N pending" card when the host has any pending requests (deep-links to `/games?tab=requests`). Both share the existing Mail/Wallet/Gamepad card visual language. **No backend change** — every endpoint (`/host/games/available`, `/host/games/requests`, `POST /host/games/:id/request`, `DELETE /host/games/requests/:id`) was already implemented and exposed. No R-11 surfaces touched. Deploy: `docker compose up -d --build host`.


[2026-06-09] [A-01] [BUILD] **Admin — game ↔ host assignment modal + global host-requests queue.** Closes the loop on the host-platform flow: admins can now pick a Quiz4Win host directly from the create/edit game dialogs, and there's a global queue where every host request (across all games + all hosts) can be approved/rejected in one place. **(1) Reusable picker** — `admin/src/components/host-picker-dialog.tsx` is a searchable Dialog backed by a new `listApprovedHosts(query, limit)` server action that returns approved+active `show_hosts` rows (id, name, avatar, country, languages, total_shows_hosted). The list filters by name with ILIKE, defaults to ranking by total_shows_hosted DESC, and exposes Unassign + Close controls. **(2) Game schema + actions** — `admin/src/lib/actions/games.ts` `GameSchema` extends with `host_id: z.string().uuid().nullable().optional()`. New private helper `resolveHostAssignment(db, hostId, gameId)` validates approved/active status, runs `check_host_schedule_conflict` RPC (INV-17) when a game_id exists, and returns the canonical host name for denormalised storage on `games.host_name`. `createGame()` now inserts the game first WITHOUT host_id, then atomically claims it via UPDATE…WHERE host_id IS NULL after the conflict check — so a created game is never silently mis-assigned (and on conflict the game is still created, just unassigned, with a clear message). `updateGame()` only re-validates when `host_id` actually transitions, supports clearing (`null` → drops host_name) and switching, and auto-fills host_name from `show_hosts.name` when not explicitly overridden. **(3) Create-game dialog** — `admin/src/app/(app)/games/create-game-dialog.tsx` Styling & Host tab gains a full-width "Assigned host" picker above the existing host_name/host_title text inputs; the text input now reads as a Display name override and falls back to the picked host's name on submit. **(4) Edit-game form** — same picker wired into `admin/src/app/(app)/games/[id]/edit/edit-game-form.tsx`, pre-seeded from `game.host_id`. **(5) Global host requests** — new `admin/src/app/(app)/host-requests/page.tsx` lists every `host_game_requests` row with status tabs (Pending / Approved / Rejected / Cancelled / All — each with a count badge), name+game search, and reuses the existing `RequestActions` client component from the host-detail page (which already calls `reviewHostRequest()` — INV-17 + atomic host_id claim + notification). Linked to from nav with the ListChecks icon; ROUTE_ROLES allows super_admin/admin/moderator. **(6) `assignGameHost()` server action** — also added in `actions/hosts.ts` as a direct admin path that bypasses the request flow (used as the underlying call by the picker on save, but also reusable from any other admin context). **Host-app side** — already complete from prior commits: `GET /host/games/available` (unassigned upcoming live games), `GET /host/games/upcoming` (host's assigned games), `POST /host/games/:id/request` (apply to host). `host-app/src/app/(app)/games/page.tsx` already shows the three tabs (available / upcoming / history) and the detail page surfaces the Request-to-host CTA for approved hosts. No backend migration needed — `games.host_id` FK + `check_host_schedule_conflict` RPC + `trg_close_stale_host_offers_on_assign` trigger were all already in place. No R-11 surfaces touched. Deploy: `docker compose up -d --build --force-recreate admin`.


[2026-06-09] [A-01] [BUILD] **host-app payout methods — crypto-only with visual network grid + QR scan.** Replaced the old multi-field "Payment methods" form (bank/IBAN/PayPal/crypto mix-bag) with a modern crypto-only UX: existing methods render as gradient-tinted wallet cards (network badge + chain + masked address `TXyz…123abc`); the add flow is a two-step picker — Step 1 shows a 2-col gradient card grid of 10 networks (USDT TRC20/ERC20/BEP20/Polygon, BTC, ETH, TRX, BNB, SOL, TON), each card colored with the network's brand gradient and showing the token badge + chain name; Step 2 shows the selected network header, a single Address field with three input paths (camera QR Scan / Paste from clipboard / manual entry), client-side address pattern check per network, optional Memo field (TON only), label, default-toggle, and an amber "double-check the address — funds sent to a wrong network are permanently lost" warning. **New files:** `host-app/src/lib/data/crypto-networks.ts` (10 networks with brand colors, addressPattern regex, hint text, examplePrefix, supportsMemo + `parseAddressFromQr()` helper that strips BIP21/EIP681 URI schemes like `ethereum:`/`bitcoin:` and query strings so wallet-app QRs always reduce to a bare address); `host-app/src/components/qr-scanner.tsx` (bottom-sheet camera modal — dynamic-imports the `qr-scanner` npm package so it never lands in SSR, renders a 1:1 video with viewfinder overlay, gracefully shows "Camera permission denied"/"unavailable" with the manual-entry fallback intact, properly stops+destroys the stream on close/unmount); `host-app/src/app/(app)/payment-methods/add-wallet-form.tsx` (client component holding the two-step state + clipboard reader + scan modal). **Backend:** migration `20260609000500_host_payment_methods_crypto_networks.sql` relaxes the `host_payment_methods.method_type` CHECK to add `usdt_bep20`/`usdt_polygon`/`eth`/`trx`/`bnb`/`sol`/`ton` (legacy iban/bank_account/paypal kept in the list so historical rows remain valid). `supabase/functions/host/index.ts` `PAY_METHODS` set updated to mirror — the API no longer accepts new bank/paypal inserts (frontend doesn't surface them either). Admin verification UI unchanged; `account_details` JSON now always carries `{ network, address, memo? }`. **Dependency:** `host-app` adds `qr-scanner@1.4.2` (~50 KB minified, MIT) via `npm install`; the worker file is copied to `host-app/public/qr-scanner-worker.min.js` as defensive insurance though qr-scanner 1.4 handles worker bundling automatically with Webpack 5 / Next.js 15. No R-11 surfaces touched. Deploy: `docker compose up -d --force-recreate db-maintainer` then `docker compose up -d --build host`.


[2026-06-09] [A-01] [BUILD] **host-app onboarding — multi-step wizard with searchable country/dial-code comboboxes.** Replaced the long single-form "Tell us about you" page with a 4-step wizard: (1) Basics — display name + searchable country combobox + dial-code combobox + national number; (2) Languages — chip-style multi-select toggle (min 1); (3) About — tagline + bio; (4) Socials & Review — six optional social URLs + a read-back review card; per-step inline validation gates `Continue`. New files: `host-app/src/lib/data/countries.ts` (full ISO 3166-1 alpha-2 list ~245 entries with E.164 dial codes + `flagEmoji(code)` helper computed from regional-indicator symbols — no unicode glyphs in source), `host-app/src/components/ui/combobox.tsx` (controlled searchable dropdown primitive with keyboard nav ↑/↓/Enter/Esc, click-outside close, focus-trap on search, hidden `<input>` mirror so it submits through any `<form action={…}>`), `host-app/src/app/onboarding/apply/wizard.tsx` (client component holding step + per-field state + validation + a hidden-input shadow of every field so the final submit posts ALL collected data as one FormData payload through the existing `applyAction`). Page wrapper `onboarding/apply/page.tsx` is now a thin server component that runs the existing "already pending/approved → /dashboard" guard then renders `<ApplyWizard initialError={sp.error} />`. Phone is composed as `${dial} ${national-digits-only}` and sent as the existing `phone` text field; country submits the human-readable NAME ("Germany") rather than the ISO code so the admin panel renders it unchanged. **No backend/migration change** — `applyAction` and `/host/apply` were already accepting these fields as free text. R-11 surfaces untouched. Deploy: `docker compose up -d --build host`.


[2026-06-09] [A-01] [FIX] **host-app signup email — OTP-only flow + already-confirmed detection (root cause of "Code expired" on a freshly-issued code).** Diagnosed: the confirmation email shipped BOTH the magic-link button and the 6-digit OTP, but they're derived from the same single-use GoTrue token. Mail-scanners / link-preview prefetchers (Gmail preview, corporate scanners, Brevo click-tracking) silently prefetch the link → GoTrue consumes the token → the OTP the user types ~2 minutes later is now "expired/invalid" even though TTL hasn't elapsed. Two-part fix: **(1) Backend** — `supabase/functions/_shared/email.ts` `confirmEmailTemplate` extended with `otpOnly?: boolean` that swaps the CTA-button block for an OTP-only block (no actionUrl rendered anywhere; new hero copy "Enter the code below"; preheader/text-part both rewritten). `supabase/functions/auth/index.ts` `/auth/signup` and `/auth/resend-confirmation` accept `flow:"otp"` and request the OTP-only template — eliminates the prefetch attack surface entirely for clients that don't need the magic-link UX. `/auth/verify-otp` now detects the prefetch-race retroactively: when GoTrue returns expired/invalid for `type='signup'`, the function calls a new `auth_email_confirmed(p_email)` RPC (migration `20260609000400_auth_email_confirmed_rpc.sql`, SECURITY DEFINER, service_role-only EXECUTE — bridges the un-exposed auth schema) and if the email is already confirmed returns HTTP 409 `already_confirmed`. **(2) host-app** — `signup/actions.ts` now sends `flow:"otp"`; `verify-otp/actions.ts` short-circuits on `already_confirmed` and redirects to `/signin?info=Email verified — please sign in`; `resend-otp` also requests `flow:"otp"` so the resent email matches. `signin/page.tsx` renders an emerald `?info=` banner so users see the positive confirmation rather than the inherited error styling. Customer flow (`app.quiz4win.com`) is unchanged — it still gets the link+OTP email because `flow` is omitted. R-01 (no secrets surfaced; `already_confirmed` does not leak account existence beyond what `/auth/signup` already does), R-04 (RPC locked to service_role), R-12 (migration applied via `db-maintainer`). Deploy: `docker compose up -d --force-recreate db-maintainer` then `docker compose up -d --build --force-recreate api host`.


[2026-06-09] [A-01] [AUDIT] Session ended. Tasks completed: Host Platform Tracks A + B + C delivered with five audit-driven fixes. Remaining: human deploy (db-maintainer + api + admin) + manual run of Deno/Python/SQL test suites.

[2026-06-09] [A-01] [BUILD] **Track C — host-platform test suite.** Three artefacts: (1) `supabase/functions/_shared/livekit.test.ts` — Deno unit test for `signAccessToken` validating three-segment JWT structure, HS256 header, full payload claim shape (iss/sub/iat/exp/nbf/video.{room,roomJoin,canPublish,canSubscribe,canPublishData}), HMAC signature verification with the real secret AND defensive negative test with a wrong secret, plus default-grant assertions for the viewer case. Run with `deno test --allow-env supabase/functions/_shared/livekit.test.ts`. (2) `scripts/host-platform-smoke-test.py` — production smoke test that signs in with the shared TEST_USER_* account (AGENTS.md §8), then probes 9 GET endpoints under `/host/*`, the admin-side `/admin/hosts` negative case, and a non-host write attempt. Verified end-to-end against `https://api.quiz4win.com` — sign-in succeeded; all `/host/*` paths currently return generic API 404 because the new routes have not yet been deployed. (3) `scripts/host-platform-sql-tests.sql` — four DO-block tests wrapped in BEGIN/ROLLBACK validating `check_host_schedule_conflict` (overlap → TRUE; disjoint → FALSE) and the new `trg_close_stale_host_offers_on_assign` trigger (losing request auto-cancelled, losing invitation auto-expired). All assertions use plpgsql `ASSERT` so failures abort the script under `ON_ERROR_STOP`.

[2026-06-09] [A-01] [BUILD] **Track B — admin host-detail page + actions.** New `admin/src/app/(app)/hosts/[id]/page.tsx` (server component) rendering profile card, social links, status badges, lifetime/pending/approved/paid/cancelled earnings summary, and a tabbed work area: Files / Requests / Invitations / Earnings / Payout Methods. Each tab pulls its own slice in parallel and exposes inline review buttons. Companion `host-actions.tsx` (client component) provides a single `ConfirmDialog` primitive plus eight action sets: `HostStatusActions` (approve/reject/suspend/reactivate), `FileActions`, `RequestActions`, `InvitationActions` (cancel), `EarningActions` (approve/cancel), `PaymentMethodActions` (verify/reject), `InvitationSendButton` (select unassigned upcoming live game, optional message + expiry), `EarningCreateButton` (game + amount + currency + note). All wire to the server-actions in `admin/src/lib/actions/hosts.ts`. Side-fixes while in the file: the existing list page had `<StatusBadge status=…>` (the prop is `value`) and `<EmptyState>` missing its required `icon`, and `<DataTablePagination>` was being passed the wrong props — all three pre-existing bugs corrected. Verified with `npx next build --no-lint` — admin compiles cleanly.

[2026-06-09] [A-01] [FIX] **Host Platform audit fixes (CRITICAL).** Migration `20260609000300_host_platform_followup_fixes.sql` ships three corrective DB changes uncovered while auditing Phases 1–8 implementation: (1) **admin_audit_log column rename** — the table was created with `entity_type` / `entity_id` but every Edge Function (admin-auth/-finance/-game-templates/-host-applications/-users/-hosts/-questions/-vouchers/-config/-kyc) writes `target_type` / `target_id`, meaning **every audit INSERT in the codebase has been silently failing in production**. Migration renames the columns + their indexes so the existing code finally writes successful rows. `details` keeps its original name; the new admin-side `audit()` helper is updated to write `details` (was `metadata`). (2) **`approve_host_earning_atomic(p_earning_id, p_admin_id)` SECURITY DEFINER RPC** — replaces the non-existent `increment_wallet_balance` call. Performs the entire INV-16 flow inside a single plpgsql block with `FOR UPDATE` locking: validates pending status, inserts the transactions row (R-05 append-only), credits `profiles.wallet_balance`, bumps `show_hosts.total_earnings`, and flips `host_earnings` → approved with the new `transaction_id`. Any failure rolls everything back, so partial-credit states are impossible. Both `supabase/functions/admin-hosts/index.ts` and `admin/src/lib/actions/hosts.ts` now call this RPC instead of the broken inline flow. (3) **`trg_close_stale_host_offers_on_assign` trigger** — when `games.host_id` flips from NULL → NOT NULL, the trigger cancels every other pending `host_game_requests` row and expires every other sent `host_invitations` row for the same game. Combined with two additional code-side guards — conditional `UPDATE games SET host_id=… WHERE host_id IS NULL` in both `admin-hosts` request-approve and `host` invitation-accept — this kills the previously possible race where two hosts could double-claim a game. Two more code fixes shipped in the same commit: (4) the `host/index.ts` stream-session **/live** endpoint now upserts the session row (the UPDATE was silently no-op when the row was missing) and rejects going live unless `games.status ∈ {upcoming,open,live}`; (5) admin approval of an `avatar` `host_uploaded_files` row now propagates the URL onto `show_hosts.avatar_url`. Deploy: `docker compose up -d --force-recreate db-maintainer` (for the migration), `docker compose up -d --build --force-recreate api`, and `docker compose up -d --build --force-recreate admin`.

[2026-06-08] [A-01] [AUDIT] Session ended. Tasks completed: [Pre-resolved question buffer (depth 5) + no trailing pause after final question]. Remaining: deploy (commit/push + rebuild game-orchestrator) pending human go-ahead.

[2026-06-08] [A-01] [BUILD] **Pre-resolved question buffer — removes inter-question lag.** Reworked `deploy/game-orchestrator/orchestrator.ts` so the pre-generation buffer holds **fully-resolved** questions instead of raw `GenQuestion` objects. (1) `questionQueue` is now `Map<string, ResolvedQuestion[]>` (`ResolvedQuestion = {q, id}`). (2) `prefillQueue` now does generate → `resolveUniqueQuestion` (claim + dedup + regenerate) → enqueue, so all OpenAI/DB latency happens in the background during the previous question's answer window; depth is the new `QUESTION_BUFFER_TARGET` env (default **5**, was hardcoded 3). Mutual distinctness of buffered questions is guaranteed because `resolveUniqueQuestion` marks each accepted question asked in Redis before the next prefill iteration seeds its avoid-list. (3) The inter-question hot path in both `advanceQuestion` (auto) and `handlePrepareQuestion` (presenter) is now just *pop → write Redis → broadcast* — no `claim`/regeneration on the critical path. Starvation behaviour unchanged (await prefill, finalize if still empty). (4) `closeQuestion` post-final-question delay changed from a fixed 3 000 ms to **0** — the game finalizes immediately after the last `QUESTION_CLOSED`. Docs (`game-logic.md` §3 timeline, timing table, §12 config table, dedup section) updated; added `QUESTION_BUFFER_TARGET` row and pre-resolved-buffer description. No DB/migration/financial impact; no R-11 surfaces touched. IDE reports no diagnostics. Deploy: rebuild `game-orchestrator`.

[2026-06-08] [A-01] [AUDIT] Session ended. Tasks completed: [Cap inter-question gap at 5s]. Remaining: 0 in current session focus.

[2026-06-08] [A-01] [AUDIT] Session started. Reviewing Open_Tasks_AI.md.

[2026-06-08] [A-01] [FIX] **Inter-question gap capped at 5s.** Updated `deploy/game-orchestrator/orchestrator.ts` to lower the default inter-question pause bounds to 3 000 – 5 000 ms (previously 5 000 – 10 000 ms), guaranteeing the gap between questions never exceeds the 5 s user-requested cap. Updated documentation comment to match. No env overrides found in existing config. Deploy: `docker compose up -d --build --force-recreate game-orchestrator`.

[2026-06-08] [A-01] [BUILD] **Sound Management feature.** New `app_sounds` table (migration `20260608000000_app_sounds.sql`) with usage CHECK constraint (12 slots: splash, home, home_before_start, register, game_details, correct_answer, incorrect_answer, countdown, pregame_music, winner, announcement, livestream). RLS: anon/authenticated can read active sounds; writes via service-role only. New edge functions: `admin-sounds` (GET list, POST upload multipart to same S3 bucket under `sounds/<usage>/`, PATCH update, DELETE removes S3 + DB row, audit-logged); `public-sounds` (unauthenticated GET, returns flat list + usage-grouped map). Admin panel: `/sounds` page with KPI strip, table with native `<audio>` player preview per row, active/inactive toggle, edit dialog (name + usage), delete with confirm. Nav entry "Sounds" added to Content section; ROUTE_ROLES entry for super_admin + admin. Deployment: apply migration via db-maintainer, rebuild api container.

[2026-06-08] [A-01] [BUILD] **Template generator interval decreased to 10s.** Updated `deploy/template-generator/generator.ts` to decrease the default polling/tick interval (`INTERVAL_MS`) from 60,000 ms to 10,000 ms. This allows for faster game generation, scheduling, and notifications. Updated the inline documentation to reflect the new 10s default. No DB or orchestrator changes required. Deploy: `docker compose up -d --build --force-recreate template-generator`.


[2026-06-08] [A-02] [RULE] **R-13 + R-14 established by human developer.** (1) R-13: `supabase` CLI is banned from all instructions, scripts, and documentation — only Docker Compose equivalents are permitted. (2) R-14: Supabase SDK (`@supabase/supabase-js` / `createClient`) is banned in Docker-deployed backend services (orchestrator, workers); all DB access must use the existing direct PostgREST helpers (`dbSelect`, `dbInsert`, `dbUpdate`, `dbRpc`). Both rules added to `project-brain/Rules.md`. All future agent responses must comply immediately.

[2026-06-07] [A-01] [BUILD] **Option-distribution stats on QUESTION_CLOSED + randomized inter-question gap.** Two game-pacing/UX features. (1) **Answer distribution:** added a per-question Redis hash `q4w:game:{id}:q:{idx}:optionCounts` (`redis_keys.ts` `questionOptionCounts`, added as KEYS[6] in `submitAnswerKeys`). `SUBMIT_ANSWER_SCRIPT` now `HINCRBY`s the selected `optionId` (with `EXPIRE 86400`) only on accepted (first, in-window, non-duplicate) answers, so each player counts once. `orchestrator.ts` `closeQuestion` reads the hash via the new `K.qOptions` builder, computes `{ optionId, count, percentage }` (percentage rounded to 1 dp; 0 for all when nobody answered) plus `totalAnswers`, and adds them to the `QUESTION_CLOSED` event as `answerStats`. (2) **Random inter-question gap:** replaced the hardcoded 3 000 ms auto-mode pause with a fresh uniform-random delay in `[INTER_QUESTION_MIN_MS, INTER_QUESTION_MAX_MS]` (new env vars, default 5 000–10 000 ms) via the `interQuestionGapMs()` helper. `QUESTION_CLOSED` now also carries `nextQuestionInMs` and `nextQuestionStartsAt` (null after the final question / in presenter mode) so clients can render a countdown; the authoritative start remains `QUESTION_STARTED.startsAt`. After the last question a fixed 3 000 ms pause still precedes `GAME_ENDED`. Docs (`game-logic.md` §3 timeline, timing formula, §12 config table) updated. No DB/migration/financial impact; no R-11 surfaces touched. IDE reports no diagnostics. Deploy: redeploy `game-session` edge function (Lua change) and rebuild `game-orchestrator`.

[2026-06-07] [A-01] [FIX] **Harden Sweep 1 (no-answer) with DB fallback (INV-14).** In `deploy/game-orchestrator/orchestrator.ts` `closeQuestion` handler: before the no-answer loop, a single batch `dbSelect` prefetches `wrong_count`, `lives_remaining`, `eliminated`, and `participant_role` for every user in `notAnswered`. Inside the loop, when a user's per-user Redis hash has fully expired (`status`, `wrongCount`, and `remainingLives` are all null), the code now falls back to the DB row's counters instead of silently counting from zero — eliminating under-counting when partial Redis TTL expiry occurs mid-game. Additionally, if the DB row shows the participant is already `eliminated` or demoted from `player`, they are pruned from the participants set and skipped (never double-charged). This matches the ghost-sweep's trust in the DB and ensures INV-14 (any unanswered question = missed/wrong) remains enforced deterministically regardless of Redis TTL behaviour.

[2026-06-07] [A-01] [BUILD] **Game-detail APIs expose pregame warmup timing.** The customer (`games/index.ts`) and public (`public-games/index.ts`) game-detail endpoints now surface the per-game warmup so the client can render its pregame countdown without joining first. (1) Added `start_buffer_seconds` to the shared `GAME_FIELDS` projection in both functions (kept in lock-step per the cross-file comment) — it now appears on list, history, and detail responses. (2) Both `GET /games/:id` and `GET /public-games/:id` detail branches now compute and return `pregame_duration_ms` (`start_buffer_seconds × 1000`, fallback 120 s) and `first_question_starts_at` (`start_time + start_buffer_seconds × 1000`, null when `scheduled_at` is null) — the snake_case mirror of the camelCase `pregameDurationMs`/`firstQuestionStartsAt` the orchestrator publishes to Redis once the game goes live. `is_featured` was already present in `GAME_FIELDS`, so it requires no change. No DB/migration/financial impact; no R-11 frozen surfaces touched. IDE reports no diagnostics. Deploy: redeploy the `games` and `public-games` edge functions.

[2026-06-05] [A-01] [FIX] **Branded signup email-confirmation (replaces Supabase default unbranded mail).** Two user-reported bugs: (1) the post-signup confirmation email was Supabase Cloud's default unbranded template; (2) its link redirected to `/auth/reset-password` and showed `otp_expired`. Fix mirrors the existing `forgot-password` pattern: `auth/index.ts` signup now calls `admin.auth.admin.generateLink({ type:"signup", email, password, options:{ data, redirectTo } })` instead of `supabase.auth.signUp()`. `generateLink()` creates the user + returns `action_link`/`email_otp` WITHOUT triggering Supabase's auto-email, so we send our own branded mail via Brevo through new `_shared/email.ts` `confirmEmailTemplate()` (Q4W brand shell, "Confirm email" CTA + optional 6-digit OTP fallback for link pre-consumption by mail scanners, 24h expiry copy). Redirect target is now controlled via new `EMAIL_CONFIRM_REDIRECT_URL` env (default `${APP_URL}/auth/confirm`), wired into `docker-compose.yml` (api) and `.env.docker.example`. Signup response now returns `{ user, requires_confirmation:true }` (no session until confirmed); the premature welcome email at signup was removed (was firing before confirmation). ⚠️ **R-11 SENSITIVE** — this changes the auth client on the signup path (`getPublicClient().auth.signUp` → `getAdminClient().auth.admin.generateLink`, same helper `forgot-password` already uses). NOT committed/deployed pending human approval. ACTION REQUIRED: (a) confirm/set the real app confirm route in `EMAIL_CONFIRM_REDIRECT_URL`; (b) add that URL to Supabase → Auth → URL Configuration → Redirect URLs. R-01 honoured (no secrets logged). IDE reports no diagnostics.

[2026-06-05] [A-01] [BUILD] **Service-monitor container with Brevo email alerts.** New `deploy/monitor/` module: (1) `email.ts` — self-contained Brevo transactional email helper that builds a mobile-friendly single-column inline-CSS HTML email (brand shell, service-status table with latency, OK/DOWN colour dots, recovery/alert variants); reads `BREVO_API_KEY`, `EMAIL_FROM`, `MONITOR_RECIPIENT` from env (R-01, never logged). (2) `monitor.ts` — periodic health-check loop (default 60 s) that checks: API (`API_HEALTH_URL`), Supabase REST + Auth (HTTP 200), Supabase DB (TCP to pooler), Redis (TCP AUTH + PING), RabbitMQ management API (Basic auth via `RABBITMQ_URL`), LiveKit reachability; runs all checks in parallel; transition-based alerting (email on up→down and down→up only, re-alert after `MONITOR_REALERT_MS` while still down). (3) `Dockerfile` — `denoland/deno:alpine-2.3.3` + tini, caches deps, runs with `--allow-env --allow-net` only. (4) `docker-compose.yml` — new `monitor` service (`quiz4win-monitor`, `restart: unless-stopped`, `depends_on: api`, full env mapping). (5) `.env.docker.example` — new `BREVO_API_KEY`, `MONITOR_RECIPIENT`, `EMAIL_FROM`, `MONITOR_ENV`, `MONITOR_INTERVAL_MS`, `MONITOR_TIMEOUT_MS`, `MONITOR_REALERT_MS`, `API_HEALTH_URL` entries. Deploy: `docker compose up -d --build monitor`.

[2026-06-05] [A-01] [FIX] **Restored orchestrator cleanTopic fix.** After the global revert to 0d200b6, re-applied the `cleanTopic` change: the function now returns the game title as-is (trimmed) instead of stripping a timestamp suffix, as suffixes are no longer appended since migration `20260607110000_fix_game_title_no_suffix`. This ensures the game title (which is now exactly the template name) is preserved correctly for both display and LLM reference.

[2026-06-07] [A-01] [FIX] **Game title: use template name verbatim — no suffix appended.** `generate_game_from_template()` was concatenating a timestamp suffix (`_MMDD_HH24MI`) to `v_tpl.name`, producing titles like "Daily Quiz_0607_1430". The template name is the intended game title; nothing should be added. Migration `20260607110000_fix_game_title_no_suffix.sql` replaces the function body so the INSERT uses `v_tpl.name` directly (removed the intermediate `v_title` variable). All other logic (overlap check, cron-aligned scheduling, cooldown-aware question picker, target_languages) is unchanged. R-12: applied by db-maintainer only.

[2026-06-04] [A-01] [BUILD] **Withdrawal email-OTP confirmation — a request is now parked in `awaiting_confirmation` (no debit) until the user verifies a 6-digit code emailed to them.** Implements the user spec: "for the withdraw request, OTP should send to user email and after that user should confirm the withdraw in their side to being process." (1) Migration `20260604110000_withdrawals_otp_confirmation.sql` (deploy via `db-maintainer`): adds `withdrawals.confirmation_code_hash TEXT` (SHA-256 hex only — R-01, never plaintext), `confirmation_expires_at TIMESTAMPTZ`, `confirmation_attempts INTEGER NOT NULL DEFAULT 0`, `confirmed_at TIMESTAMPTZ`; extends `withdrawals_status_check` with `awaiting_confirmation`. (2) `_shared/email.ts`: new `withdrawalOtpTemplate(name, code, amountLabel, methodLabel, ttlMinutes)` brand email (monospace code block, 10-min expiry copy, "if you didn't request this" safety note). (3) Edge function `withdrawals/index.ts`: `POST /withdrawals/request` now validates + KYC/balance-checks, then issues an OTP via new `makeOtp()` (6-digit code → `sha256Hex` hash → 10-min expiry) and `sendOtpEmail()` (fire-and-forget; a slow provider never blocks the API), inserts the row as `awaiting_confirmation` **without** debiting, and returns `requires_confirmation:true` + `confirmation_expires_at` (balance shown is pre-debit); new `POST /withdrawals/:id/confirm` verifies the code (404 not-found / 409 not-awaiting / `code_expired` / `code_invalid` increments `confirmation_attempts` / `code_locked` 429 at 5 attempts), re-checks earnings at confirm time, then atomically moves to `pending` (guarded by `.eq('status','awaiting_confirmation')` against double-confirm), debits `earnings_balance`, writes the append-only `withdrawal` transaction row (R-05), clears the consumed hash, and stamps `confirmed_at`; new `POST /withdrawals/:id/resend-otp` re-issues a fresh code + resets attempts; GET detail/list selects extended with `confirmed_at`/`confirmation_expires_at`. Constants `OTP_TTL_MIN=10`, `OTP_MAX_ATTEMPTS=5`, `METHOD_LABELS`. (4) `_shared/database.types.ts`: `WithdrawalStatus` += `awaiting_confirmation`; `Withdrawal` gains `confirmation_code_hash`/`confirmation_expires_at`/`confirmation_attempts`/`confirmed_at`. (5) Admin: `status-badge.tsx` adds an `awaiting_confirmation` tone; `finance/withdrawals/page.tsx` adds an `awaiting_confirmation` status tab (label underscores → spaces). (6) Docs `docs/withdrawals-crypto-api-reference.md` rewritten: two-step flow overview, `/confirm` + `/resend-otp` sections, lifecycle diagram with the `awaiting_confirmation` gate, per-endpoint error tables (incl. all OTP codes), and an OTP-aware integration checklist. R-01/R-02/R-05 honoured; R-11 frozen surfaces untouched; R-12 — migration applied exclusively via `db-maintainer`. IDE reports no diagnostics. Deploy: apply migration via `db-maintainer`, then `bash scripts/deploy-api.sh --build`.

[2026-06-04] [A-01] [BUILD] **Crypto withdrawal support — multi-coin/network (USDT/USDC × TRC20/ERC20/BEP20/SOL/MATIC) with full customer API, admin panel, and migration.** (1) Migration `20260604100000_withdrawals_crypto.sql`: adds `crypto_coin TEXT`, `crypto_network TEXT`, `crypto_address TEXT` to `withdrawals`; indexes on `method` and `crypto_coin`; applied via `db-maintainer`. (2) Edge function `supabase/functions/withdrawals/index.ts`: validates supported coins (USDT/USDC) and per-coin network lists; validates wallet address length; populates new structured columns on insert; new route `GET /withdrawals/supported-crypto` returns coin+network metadata; list (`GET /withdrawals`) now accepts `?method=` filter and returns `crypto_coin/network/address`; detail returns same plus `account_details`. (3) `_shared/database.types.ts` `Withdrawal` interface gains `crypto_coin`, `crypto_network`, `crypto_address`. (4) Admin `finance/withdrawals/page.tsx`: method filter tabs (All/Bank/Crypto/PayPal); crypto rows show coin+network badges; pagination carries `method` param. (5) Admin `finance/withdrawals/[id]/page.tsx`: DB query includes new columns; Destination card is crypto-aware (coin/network badges, monospace+copy address, TX hash display); history table shows coin·network inline for crypto rows; `WithdrawalActions` receives `method` prop. (6) Admin `finance/withdrawals/[id]/withdrawal-actions.tsx`: Complete dialog label says "Transaction hash (TX ID)" for crypto; crypto-specific quick-reject reasons added. R-01/R-02/R-05 honoured; R-12 (migration via `db-maintainer` only). IDE reports no diagnostics.

[2026-06-07] [A-01] [BUILD] **Three-bucket financial model — wallet / earnings / score, with customer + admin APIs and full admin-panel coverage.** Implements the model in `docs/financial-model-api-reference.md`. (1) Migration `20260607000000_three_bucket_financial_model.sql` (untracked, deploy via `db-maintainer`): adds `profiles.earnings_balance NUMERIC(12,2)` + `profiles.score_balance BIGINT`; new append-only `score_events` ledger (RLS: owner SELECT) with user/game/created_at indexes; extends `transactions_type_check` with `earnings_transfer`; new atomic `transfer_earnings_to_wallet(user_id, amount)` RPC (debits earnings, credits wallet, writes `earnings_transfer` row, RAISE on `insufficient_earnings`/`amount_must_be_positive`, SECURITY DEFINER, granted to authenticated+service_role); rewrites `distribute_prizes` to credit `earnings_balance` (INV-15, not wallet) + `total_prizes_won`, insert `prize` tx, and award `score_balance` + `score_events` rows (winners by rank → `game_winner`/`top3`/`prize_winner`; non-prize survivors → `participant`). (2) Edge functions: `wallet/index.ts` GET `/balance` now returns all three balances (+legacy `balance`), new POST `/transfer` (calls RPC) and GET `/score-history` (paginated `score_events`); `withdrawals/index.ts` debits `earnings_balance` and enforces KYC **only** for amount > €1,000 (INV-05 updated), returns `kyc_bypassed`; `profile/index.ts` GET exposes `earnings_balance`+`score_balance`; `_shared/database.types.ts` `TransactionType` += `earnings_transfer`. (3) Admin: `actions/users.ts` `adjustWallet` gains `target: wallet|earnings`; `actions/finance.ts` rejected-withdrawal refund now credits `earnings_balance`; `users/[id]/user-actions.tsx` Adjust-balance dialog target selector; `users/[id]/page.tsx` Earnings + Score stat cards and Score-history table; `finance/withdrawals/[id]/page.tsx` "Source: Earnings" badge, player Earnings row, and "KYC not required (≤ €1,000)" note. Docs §5 updated (gaps → implemented). R-02 (NUMERIC dollars / BIGINT score), R-05 (append-only ledgers), R-09 (atomic transfer) honoured; INV-15/INV-16 enforced. R-11 frozen surfaces untouched; R-12 — migration applied exclusively by `db-maintainer` (`docker compose up -d --force-recreate db-maintainer`). Edge/admin redeploy after migration. IDE reports no diagnostics.

[2026-06-05] [A-01] [FIX] **Template game generation — a LIVE game no longer blocks the next upcoming game (overlap regression) + restored cron-aligned scheduling and cooldown-aware picker.** User report: when a template's current game is `live`, no new game is generated, contradicting the rule that a template may have several games as long as no two are simultaneously *registerable* (one upcoming/open at a time; live/completed/cancelled do NOT block the next upcoming). Root cause: `20260606000000_target_languages.sql` redefined `generate_game_from_template()` from a STALE copy while bolting on `target_languages`, silently reverting three prior improvements: (1) the overlap guard was widened to `v_current_status IN ('upcoming','open','live','paused')` — so a live/paused current game returned NULL and `audit_game_template_rules()` queue-refill could never spawn the next upcoming; (2) `scheduled_at` reverted to `NOW()+start_buffer_seconds` (lost `next_cron_match()` cron-alignment from `20260531300000`); (3) the question picker reverted to plain `ORDER BY random()` (lost the 7-day re-ask cooldown preference + `last_asked_at` stamping from `20260603120000`). Fix: new migration `20260606130000_fix_generate_overlap_live.sql` re-installs the canonical `20260603120000` body — overlap blocks **only** on `('upcoming','open')`, `scheduled_at = COALESCE(next_cron_match(cron_expression, NOW()+buffer), NOW()+buffer)`, cooldown-aware picker with `last_asked_at = NOW()` stamping — while preserving the `target_languages` set computation + INSERT column added by `20260606000000`. No financial logic changed (entry_fee/prize_pool copied verbatim as before); the partial unique index `games_one_registerable_per_template` continues to enforce §5 at the DB level. R-11 frozen surfaces untouched (no auth/Redis/.env/docker-compose). R-12: migration applied exclusively by `db-maintainer` — deploy with `docker compose up -d --force-recreate db-maintainer`.

[2026-06-05] [A-01] [FIX] **LLM question generation — driven by category + description (NOT the game title), with mandatory factual accuracy and server-side answer shuffling.** Two user-reported defects in the on-the-fly OpenAI path (`run_mode` `auto`/`presenter`) in `deploy/game-orchestrator/orchestrator.ts`: (A) the correct answer was always option A (LLM positional bias), and (B) questions drifted to the game's instance title (e.g. "Science Quiz_0604_1400") instead of being constrained to the configured subject, and could contain incorrect/debatable answers. Fixes: (1) **Answer-position randomization** — new `shuffleQuestionOptions(q)` helper applies a Fisher-Yates permutation to the four options after every `generateQuestion()` parse, rewrites `correctOptionId` to wherever the correct answer landed, and applies the **same** permutation to every `localizedPayloads[].options` so labels A/B/C/D stay consistent across languages; logs `original→shuffled` correct id + permutation. Called immediately after the `correctOptionId` validation in `generateQuestion`. (2) **Generation inputs** — the question subject is now driven by `category` (primary) + `games.description` (optional focusing guidance), and the game **title is no longer a generation input**. Added `cleanDescription(description)` helper (trim → "" when absent). The `generateQuestion` params field `topic` was renamed to `description` across all plumbing (param type, body, `userPayload`, request/result logs, `PresenterGameConfig`, `prefillQueue`/`resolveUniqueQuestion`/`startQuestionLoop`/`closeQuestion` config types, the `handleStartGame`/`recoverRunningGames` `gameCommon` construction, and the `handleStartQuestion`/early-`CloseQuestion` `gameParams`); the source changed from `topic: cleanTopic(g.title)` to `description: cleanDescription(g.description)`. `cleanTopic(g.title)` is still used only for the display `title` Redis field. (3) **DB selects** — `description` added to the `games` SELECT in both `handleStartGame` and `recoverRunningGames` so `g.description` is loaded. (4) **Factual accuracy** — the `generateQuestion` system prompt rewritten: `category` is the primary subject, `description` is optional guidance (never quoted back, never the title), and a MANDATORY rule requires exactly one verifiably-correct answer with three verifiably-wrong distractors, instructing the model to pick a different question when uncertain rather than emit a debatable/incorrect answer. `description` flows template→game via the existing `generate_game_from_template` RPC (`v_tpl.description → games.description`) — no migration needed. Docs: `docs/game-logic.md` new §14 "Question Generation" documenting the inputs table (category/description/difficulty/targetLanguages — title excluded), factual-accuracy requirement, and the server-side shuffle. No R-11 frozen surfaces touched (no auth, Redis wiring, `.env`, or `docker-compose.yml`); R-12 honoured (no manual SQL). IDE Deno LSP reports no diagnostics. Deploy: `docker compose up -d --build --force-recreate game-orchestrator`.

[2026-06-04] [A-01] [BUILD] **Unified missed-answer sweep — INV-14 extended to all player states (ghost participants, disconnected, never-joined).** Before this change, the no-answer sweep only caught players already in the Redis `participants` set (those who called `POST /game-session/:id/join`). Players who paid entry but never established a Redis session ("ghost participants") were silently missed at every question close. This change closes the gap so that any question without a valid submission always counts as a wrong answer for every registered participant. Changes: (1) `deploy/game-orchestrator/orchestrator.ts` — added `K.specs` to the K key map; after the existing Redis `notAnswered` loop in `closeQuestion`, a §10.3 ghost-participant sweep queries `game_participants` for `role=player, eliminated=false, participant_role NOT IN (spectator,eliminated)` players absent from `SMEMBERS(participants) ∪ SMEMBERS(spectators)`, increments `wrong_count`/`lives_remaining` in DB, broadcasts `PLAYER_WRONG_ANSWER` or `PLAYER_ELIMINATED` (legacy `USER_ELIMINATED` too) per ghost user, inserts `game_answers` audit rows; the sweep is wrapped in try/catch so a DB error never crashes the close flow. `QUESTION_CLOSED` counts updated: `noAnswerCount = redisNotAnswered + ghostNoAnswer`, `noAnswerEliminatedCount = redisElim + ghostElim`. (2) `supabase/functions/_shared/redis_scripts.ts` — `JOIN_GAME_SCRIPT` gains ARGV[6]=`preChargedWrong` and ARGV[7]=`preChargedLives`; when ARGV[6] is non-empty (ghost-sweep pre-charged path) the script uses those as the base and only charges 1 for the currently-active question (`inCharge = qStatus=="active" ? 1 : 0`), then determines role/elimination from the updated total; `missedQuestions` in the return is set to `inCharge` (not total) so the edge function only fires `LATE_JOIN_RECONCILE` for the new charge, not for already-broadcast ghost-sweep events. (3) `supabase/functions/game-session/index.ts` — `/join` handler queries `game_participants` for existing `wrong_count`/`lives_remaining`/`eliminated` before the Lua script; if `wrong_count > 0`, passes ARGV[6]/ARGV[7] (pre-charged path); stores `preChargedEliminated`; the `LATE_JOIN_RECONCILE` publish condition is now `missedQuestions > 0 && !preChargedEliminated` to suppress duplicate broadcasts for already-eliminated ghost players. (4) `project-brain/Domain_Knowledge.md` — INV-14 rewritten as the unified "any unanswered question = wrong answer" rule with a full table covering all player states. No DB migration (all columns exist). IDE LSP reports no diagnostics. Deploy: `docker compose up -d --build --force-recreate api game-orchestrator`.

[2026-06-04] [A-01] [DOMAIN] **Late-join rule (INV-14) — questions presented before a player joins count as wrong answers; over the limit ⇒ spectator on arrival.** Implemented the user spec: "missed questions before joining = wrong answers … after reaching the allowed wrong answer limit, the player must become spectator/viewer only and cannot participate anymore" (examples: allowed=3 can join until before Q3 starts; allowed=1 joining from Q2 is already eliminated). All charging is atomic inside `JOIN_GAME_SCRIPT`. (1) `supabase/functions/_shared/redis_scripts.ts` — rewrote `JOIN_GAME_SCRIPT`: for the `first_question_only` policy `missed = currentQuestionIndex + 1` (the in-progress question started before arrival, so it counts); `maxW = maxWrongAnswers`; if `maxW` set, `remainingLives = maxW − missed`, and when `remainingLives ≤ 0` the joiner is demoted to `spectator` with `eliminated=true`, `eliminationReason="late_join_missed"`, `remainingLives=0`. Surviving late joiners (`missed < maxW`) join as `participant` with reduced lives and the already-charged in-progress question is blocked: `SADD userAnswers currentQuestionId` (SUBMIT rejects re-attempt as duplicate) + `SADD questionAnswered userId` (the close-question no-answer `SDIFF` sweep skips them → no double charge). Block is guarded by `qStatus=="active"` and `qIdxLive==ARGV[5]` (the index the caller read) to avoid a mid-request question-advance race. `any_time` keeps no-penalty behaviour; `closed` still rejects. Reconnect path now also returns `eliminated`/`eliminationReason`/`missedQuestions` for a uniform shape. New script signature: KEYS[5]=userAnswers, KEYS[6]=questionAnswered(qIdx); ARGV[5]=currentQuestionIndex (""=none). (2) `supabase/functions/_shared/redis_keys.ts` — `joinGameKeys(gameId,userId,qIdx=0)` now appends `userAnswers` (KEYS[5]) and `questionAnswered(qIdx)` (KEYS[6]). (3) `supabase/functions/game-session/index.ts` — `/join` reads live `currentQuestionIndex` before the script (like `/answer`), builds the 6-key set, passes ARGV[5]; `joinResult` type + response extended with `eliminated`/`eliminationReason`/`missedQuestions`; the `game_participants` upsert now persists the real `wrong_count`/`lives_remaining`/`eliminated` (+`elimination_reason`/`eliminated_at` when over the limit) instead of `0`/`allowed`; when `missedQuestions > 0` it publishes a `LATE_JOIN_RECONCILE` orchestrator command (best-effort, never blocks the join). (4) `deploy/game-orchestrator/orchestrator.ts` — `reasonToSpec` maps `late_join_missed → NO_ANSWER`; new `handleLateJoinReconcile(payload)` (wired into the RabbitMQ switch) mirrors the already-authoritative DB state to the room over LiveKit WITHOUT re-PATCHing (so a late joiner who never played keeps `participant_role="spectator"`, not "eliminated"): under the limit → `broadcastPlayerWrongAnswer(reason=late_join_missed)`; over the limit → `USER_ELIMINATED` + `PLAYER_ELIMINATED` (`status:"SPECTATOR"`, `reason:"NO_ANSWER"`, `lateJoin:true`, `missedQuestions`); the survivor-snapshot fields are intentionally omitted since the joiner never occupied a survivor slot (no `eliminatedUserCount` increment). (5) Docs — `project-brain/Domain_Knowledge.md` INV-14 added; `docs/livekit-events.md` §4/§5 annotated with the late-join variants and the TS `PLAYER_ELIMINATED` union extended (`lateJoin?`, `missedQuestions?`, snapshot fields now optional). No DB migration (all `game_participants` columns — `wrong_count`/`lives_remaining`/`eliminated`/`elimination_reason`/`eliminated_at`/`participant_role` — already exist via `20260528360000_realtime_quiz_v1.sql`). R-11 frozen surfaces (`getPublicClient`, `REDIS_URL` expansion, `.env` byte hygiene, `docker-compose.yml`) untouched; R-12 honoured (no manual SQL). IDE Deno LSP reports no diagnostics. Deploy: `docker compose up -d --build --force-recreate api game-orchestrator`.

[2026-06-04] [A-01] [FIX] **Auto/presenter LLM question generation now faithful to the Game Template config (topic / category / difficulty / language).** Symptom: games generated from a `game_templates` row produced questions that ignored the template's configured subject and language. Root causes in `deploy/game-orchestrator/orchestrator.ts` (the on-the-fly OpenAI path used by `run_mode` `auto`/`presenter` — distinct from the question-bank picker in `generate_game_from_template`): (1) `topic` was set to `g.title`, but `generate_game_from_template` stamps the title as `name || '_' || to_char(start,'MMDD_HH24MI')` (e.g. `"Science Quiz_0604_1400"`), so the timestamp slug leaked into the LLM topic. (2) The `generateQuestion` system prompt never instructed the model to honour topic/category/difficulty/language, and its hard-coded schema example showed `"language":"en"`, biasing every output to English regardless of the game's configured `language`. (3) No logging of the generation inputs/outputs and no check that the requested language was actually produced, so mismatches were silent. (4) `claimQuestion` persisted every canonical row with a hard-coded `p_language:"en"`, mis-tagging non-English questions in the bank. Fixes: added `cleanTopic(title)` helper (strips trailing `_\d{4}_\d{4}` → real subject, falls back to "general knowledge") and applied it at both `gameCommon` construction sites (`handleStartGame` + `recoverRunningGames`). Rewrote the `generateQuestion` prompt to explicitly bind `topic`/`category`/`difficulty`, write `canonicalText` in `baseLanguage` (= the game's single configured `language`), and require `localizedPayloads` to cover exactly the `targetLanguages` set; `baseLanguage` now passed in the user payload. Added structured logging of the exact inputs (`topic/category/difficulty/baseLanguage/targetLanguages` — config values only, no secrets) before the call and the produced `canonicalText`/languages/`correctOptionId` after, plus a `console.warn` listing any requested target language missing from the response. `claimQuestion` now tags the stored row with `meta.targetLanguages?.[0] ?? "en"` instead of a hard-coded "en" — safe because `claim_question.content_hash` is md5 of the normalized text only and `p_language` merely sets the stored `language` column (no dedup impact). Template→games column mapping itself (`questions_count`/`category`/`difficulty`/`language`/`title`) was already correct in `generate_game_from_template`; no migration and no SQL change. No R-11 frozen surfaces touched (no auth, no Redis wiring, no `.env`, no `docker-compose.yml`). IDE Deno LSP reports no diagnostics. Deploy: `docker compose up -d --build game-orchestrator`.

[2026-06-04] [A-01] [FIX] **LiveKit `serverTime` now stamped at the true send moment — fixes client clock-sync drift / "late" countdown perception.** `docs/livekit-events.md` documents `serverTime` as "server epoch ms when the event was sent", but the orchestrator captured it at *logic time* and then awaited DB/Redis/JWT/HTTP work before the bytes actually left via LiveKit `SendData`. For `GAME_STARTED`, `nowMs` was captured before the `dbUpdate(games, status:live)` + JWT sign + SendData round-trip; for `QUESTION_STARTED`, `now` was captured before the `PREPARE_Q_SCRIPT` `evalLua`. Clients computing `clockOffset = serverTime − receiveTime` therefore measured an offset inflated by that latency, skewing every countdown derived from the absolute scheduling fields (`firstQuestionStartsAt`/`startsAt`/`endsAt`) — the reported "events emitted late / out of sync" symptom. Fix (`deploy/game-orchestrator/orchestrator.ts`): both `broadcast()` and `broadcastToIdentity()` now override `serverTime` with `Date.now()` on the line immediately before `SendData` (`JSON.stringify({ topic, ...event, serverTime: Date.now() })`), so it always reflects the real send instant. Caller-supplied `serverTime` values are intentionally superseded; **no game-logic timestamp is changed** — `startsAt`/`endsAt` (must match Redis for answer validation), `firstQuestionStartsAt`, `eliminatedAt`, and `closedAt` remain authoritative absolute fields. Single-publish semantics mean all participants receive one identical payload, so absolute-timestamp scheduling + this corrected offset is deterministic across clients; per-client network latency is inherently un-eliminable server-side and is what the offset correction exists to absorb. `docs/livekit-events.md` common-fields section expanded with the explicit clock-offset recipe (`clockOffset = serverTime − Date.now()`; `localTarget = absoluteTimestamp − clockOffset`; never `serverTime + duration`). No R-11 frozen surfaces touched (no auth, no Redis wiring, no `.env`, no `docker-compose.yml`); no migration. Deploy: `docker compose up -d --build game-orchestrator`.

[2026-06-04] [A-01] [BUILD] **Enrich `PLAYER_ELIMINATED` and `QUESTION_CLOSED` LiveKit events with game-state snapshot.** Added `eliminatedCount`, `activeSurvivorCount`, `prizePool`, and `projectedPrizePerSurvivor` to both events so clients can update survivor-count and prize-projection HUD elements without a REST round-trip. `PLAYER_ELIMINATED` reads the four values from Redis inside `applyElimination` after `sRem` (so `activeSurvivorCount` excludes the just-eliminated user). `QUESTION_CLOSED` reads them just before the broadcast (post no-answer-elimination loop). Both reads are best-effort (failure caught and swallowed; stats default to zero/null — never blocks the event). `docs/livekit-events.md` updated: JSON examples, field-description notes, and TypeScript discriminated union for both events. No DB migration, no R-11 frozen surfaces touched. Deploy: `docker compose up -d --build game-orchestrator`.

[2026-06-04] [A-01] [RULE] **Live API smoke-test credentials codified — `agents.md §8` + `Coding_Standards.md §7`.** Added `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` (read from `.env`, never hardcoded) as the standard test account for verifying developed endpoints against `https://api.quiz4win.com`. Verified: GoTrue `/auth/v1/settings` probe returns HTTP 200 (anon key valid); GoTrue direct sign-in with these credentials returns HTTP 200 + JWT (len=815) — credentials are confirmed correct. App-layer `POST /auth/signin` currently returns 401 only because the production `api` container has a stale anon key (stale-container issue, not a credential issue) — fix: `bash scripts/deploy-api.sh` on the production server. Rule text added to `agents.md §8` (test account table, usage rules, 401-triage flowchart) and `Coding_Standards.md §7` (live smoke-test sub-section). Reference script: `scripts/test-login.py` (reads .env, prints metadata only, no secret values).

[2026-06-04] [A-01] [FIX] **Permanent guard against "Invalid API key" from stale container env — `scripts/deploy-api.sh`.** Root cause of recurring `[auth] signin failed … Invalid API key`: the `api` container is restarted with `docker restart` or `docker compose restart` instead of `docker compose up -d --force-recreate api`, so it never reloads `.env` and keeps the old (possibly byte-corrupted) `SUPABASE_ANON_KEY`. This does NOT touch any R-11-frozen file (no changes to `auth/index.ts`, `_shared/supabase.ts`, `_shared/auth.ts`, `docker-compose.yml`, or `.env`). New file: `scripts/deploy-api.sh` — a bash script that (1) detects and strips CRLF from `.env` in-place before any deploy; (2) validates `SUPABASE_ANON_KEY` is present and at least 100 chars in `.env`; (3) compares the running container's key length against `.env` and logs a warning on mismatch; (4) always runs `docker compose up -d --force-recreate [--build] api`; (5) re-checks key length post-deploy and exits non-zero if still mismatched with actionable remediation hint (unset host-exported env var). Usage: `bash scripts/deploy-api.sh` (image unchanged) or `bash scripts/deploy-api.sh --build` (after source edits). All future api deploys should use this script — never `docker restart api`.

[2026-06-05] [A-01] [BUILD] **Frontend LiveKit event reference — `docs/livekit-events.md`.** Single consolidated document for the mobile/web clients listing every LiveKit DataChannel event the orchestrator emits, with exact JSON payload, lifecycle ordering (`GAME_STARTED → QUESTION_PREPARED (presenter only) → QUESTION_STARTED → PLAYER_WRONG_ANSWER / PLAYER_ELIMINATED (zero or more) → QUESTION_CLOSED → loop → GAME_ENDED → GAME_RESULT`), reconnect/catch-up REST endpoints (`GET /game-session/:id/state`, `GET /games/:id/result`, `GET /games/history`), and a paste-ready TypeScript discriminated union covering all event types. Each event entry calls out delivery mode (broadcast vs per-identity for `QUESTION_PREPARED`), null-able fields, and back-compat notes (`USER_ELIMINATED` still emitted alongside `PLAYER_ELIMINATED` for older client builds). Sourced exhaustively from `deploy/game-orchestrator/orchestrator.ts` lines 432, 490, 501, 818, 937, 967, 994, 1113, 1338, 1395, 1680. No code touched — pure documentation.

[2026-06-05] [A-01] [DOMAIN] **Allowed-Wrong-Answers spec alignment — `PLAYER_WRONG_ANSWER` + `PLAYER_ELIMINATED` LiveKit events.** Aligned the orchestrator with the user-supplied "Quiz Game – Allowed Wrong Answers Logic" spec. The four failure modes (wrong submission, no answer in window, late arrival after `endsAt+gracePeriodMs`, disconnect during answer window) are all already counted as a single life deduction by the existing `SUBMIT_ANSWER_SCRIPT` + `closeQuestion` no-answer sweep — that domain rule is unchanged. New behaviour: (1) `deploy/game-orchestrator/orchestrator.ts` — added `reasonToSpec(reason)` mapper (`wrong_answer→WRONG_ANSWER`, `no_answer→NO_ANSWER`, `late|timeout→TIMEOUT`) and `broadcastPlayerWrongAnswer(roomName, gameId, userId, wrongAnswersCount, remainingChances, reason)` helper that fires `PLAYER_WRONG_ANSWER` on the room data channel (topic `PLAYER_WRONG_ANSWER`). (2) `applyElimination` now accepts optional `allowedWrongAnswers` and broadcasts a second event `PLAYER_ELIMINATED` (carrying `wrongAnswersCount`, snake_case `allowed_wrong_answers`, `remainingChances`, `status:"SPECTATOR"`, uppercase `reason`, `questionId`, `questionIndex`, `eliminatedAt`) **in addition to** the legacy `USER_ELIMINATED` event introduced 2026-06-05 — kept for back-compat with the iOS build already in TestFlight; new clients should consume `PLAYER_ELIMINATED`. (3) `closeQuestion` no-answer survivor branch now fires `PLAYER_WRONG_ANSWER` with reason `no_answer`, and threads `allowedWrongAnswers: maxWrong` into `applyElimination` for the no-answer eliminated branch. (4) `handlePersistAnswer` now (i) one-shot `HGET K.game(gameId) maxWrongAnswers` to resolve `allowedWrongAnswers` for the wrong-answer path, (ii) wrong-answer-not-eliminated branch fires `PLAYER_WRONG_ANSWER` with reason `wrong_answer`, (iii) wrong-answer-eliminated branch passes `allowedWrongAnswers` to `applyElimination`. Server-side rejection of eliminated submissions is already enforced by `SUBMIT_ANSWER_SCRIPT` lines 93–99 (`if userStatus ~= "participant" then return {status="rejected", reason=userStatus.."_cannot_answer"}`); the Edge Function never sees those rows. DB persistence layer unchanged: `game_participants.wrong_count` ↔ `wrongAnswersCount`, `lives_remaining` ↔ `remainingChances`, `eliminated` ↔ `isEliminated`, `eliminated_at` ↔ `eliminatedAt`, `participant_role` ↔ `status` (lowercase in DB, uppercase enum on the wire). `games.allowed_wrong_answers` is the source of truth for the limit. Updated `docs/player-integration.md` §8 with the new event payloads and the back-compat note. R-11 frozen surfaces (`getPublicClient`, `REDIS_URL` expansion, `.env` byte hygiene) untouched. IDE Deno LSP reports no diagnostics. Deploy: `docker compose up -d --build --force-recreate game-orchestrator` (no migration, no api rebuild needed).

[2026-06-05] [A-01] [BUILD] **Customer participation history API — `GET /games/history`.** Added a single new branch to `supabase/functions/games/index.ts` returning the authenticated user's joined games with per-game participation state. Query: `supabase.from("game_participants").select("score, rank, correct_answers, wrong_answers, entry_fee_paid, prize_amount:prize_earned, eliminated, eliminated_at, elimination_reason, participant_role, joined_at, completed_at, games!inner(<GAME_FIELDS>)", { count: "exact" }).eq("user_id", user.id).order("joined_at",{ascending:oldestFirst}).range(offset,offset+limit-1)`. Route placement: matched as `gameId === "history" && !action` **before** the `/games/:id` detail branch so PostgREST never attempts `games.id='history'` and 404s. Filters: `status` (pipe-delimited, applied as `games.status` on the embedded resource), `result=won|lost` (mapped to `gt('prize_earned',0)` / `eq('prize_earned',0)`), `from`/`to` (ISO ts on `joined_at`), `oldest_first=true` to flip order. Pagination: `page`, `limit` (cap 50, default 20). Response shape: each row is a `GameSummary` (reuses the same `GAME_FIELDS` projection as `GET /games` / `GET /games/:id`) merged with `joined_by_me:true` and a nested `participation` object carrying the caller's per-game state — same field set the mobile client already consumes from `GET /games/:id/result`, so no new client model. Rule compliance: **R-03** JWT already validated by `validateJWT(req)` at the top of the handler; **R-04** uses `getAnonClient(req)` (the existing `game_participants_select_all FOR SELECT TO authenticated USING (true)` policy from `20260524200000_customer_rls_policies.sql` would otherwise let any signed-in user read any row, so the `.eq("user_id", user.id)` is the authoritative scoping filter — explicit not implicit, documented inline); **R-01** no PII outside the caller's own rows; **R-06** zero cross-module imports — pure addition inside the existing `games` Edge Function; **R-11** untouched (no `getPublicClient`, no Redis, no `.env`, no `docker-compose.yml`, no auth surface). No migration required — the underlying RLS, indexes, and columns already exist. Updated `docs/api-reference.md` with the full endpoint section (params table, response shape, notes on `rank=NULL` for eliminated players post-`20260605200000_compute_game_ranks_exclude_eliminated.sql`, dollars-not-cents convention). Deploy: `docker compose up -d --build --force-recreate api`. IDE Deno LSP reports no diagnostics.


[2026-06-05] [A-01] [DOMAIN] **No-answer = wrong-answer + prize pool restricted to survivors.** Implemented the user spec: "not answering a question must be treated exactly the same as answering incorrectly" and "the prize pool is always distributed only among the players who remain in the game until the end and successfully survive all elimination rules." (1) `deploy/game-orchestrator/orchestrator.ts` — new `applyElimination(params)` helper that (a) removes the user from the Redis `K.parts(gameId)` participants set via `sRem` so subsequent question-close `SDIFF` rounds skip them (was a real bug: eliminated users kept appearing in `notAnswered` every round, getting their `wrongCount` bumped again and emitting duplicate audit rows), (b) PATCHes `game_participants` with `participant_role='eliminated'`, `eliminated=true`, `eliminated_at`, `elimination_reason`, `wrong_count`, `lives_remaining` so the DB row is the authoritative survivor record at finalize time, (c) broadcasts a new `USER_ELIMINATED` LiveKit DataChannel event (topic `USER_ELIMINATED`) carrying `gameId, userId, reason, wrongCount, remainingLives, questionId, questionIndex, eliminatedAt, serverTime` so clients flip the eliminated player to spectator UI immediately. `closeQuestion` now skips already-eliminated users in the `notAnswered` loop (defence-in-depth + retro-cleanup for users that pre-date this patch), calls `applyElimination` on newly-eliminated no-responders, and mirrors `wrong_count`/`lives_remaining` to the DB on still-alive no-responders; `QUESTION_CLOSED` event now also carries `noAnswerEliminatedCount`. `handlePersistAnswer` now (i) PATCHes `wrong_count`/`lives_remaining` on every wrong answer (previously only correct answers updated the row) and (ii) calls `applyElimination` when `payload.eliminated` is true (previously the elimination only existed in Redis and was never mirrored to Postgres, so `compute_game_ranks` would still rank the player). New `gameRoomNames: Map<gameId, livekit_room_name>` populated in `handleStartGame` + `recoverRunningGames`, with a one-shot `dbSelect` fallback in `resolveRoomName` so `handlePersistAnswer` can broadcast without an extra round-trip per answer. (2) Migration `20260605200000_compute_game_ranks_exclude_eliminated.sql` — `CREATE OR REPLACE compute_game_ranks(p_game_id)` now `RANK()`s only over rows where `role='player' AND participant_role='participant' AND eliminated=FALSE`; eliminated rows get `rank=NULL` + `status='disqualified'`. `distribute_prizes` pays anyone whose rank falls inside a tier's `BETWEEN v_rank_from AND v_rank_to`; `NULL BETWEEN …` is `NULL` (not TRUE), so eliminated players are now naturally excluded from every prize tier without touching `distribute_prizes` itself. (3) Domain invariant codified: a missed-answer life deduction is identical to a wrong-answer life deduction (`max_wrong_answers`-aware), `max_wrong_answers=1` ⇒ first miss eliminates, `max_wrong_answers=3` ⇒ first three misses deduct, fourth eliminates. Behavioural changes touch only the orchestrator code path + the compute_game_ranks SQL; no Edge Function / docker-compose / env changes; R-11 frozen surfaces (`getPublicClient` usage, `REDIS_URL` expansion, `.env` byte hygiene) untouched. Deploy: `docker compose up -d --force-recreate db-maintainer` then `docker compose up -d --build --force-recreate game-orchestrator`.

[2026-06-04] [A-02] [RULE] **R-12 added — database migrations are applied exclusively by the `db-maintainer` container.** Formalises the existing architecture: `./supabase/migrations/*.sql` files are mounted read-only into the container; `migrate.sh` applies any unapplied file (sorted by filename timestamp) inside a single transaction on every startup; the `api`, `game-orchestrator`, and `template-generator` containers block on the `db-maintainer` healthcheck (`/tmp/db-maintainer-ready`) before starting. Forbidden patterns: `supabase db push`, `psql -f`, ad-hoc SQL in the Supabase Studio editor, or any instruction directing an agent/developer to apply a migration manually. Correct deploy commands documented in R-12.4–5. Any changelog or script entry prescribing a manual migration path is a rule violation; correct it and log `[RULE]` immediately. Added to `project-brain/Rules.md` §R-12; AGENTS.md §3 summary table updated.

[2026-06-04] [A-01] [BUILD] **Full multilingual question generation — `target_languages` column, orchestrator, admin UI.** Added end-to-end multilingual support so every generated question is produced in all platform languages simultaneously, not just the primary display language. (1) Migration `supabase/migrations/20260606000000_target_languages.sql` — adds `target_languages TEXT[]` to `game_templates` (NOT NULL, DEFAULT `ARRAY['en','ar','fa','tr']`, CHECK against supported set) and `games` (nullable); back-fills existing rows from their `language` column; updates `generate_game_from_template` RPC to copy `target_languages` from template to game. (2) Orchestrator `deploy/game-orchestrator/orchestrator.ts` — added `SUPPORTED_LANGUAGES` constant and `resolveTargetLanguages(primary, target)` helper (primary language always first, deduped, validated against supported set, fallback `["en"]`); `gameCommon` now reads `g.target_languages` via `resolveTargetLanguages`; `generateQuestion` receives the full `targetLanguages[]`; LLM system prompt enforces exactly one `localizedPayloads` entry per requested language; post-generation validation warns on missing languages; `QUESTION_STARTED` broadcast now carries `primaryLanguage` and `languages` alongside `localizedPayloads`. (3) Admin Edge Function `supabase/functions/admin-game-templates/index.ts` — added `target_languages` to `WRITABLE_FIELDS`. (4) Server-side Zod schemas `admin/src/lib/actions/templates.ts` and `admin/src/lib/actions/games.ts` — extended to accept `target_languages: z.array(z.enum(LANG))`. (5) Admin UI — created `admin/src/components/language-multi-select.tsx` (checkbox list; primary language locked+always selected); integrated into `create-template-dialog.tsx`, `edit-template-form.tsx`, `create-game-dialog.tsx`, `edit-game-form.tsx` with state, reset, and payload wiring. (6) Types `supabase/functions/_shared/database.types.ts` — added `target_languages?: string[] | null` to `Game` interface. (7) Docs `docs/livekit-events.md` — updated `QUESTION_STARTED` section with full multilingual sample payload (EN/AR/FA/TR), field table, client rendering guidance, and TypeScript union updated to include `primaryLanguage`/`languages`. No R-11 frozen surfaces touched. Deploy: `docker compose up -d --build --force-recreate db-maintainer game-orchestrator admin` — `db-maintainer` picks up `20260606000000_target_languages.sql` automatically on startup (no manual `supabase db push` needed); `api` and `template-generator` wait on `db-maintainer` healthy before starting.

[2026-06-05] [A-02] [RULE] **R-11 added — locked-down auth & Redis wiring (DO NOT MODIFY without `[BLOCKED]` + human approval).** After repeated debugging cycles on the same three failure modes (`Invalid API key` from `getAnonClient(req)` on `/auth/v1/*`; orchestrator `TypeError: Invalid URL: 'redis://:${REDIS_PASSWORD@redis:6379}'` from nested `${...}` in `docker-compose.yml`; `Invalid API key` from CRLF/trailing-space in `.env`), the user explicitly directed that the now-working patterns be frozen so no future agent (including future A-01 sessions) re-introduces the bugs. Added `R-11` to `project-brain/Rules.md` with three subsections: R-11.1 freezes `getPublicClient()` usage in `_shared/supabase.ts`, `_shared/auth.ts`, and `auth/index.ts` and forbids the `getAnonClient(req).auth.*` anti-pattern (refs `408cd35`, `d7754d9`, `983ea01`, `535baf6`). R-11.2 freezes the flat `REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379` expansion in both the `api` and `game-orchestrator` services and forbids the nested-default form (ref `6ea4d9a`). R-11.3 codifies `.env` byte hygiene (no CRLF, no trailing whitespace) and the diagnostic playbook to distinguish env-byte mismatch from code regressions. Updated AGENTS.md §3 to include R-11 in the summary table. Enforcement: any diff that re-introduces a forbidden pattern is an automatic revert target for A-02; agents must post a `[BLOCKED]` task in `Open_Tasks_AI.md` with the failing log line before editing any frozen file. Rationale: the symptom strings for these three bugs overlap (`Invalid API key` for two of them; Redis nested expansion silently writes a malformed URL into the env), so they have been mistakenly conflated and "fixed" by reverting the working code at least once already (commit `535baf6` reverted `3e4da41`+`59c562e` for exactly this reason).

[2026-06-05] [A-01] [AUDIT] **`Invalid API key` — 4th documented cause: env-load byte mismatch (NOT a code regression).** During post-deploy verification the iOS client reported `[auth] signin failed … Invalid API key` on `POST /auth/signin`. Initial suspicion was that recent edits to `public-games`/`games` endpoints had altered anon/RLS handling — `git diff HEAD~2..HEAD -- supabase/functions/auth supabase/functions/_shared docker-compose.yml` returned **zero lines**, ruling that out. Diagnostic ran `docker compose exec api sh -c 'echo ${#SUPABASE_ANON_KEY}'` vs `awk -F= '{print length($2)}' .env`: container reported **209 chars**, `.env` reported **208 chars** — one extra byte loaded into the running container (typically a trailing `\r` from CRLF line endings or a trailing space introduced when the file was edited on a Windows host / pasted from a rich editor). GoTrue rejects the resulting JWT because the signature does not end where the encoded length says it should, surfacing as the generic `Invalid API key`. This is distinct from the three prior code-side causes already fixed in the repo: (1) `408cd35` — `getAnonClient(req)` forwarding the user's `Authorization: Bearer <jwt>` into `supabase-js` `global.headers`, overriding the SDK's default `Bearer <ANON_KEY>` on `/auth/v1/*` calls; fix: created `getPublicClient()` (no header forwarding) and routed unauth endpoints through it. (2) `d7754d9` — same root cause inside `validateJWT` in `_shared/auth.ts`; fix: switched to `getPublicClient()` and pass the token explicitly to `supabase.auth.getUser(token)`. Follow-up `983ea01` added the missing import. (3) `535baf6` — reset-password edits `3e4da41`+`59c562e` regressed signin; reverted to `408cd35`; standing rule: do not retry password-reset edits without explicit approval and a fresh edge-function log diagnostic first. Verified by grep that the current source already has every code-side fix in place: `_shared/supabase.ts` lines 27–63 document the `getPublicClient` vs `getAnonClient` distinction; `_shared/auth.ts` lines 53–63 use `getPublicClient()` inside `validateJWT`; `auth/index.ts` signin uses `getPublicClient()` (line 111). **Playbook for next occurrence of `Invalid API key`:** (a) `git diff HEAD~N -- supabase/functions/auth supabase/functions/_shared docker-compose.yml` to rule out code regressions on the auth surface; (b) `docker compose exec api sh -c 'echo ${#SUPABASE_ANON_KEY}'` and compare with `awk -F= '/^SUPABASE_ANON_KEY=/{print length($2)}' .env` — any mismatch is the bug; (c) inspect `.env` for control chars: `grep -n '^SUPABASE_ANON_KEY=' .env | cat -A` (look for `^M$` = `\r` or trailing space before `$`); (d) fix: `sed -i '' $'s/\r$//' .env` (macOS) or `sed -i 's/\r$//' .env` (Linux), then `docker compose up -d --force-recreate api`; (e) confirm: `docker compose exec api sh -c 'echo ${#SUPABASE_ANON_KEY}'` should now match `.env`. Rule: env values that are JWTs must be byte-exact — single trailing `\r`/space breaks signature verification with the same error string as the three code-side causes above, which is why this took diagnostic effort.

[2026-06-05] [A-01] [BUILD] **Game result broadcast + persisted aggregate distribution — GAME_RESULT LiveKit event and idempotent REST surface.** Implemented the spec "after prize distribution is finalized, broadcast a LiveKit event with total winners / total prize / per-winner share, store the result in the DB, and serve subsequent API requests from the stored result instead of recalculating". (1) Migration `20260605120000_games_result_summary.sql`: adds nullable `public.games.result_summary JSONB`; `CREATE OR REPLACE FUNCTION distribute_prizes(uuid)` now builds the aggregate JSONB (`game_id`, `total_winners`, `total_prize`, `prize_pool`, `currency`, `share_per_winner`, `winner_user_ids[]`, `winners[]{user_id,rank,prize_amount}`, `distributed_at`), persists it on `games.result_summary`, AND returns it (merged with `distributed`/`already_distributed` flags) on both the first-run and idempotent-replay paths; back-fills `result_summary` from `game_participants` on first read for pre-migration completed games. (2) `deploy/game-orchestrator/orchestrator.ts` `finalizeGame`: parses the RPC response and broadcasts a new `GAME_RESULT` LiveKit event (room data channel, same mechanism as `GAME_STARTED`/`GAME_ENDED`) with fields `totalWinners`, `totalPrize`, `prizePool`, `currency`, `sharePerWinner`, `winnerUserIds`, `winners`, `distributedAt`, `alreadyDistributed`. `GAME_ENDED` semantics are unchanged (still sent first). (3) `supabase/functions/public-games/index.ts`: allow `/public-games/:id/result` sub-path; returns the persisted `result_summary` with `prizes_distributed_at`/`ended_at`/`status`. Returns 409 `result_pending` when status='completed' but summary still NULL, 409 `game_not_completed` while live/upcoming. (4) `supabase/functions/games/index.ts` `GET /games/:id/result`: now returns `summary` alongside the existing per-user `result` (with `game_status`, `prizes_distributed_at`, `ended_at`); legacy `result` shape unchanged. (5) `docs/game-result-api.md`: full integration guide for frontend (event payload, REST shapes, lifecycle ordering, reconnect/poll strategy, backwards-compat notes). Deploy (all self-hosted, no Supabase CLI involved — events use LiveKit DataChannel, not Supabase Realtime): `docker compose up -d --force-recreate db-maintainer` then `docker compose up -d --build --force-recreate game-orchestrator api`.

[2026-06-05] [A-01] [BUILD] **Pregame warmup — 2-minute synchronized countdown before the first question (auto mode).** Implemented the spec "2-minute gap between game start and first question with a 'Get ready' message". (1) Migration `20260605000000_games_first_question_starts_at.sql`: adds nullable `public.games.first_question_starts_at TIMESTAMPTZ` (idempotent `ADD COLUMN IF NOT EXISTS`). (2) `deploy/game-orchestrator/orchestrator.ts`: new env-tunable constant `PREGAME_WARMUP_MS` (default `120000`); in `handleStartGame` for auto-mode games the orchestrator now computes `firstQuestionStartsAtMs = now + PREGAME_WARMUP_MS`, persists it on the game row, extends the `GAME_STARTED` LiveKit broadcast with `runMode`, `pregameDurationMs`, and `firstQuestionStartsAt` (epoch ms; `null` for presenter mode), and **kicks off `prefillQueue` before** sleeping the warmup so OpenAI generation latency is absorbed behind the visible countdown. (3) `recoverRunningGames` now selects `started_at,first_question_starts_at`; the `live-with-no-question-yet` branch computes the remaining warmup from `first_question_starts_at`, re-broadcasts `GAME_STARTED` with `recovered: true` so reconnecting clients re-sync the countdown, pre-fills the queue, and only starts `startQuestionLoop` after the remainder elapses (immediate if already past). Presenter mode is unchanged (no warmup). (4) `docs/pregame-warmup-api.md`: full integration guide for the frontend (event payload, DB column, recovery semantics, UX recommendation, env var, backwards compatibility). Deploy: `docker compose up -d --force-recreate db-maintainer` then `docker compose up -d --build --force-recreate game-orchestrator`.

[2026-06-03] [A-01] [FIX] **Live question generation never repeats — avoid-list + diversity seed + temperature escalation.** The cooldown RPC (`claim_question`) only prevented re-using a question's canonical row; it did NOT stop OpenAI from re-emitting the same popular trivia every game. On a content-hash collision the orchestrator re-generated with identical params, got the same question back, and after `QUESTION_DEDUP_MAX_RETRIES` accepted the duplicate. Fix in `deploy/game-orchestrator/orchestrator.ts`: (1) `generateQuestion` now accepts `avoidTexts` (already-asked canonical texts, capped to the last 40) and an optional `temperature` override; the user payload carries an `avoid` list + a fresh `diversitySeed` (random UUID) every call, and the system prompt instructs the model to produce a question clearly DIFFERENT from every avoid entry (default temperature raised 0.7→0.8). (2) New per-game `askedQuestionTexts` Set + `normalizeText()` (mirrors `claim_question`'s `md5(lower(regexp_replace(btrim(text),'\s+',' ','g')))` basis). (3) `resolveUniqueQuestion` seeds the avoid-list from already-asked texts, adds each colliding text back into it, passes `avoidTexts` on every re-roll, and escalates temperature `min(1.2, 0.7 + attempt*0.15)` per retry; records the accepted text in `askedQuestionTexts`. (4) `prefillQueue` supplies `avoidTexts` (asked ∪ already-queued) so the 3 pre-generated questions are mutually distinct. (5) `finalizeGame` clears the per-game `askedQuestionTexts` entry. IDE Deno LSP reports no diagnostics (local `deno` binary unavailable for `deno check`). Deploy: `docker compose up -d --build --force-recreate game-orchestrator`.
[2026-06-03] [A-01] [DOMAIN] **No-duplicate-question invariant (cooldown-based global uniqueness).** New rule: a question is never repeated within a single game, and is never re-asked across the whole platform until a configurable cooldown elapses (default 7 days). There is no curated question bank — every live question is generated on the fly — so de-duplication is enforced cheaply at the DB layer. (1) Migration `20260603120000_questions_dedup_cooldown.sql`: adds `questions.content_hash` (STORED generated `md5(lower(regexp_replace(btrim(text),'\s+',' ','g')))`) + `questions.last_asked_at`; soft-deletes pre-existing duplicate-content rows (keeps earliest per hash); creates partial unique index `uq_questions_content_hash` WHERE `deleted_at IS NULL` + `idx_questions_last_asked_at`; adds `claim_question(...)` SECURITY DEFINER RPC that atomically looks up by content_hash and returns `was_recent=TRUE` when `last_asked_at` is inside the cooldown (caller re-rolls) else inserts/claims, stamps `last_asked_at=NOW()`, bumps `used_count`, and returns the canonical `question_id` (normalizes difficulty/language/category to satisfy CHECK constraints; concurrent-insert race caught via the partial unique index). Also `CREATE OR REPLACE generate_game_from_template` so the template picker prefers questions outside the cooldown window (falls back to recent ones so a game is never short of its `questions_count`) and stamps `last_asked_at`. (2) `deploy/game-orchestrator/orchestrator.ts`: added `dbRpc` helper, `askedQuestionIds` per-game set (cleared in `finalizeGame`), `claimQuestion()` + `resolveUniqueQuestion()` (re-generates a fresh question on collision up to `QUESTION_DEDUP_MAX_RETRIES`, then accepts the last claim to avoid stalling; falls back to a local UUID if the DB is unreachable). Both question sites (auto-mode `advanceQuestion` and presenter-mode `handlePrepareQuestion`) now route through `resolveUniqueQuestion` and broadcast the RPC-returned canonical id. (3) `docker-compose.yml` game-orchestrator: added `QUESTION_REASK_COOLDOWN_SECONDS` (default 604800) and `QUESTION_DEDUP_MAX_RETRIES` (default 5). Deploy: `docker compose up -d --force-recreate db-maintainer` (apply migration) then `docker compose up -d --build --force-recreate game-orchestrator`.
[2026-06-03] [A-01] [FIX] **test-participant: --api-url flag fixes 404 on join.** Added `--api-url <URL>` CLI argument to `deploy/test-participant/participant.mjs`. The bot was falling back to `SUPABASE_URL` for API calls which pointed to the Supabase cloud platform where `game-session` is not deployed as a native Edge Function, causing Supabase's own "Requested function was not found" error. Fix: `--api-url` takes highest priority, then `API_URL` env var, then the supabase.co detection heuristic. Startup log now prints the resolved `api=` URL for verification. End-to-end test confirmed working: both bots signed in, joined, connected to LiveKit, received `QUESTION_STARTED` events via DataChannel, submitted random answers, and received `QUESTION_CLOSED` events with correct options — all 20 questions of a full game without a single crash. (commit 7585e1b)
[2026-06-03] [A-01] [BUILD] **Test participant bot for end-to-end game testing.** Added `deploy/test-participant/participant.mjs` (Node ESM) + `package.json`. The bot: (1) signs in a throwaway test user via Supabase Auth password grant (auto-creates it with the service-role admin API if sign-in fails), (2) `POST /game-session/:id/join` to obtain the LiveKit token + room name and confirm `participant` role, (3) connects to the LiveKit room via `@livekit/rtc-node` so the room actually exists (this also clears the benign `SendData` 404s when a real player is present), (4) listens for `QUESTION_STARTED` data-channel events and submits a RANDOM option to `POST /game-session/:id/answer`, and (5) runs a `GET /state` poll as a fallback so it still answers if a data message is missed. Idempotent per `questionId` (LiveKit event + poll never double-answer). CLI: `--game <id>` (required), `--bots N`, `--no-livekit` (poll-only, no native dep), `--password`. Run: `cd deploy/test-participant && set -a && source ../../.env && set +a && npm install && node participant.mjs --game <GAME_ID>`. No production code touched — test tooling only.
[2026-06-03] [A-01] [FIX] **Orchestrator crash-loop root cause — empty SDIFF encoded as object.** The orchestrator restarted on every question close (crash-recover-recover cycle 0→19). After the previous commit added `.catch()` wrappers + a global `unhandledrejection` handler (which kept the process alive and exposed the stack trace), the real error surfaced: `closeQuestion` line 585 `TypeError: object is not iterable`. Cause: `CLOSE_Q_SCRIPT` computes `notAnswered = SDIFF(...)`; when the set-difference is empty (zero participants, or everyone answered), Lua returns an empty table `{}` and `cjson.encode({})` emits the JSON object `{}` (not `[]`). The `?? []` guard does not catch a non-null object, so `for...of {}` threw on every close. Fix in `deploy/game-orchestrator/orchestrator.ts`: guard with `Array.isArray(closeResult.notAnswered) ? … : []` before iterating; reuse the guarded var for `noAnswerCount` and the close log. Also verified `[livekit] broadcast HTTP 404` is benign: LiveKit `RoomService.SendData` returns twirp `not_found`→404 when the room has no participants (auto-games with no players); keys are correct (auth failure would be 401). Deploy: `git pull && docker compose up -d --build --force-recreate game-orchestrator`.
[2026-05-31] [A-01] [FIX] **join_game: status is the sole registration gate.** Migration `20260531500000_join_open_anytime.sql` replaces `join_game` to remove the `scheduled_at <= NOW()` guard introduced in `20260531200000_game_lifecycle.sql`. Cause of `registration_closed` 400 on games with status `open`: when the scheduler flips `upcoming` → `open` at/after `scheduled_at`, the time-gate immediately rejected joins even though status said registration was open. Per documented status semantics, status alone (`upcoming`/`open`) authorizes registration; the orchestrator owns the `open` → `live` transition, and stuck/orphan games are handled by `audit_game_template_rules`. Edge Function `supabase/functions/games/index.ts` mirrors: dropped the fast-path `scheduled_at <= now` check (and the column from the SELECT). Deploy: `docker compose up -d --force-recreate db-maintainer` then `docker compose up -d --build --force-recreate api`.
[2026-05-31] [A-01] [BUILD] **rules watchdog — self-healing audit for game_template_logic.md invariants.** (1) Migration `20260531400000_rules_watchdog.sql`: adds `audit_game_template_rules(p_orphan_grace_minutes)` SECURITY DEFINER RPC. Each tick it (a) cancels duplicate upcoming/open games per template keeping the earliest (`duplicate_upcoming_audit`), (b) cancels orphan never-started registerable games whose `scheduled_at < NOW - grace` and `started_at IS NULL` (`orphan_never_started`), (c) for every active template missing an upcoming/open game calls `generate_game_from_template` to refill the queue. Returns a JSONB array of actions. (2) `generator.ts`: replaces `queueBuilderTick` with `rulesAuditTick` (subsumes queue-builder + adds rules a & b). Env var `RULES_AUDIT_ORPHAN_GRACE_MINUTES` (default 15). Deploy: `docker compose up -d --force-recreate db-maintainer` then `docker compose up -d --build --force-recreate template-generator`.
[2026-05-31] [A-01] [BUILD] **cron-aligned scheduling — align with game_template_logic.md spec (commit 2148ee3).** (1) Migration `20260531300000_cron_aligned_scheduling.sql`: adds `next_cron_match(expr, from)` helper; rewrites `generate_game_from_template` so `scheduled_at` is set to the next cron-expression match (falls back to `NOW+start_buffer_seconds` if cron invalid); cancels duplicate upcoming/open games per template (pre-flight); creates partial unique index `games_one_registerable_per_template` to enforce DB-level §5 invariant. (2) `admin-game-templates` activate endpoint: calls `generate_game_from_template` immediately after `is_active=true` so the first upcoming game exists right away; returns `first_game_id`. (3) `queueBuilderTick` in generator.ts: checks only `live` status (not `paused`) per spec. Deploy: `docker compose up -d --force-recreate db-maintainer` then `docker compose up -d --build --force-recreate template-generator`.
[2026-05-31] [A-01] [BUILD] **queue-builder: always keep one upcoming game per template.** Two changes (commit 8db0017): (1) Migration `20260531100001_overlap_fix_queue_builder.sql` — fix overlap check in `generate_game_from_template` from blocking on `('upcoming','open','live','paused')` to only `('upcoming','open')`. A live/paused session no longer prevents queuing the next game. (2) New `queueBuilderTick` in `deploy/template-generator/generator.ts` — runs every 60s; for each active template whose `current_game_id` is `live` or `paused` and has no upcoming/open game queued, calls `generate_game_from_template` to fill the queue. SQL `FOR UPDATE` row-lock + overlap check guarantee idempotency vs the cron tick. Deploy: run `db-maintainer` migration then `docker compose up -d --build --force-recreate template-generator`.
[2026-05-31] [A-01] [FIX] **template-generator: removed event-driven next-game creation.** `schedulerTick` was calling `generateNextGameForTemplate` immediately after publishing `StartGame` to RabbitMQ, bypassing the template's `cron_expression`. With cron `0,30 * * * *` and `duration_minutes=5`, this produced a new game every ~5 minutes (each start triggered the next) instead of every 30 minutes. Removed the call and the now-unused `generateNextGameForTemplate` function from `deploy/template-generator/generator.ts`. The minutely cron tick (`generate_games_from_active_templates`) already creates games at every matching cron minute and handles gap recovery. Deploy: `docker compose up -d --build --force-recreate template-generator`.
[2026-05-31] [A-01] [AUDIT] Session started. Resolving Game Orchestrator AMQP connection issues.
[2026-05-31] [A-01] [FIX] **Game Orchestrator AMQP Consumer restoration.** Fixed the orchestrator crash `The requested module 'jsr:@std/io' does not provide an export named 'BufReader'` and subsequent TLS handshake hangs by:
- Creating `deploy/game-orchestrator/deno.json` to pin `jsr:@std/io` to `0.224.9`, ensuring the `BufReader` export required by `deno.land/x/amqp` remains available.
- Switching to the native Deno AMQP client `https://deno.land/x/amqp@v0.24.0/mod.ts`. This client uses `Deno.connectTls` (native rustls) which correctly handles SNI and the AMQP/TLS handshake with CloudAMQP, unlike the Node-compat `amqplib` layer.
- Restoring the `startConsumer` function with a robust `while(true)` reconnect loop, QoS prefetch(1), and manual ack/nack logic.
- Updating `deploy/game-orchestrator/Dockerfile` to copy the `deno.json` import map and pre-cache dependencies with it.
- Removed all temporary RabbitMQ HTTP-polling code and associated environment variables (`MQ_POLL_MS`, `MQ_POLL_BATCH`, `RABBITMQ_VHOST`).


[2026-05-30] [A-01] [FIX] **Admin container missing LiveAvatar/LiveKit env.** The admin service in `docker-compose.yml` did not pass `LIVEAVATAR_API_URL`, `LIVEAVATAR_API_KEY`, `LIVEKIT_SERVER_URL`, `LIVEKIT_API_KEY`, or `LIVEKIT_API_SECRET` through to the container. As a result the new `AvatarPicker`/`VoicePicker` server actions returned `{ notConfigured: true }` and the UI silently fell back to the plain UUID inputs — making the new template editor look unchanged after a rebuild. Added all five vars to the admin service `environment` block (with `${VAR:-}` defaults so a missing value does not break compose). Requires `docker compose up -d admin` to recreate the container with the new env.

[2026-05-30] [A-01] [FIX] **Admin panel cache disabled.** Added `Cache-Control: no-cache, no-store, must-revalidate` + `Pragma: no-cache` + `Expires: 0` headers to all non-static admin routes in `next.config.ts`. Static assets (`_next/static/*`) remain long-term cached via immutable fingerprinted URLs. Ensures admins always see the latest deployed version without a hard-refresh.


[2026-05-30] [A-01] [BUILD] **LiveAvatar Admin UI Integration.** Enhanced game-template editing with a rich selection UI for AI Presenters:
- Created `admin/src/lib/actions/liveavatar.ts` server actions (`fetchAvatars`, `fetchVoices`, `fetchVoicePreview`) calling the provider directly using `LIVEAVATAR_API_URL` and `LIVEAVATAR_API_KEY`.
- Built `AvatarPicker` component with a 4-column portrait grid, preview images, and selection highlights.
- Built `VoicePicker` component with searchable list, language filtering, and on-demand audio preview playback.
- Integrated pickers into `edit-template-form.tsx` with graceful manual UUID fallbacks.

[2026-05-30] [A-01] [BUILD] **Payments detail enhancements.** Expanded the payment detail view with identifiers, crypto details, extra data, and enriched transaction summary. Added a "Verify with gateway" button available for all non-succeeded statuses.

[2026-05-30] [A-01] [FIX] **.env cleanup & validation.** Deleted dead commented-out block (lines 1-35), fixed invalid inline comment in `BACKUP_SCHEDULE`, filled `MONGODB_URL` placeholder, and removed duplicate `LIVEKIT_*` vars.
[2026-05-30] [A-01] [REFACTOR] **RabbitMQ env consolidation.** Replaced four separate env vars (`RABBITMQ_MGMT_URL`, `RABBITMQ_USER`, `RABBITMQ_PASSWORD`, `RABBITMQ_VHOST`) with a single `RABBITMQ_URL` AMQP connection URI (e.g. `amqps://user:pass@host/vhost`). Added `parseRabbitUrl()` helper in `supabase/functions/_shared/rabbitmq.ts` that rewrites `amqp(s)://` to `http(s)://`, strips the AMQP port, and extracts user/password/vhost from the URI; `http(s)://` management URLs are still accepted as-is (preserves explicit ports such as 15672). `RABBITMQ_VHOST` is retained as an optional override only. Updated `_shared/rabbitmq_test.ts` (new parser tests + endpoint URL assertions), `.env.docker.example`, `docker-compose.yml` (api service env block), and `docs/game-templates.md` to reflect the new single-URL contract. No call-site changes required — `publishQuizShowStart()` signature unchanged.


[2026-05-30] [A-01] [BUILD] Added Payments management to Admin Panel:
- Navigation: Added "Payments" under Finance section.
- List View: Created admin/src/app/(app)/finance/payments/page.tsx with pagination and filters (method, status).
- Detail View: Created admin/src/app/(app)/finance/payments/[id]/page.tsx showing player info and raw gateway response.
- Actions: Added reconcilePayment Server Action to trigger status re-verification from the admin panel.
- Export: Added CSV export route for payments at admin/src/app/api/exports/finance/payments/route.ts.

[2026-05-28] [A-01] [BUILD] **Host avatar: URL input → file upload.** The only file-style URL input remaining in the admin UI (host avatar in `shows/create-host-dialog.tsx`) is now a file picker. New generic server action `admin/src/lib/actions/upload.ts::uploadAsset(kind, formData)` uploads to S3 under `hosts/avatars/<ts>-<uuid>.<ext>` (public-read, JPEG/PNG/WebP/SVG, 10 MB cap) and returns the public URL; reusable client component `admin/src/components/file-upload-input.tsx` shows preview + Replace/Remove controls and writes the URL into the parent form's state, which then gets passed to `createHost` alongside the rest of the host fields. Other URL inputs left untouched because they are external website links, not file pointers: voucher `partner_url`, email-broadcast CTA URL, custom-email URL, and the API-key `allowed_origins` whitelist.
[2026-05-28] [A-01] [BUILD] **Game poster image.** (1) Migration `20260528100000_games_poster_url.sql` adds nullable `poster_url TEXT` to `public.games`. (2) Edge Function `admin-games/index.ts` ASSET_FIELDS extended with `poster_url` \u2014 admins can now upload via `POST /admin/games/:id/asset` with `field=poster_url`; file goes to S3 under `games/<id>/poster_url-<ts>.<ext>` (public-read), URL stored on the game row, audit-logged. (3) Customer `/games` and public `/public-games` projections both extended to return `poster_url`. (4) Admin server action `uploadGameAsset` accepts the new field; UI page `games/[id]/page.tsx` shows a third upload tile next to Icon and Thumbnail. (5) `docs/public-api-reference.md` updated with `poster_url` in sample payloads and field reference table.\n[2026-05-28] [A-01] [BUILD] **Public game listing endpoints.** New `supabase/functions/public-games/index.ts` exposes two unauthenticated, read-only routes for the public marketing website: `GET /public-games` (list, paginated, filterable by `mode`, `status`, `featured`, `category`, `difficulty`, `language`, `search`, `sort`) and `GET /public-games/:id` (detail). Uses `getPublicClient()` (anon key, no JWT) — relies on the existing `games_select_all` RLS policy (`FOR SELECT TO anon USING (true)`) from `20260524200000_customer_rls_policies.sql`. Projection is the same `GAME_FIELDS` shape as customer `/games` minus `joined_by_me`. Existing `/games` JWT-gated endpoints are untouched. R-01: no PII fields (no participant lists, no host emails, no internal flags). R-04: anon client, RLS enforced.
[2026-05-28] [A-01] [BUILD] **Admin API Keys management.** (1) New migration `20260528000000_api_keys.sql` creates `public.api_keys` (key_id UNIQUE, secret_hash SHA-256, secret_hint, name, description, role with CHECK matching admin_users, allowed_domains TEXT[], expires_at, revoked_at, last_used_at/ip, created_by FK admin_users, created_at, updated_at). RLS enabled with no policies — service-role only (R-04). Raw secret is shown to the operator exactly once at creation, never persisted (R-01). (2) Extended `supabase/functions/_shared/auth.ts` with `validateAdminAccess(req, allowedRoles)` accepting either a Bearer JWT (existing path) or an `X-API-Key: key_id.secret` header. API-key path looks up by `key_id`, hashes the submitted secret with Web Crypto SHA-256, constant-time compares against `secret_hash`, enforces `revoked_at` / `expires_at` / `allowed_domains` (Origin), best-effort stamps `last_used_at`/`last_used_ip`, and returns a unified `{source, role, actorId, apiKeyId}`. Existing `validateJWT` / `requireAdminRole` retained — endpoints opt in to the new helper on demand. (3) New admin server actions `admin/src/lib/actions/api-keys.ts` (`createApiKey` returns full token ONCE, `revokeApiKey` flips `revoked_at`). Both audit-logged. (4) New page `admin/src/app/(app)/api-keys/page.tsx` + `api-key-actions.tsx` with create-dialog (one-time secret reveal + copy) and revoke-dialog. (5) Nav entry under System + `ROUTE_ROLES["/api-keys"] = ["super_admin"]`. Touched: 1 migration, 1 shared helper, 2 admin client files, 2 admin server files.
[2026-05-26] [A-01] [AUDIT] Session started. Continuing game management UI improvements.
[2026-05-26] [A-01] [BUILD] **Dedicated Game Edit Page.** (1) Replaced the `EditGameDialog` modal with a dedicated internal edit page at `admin/src/app/(app)/games/[id]/edit/page.tsx` for better UX on complex forms. (2) Extracted form logic into `EditGameForm` component with tab-free card-based layout. (3) Updated game detail page to link to the new edit route. (4) Cleaned up `game-actions.tsx` by removing the modal code and 10+ unused imports. (5) Added `questions_count` to the `GameSchema`, `EditGameForm`, and `updateGame` action to allow editing the total number of questions for a game.

[2026-05-25] [A-01] [BUILD] **Admin Panel extended game styling & asset uploads.** (1) Extended `GameSchema` in `admin/src/lib/actions/games.ts` with `accent_color`, `glow_color`, `gradient_colors`, `sponsor`, `tags`, `host_name`, `host_title`, and `rules` (with hex-color Zod validation). (2) Added `uploadGameAsset` server action using the existing `s3.ts` helper to handle `multipart/form-data` uploads for `icon`, `thumbnail_url`, and `host_avatar_url`. (3) Organised `create-game-dialog.tsx` into a tabbed layout ("Basic" and "Styling & Host") with color pickers, a dynamic gradient builder, and meta/host field inputs. (4) Added `EditStylingDialog` and `AssetUploadButton` to `game-actions.tsx`. (5) Enhanced `[id]/page.tsx` with a "Styling & Assets" card showing color swatches, asset previews, and upload controls. Updated grid to `lg:col-span-3` for participants.
[2026-05-25] [A-01] [FIX] **db-maintainer Postgres version mismatch.** Updated `deploy/db-maintainer/Dockerfile` to use `postgres:17-alpine` base image. This resolves the "server version mismatch" error where `pg_dump` v16 was unable to back up the Supabase Postgres 17 database.
[2026-05-25] [A-01] [AUDIT] Created `docs/api-reference.md` containing the complete Markdown API reference for customer and admin game-related endpoints.

[2026-05-25] [A-01] [BUILD] **Account security endpoints (password change + 2FA).** Added `POST /auth/change-password` (verifies current password via signInWithPassword then `admin.auth.admin.updateUserById`), `GET /security/settings`, `POST /security/2fa/email/{setup,enable,disable}` and `POST /security/2fa/totp/{setup,enable,disable}`. New migration `20260525200000_user_security.sql` creates `public.user_security` (1:1 with profiles) with RLS enabled and a SELECT-own policy. New `_shared/totp.ts` implements RFC 6238 TOTP + RFC 4648 Base32 + SHA-256 hex via Web Crypto (no npm dep, ±1 step drift tolerance). New `twoFactorCodeTemplate` added to `_shared/email.ts` reuses the existing brand shell. R-01: email OTPs are stored only as SHA-256 hashes; TOTP secrets never appear in logs.
[2026-05-25] [A-01] [FIX] **Fixed admin-games field mismatches.** Resolved mapping issues where request fields (`start_time`, `max_participants`) did not match DB columns (`scheduled_at`, `max_players`). Corrected `duplicate` logic to strip `participant_count` (which doesn't exist) and use `total_participants` if needed (though typically reset to 0 for clones). Fixed `POST /admin/games/:id/questions` to use the correct `order` column name for `game_questions` and removed `time_limit_sec` which is missing from the table.
[2026-05-25] [A-01] [FIX] **Environment variable pass-through in `docker-compose.yml`.** Added `BREVO_API_KEY` and `EMAIL_FROM` to the `api` container's `environment` block. Previously these were only in the `admin` container, causing the `security/2fa/email/setup` endpoint to silently skip sending emails with a warning log.
[2026-05-25] [A-01] [BUILD] **Game-flow completion + asset uploads.** (1) `GET /games` now returns the full styling payload customer apps need to render game cards — `accent_color`, `glow_color`, `gradient_colors`, `icon`, `thumbnail_url`, `subtitle`, `description`, `sponsor`, `tags`, `host_name`, `host_avatar_url`, `host_title`, `difficulty`, `language`. The DB already had every column (see initial_schema lines 188-211); only the list-select needed to be widened. (2) New admin endpoint `POST /admin/games/:id/asset` — multipart upload (`file` + `field`) for `icon` / `thumbnail_url` / `host_avatar_url`, mirrors the `/profile/avatar` S3 pipeline (public-read, 10 MB cap, JPEG/PNG/WebP/SVG), writes the URL to the matching column and emits an audit log entry. (3) New migration `20260525210000_game_rpcs.sql` defines the four SECURITY DEFINER functions the customer `games` Edge Function had been calling without a definition: `credit_wallet`, `join_game` (R-09 atomic debit + insert + tx), `leave_game` (refund pre-start only), `submit_answer` (scores + updates participant tallies). (4) `games/index.ts` fixed to schema reality: `GET /games/:id/question` no longer references the non-existent `current_question_index` / `question_order` / `time_limit_sec` — it derives the next question from the participant's answered-count, joins via `game_questions."order"`, and returns `time_per_question_sec` from `games.time_per_question`. `DELETE /games/:id/join` now goes through the `leave_game` RPC. `POST /games/:id/claim-prize` no longer multiplies the prize by 100 before passing to `credit_wallet` — both sides treat the value as NUMERIC dollars (matching the existing R-02 conflict note). (5) `scripts/api-smoke-test.py` extended with the customer game flow (`/games/:id`, `/participants`, `/leaderboard`, `/result`, `/question`, join + leave).
[2026-05-25] [A-01] [BUILD] **db-maintainer service.** New container under `deploy/db-maintainer/` (image: `postgres:16-alpine` + `aws-cli` + busybox `crond`) that owns the database lifecycle outside of the Edge Functions. On every start it (1) connects to Supabase Postgres via `SUPABASE_DB_URL`, (2) applies pending SQL files from `supabase/migrations/` (mounted read-only) using a `public.schema_migrations(version, checksum, applied_at)` tracking table — single-transaction per file, with a one-shot bootstrap that marks existing files as applied if `profiles` already exists so we don't double-run anything that Supabase Studio already executed, (3) runs a consistency check (connectivity, REQUIRED_TABLES list, every public table has RLS enabled, `schema_migrations` non-empty), and (4) signals readiness via `/tmp/db-maintainer-ready`. The `api` service now `depends_on: db-maintainer { condition: service_healthy }` so it never boots against a stale schema. A cron job (`BACKUP_SCHEDULE`, default `0 2 * * *` UTC) runs `pg_dump -Fc | gzip` and uploads to `s3://$S3_BUCKET/$S3_BACKUP_PREFIX/YYYY-MM-DD/quiz4win-<ts>.dump.gz` via `aws --endpoint-url $S3_ENDPOINT s3 cp` (reuses the existing DigitalOcean Spaces credentials), then re-runs the consistency check and prunes both local and remote dumps older than `BACKUP_RETENTION_DAYS` (default 14). The `schema_migrations` table has RLS enabled with no policies (R-04) so PostgREST clients can't read it; the db-maintainer connects as `postgres` which bypasses RLS. New env keys in `.env.docker.example`: `SUPABASE_DB_URL`, `BACKUP_SCHEDULE`, `RUN_BACKUP_ON_START`, `BACKUP_RETENTION_DAYS`, `S3_BACKUP_PREFIX`. New volume `quiz4win-db-backups` keeps a short local cache.



[2026-05-24] [A-01] [FIX] Aligned all customer-facing Edge Functions with actual DB schema (commit 72c52c4): auth (is_active/profile-create), profile (lazy-create/DELETE), wallet (currency/reference), settings (columns), notifications (is_read/push-tokens), withdrawals (currency/completed_at), games (schema columns/prize_earned), leaderboard (full_name alias), support (content/no-priority), referrals (owner_id/referral_uses), vouchers (reward_value/voucher_redemptions/announcements).
[2026-05-24] [A-01] [AUDIT] Smoke test script at scripts/api-smoke-test.py; all 12 handlers tested. Changes committed and pushed (72c52c4). Awaiting deploy on server.
[2026-05-24] [A-02] [AUDIT] Session started. Auditing project state against Rules.md and Architecture_Map.md.
[2026-05-24] [A-02] [RULE] **VIOLATION (R-07):** Previous session changes by A-01 were logged but didn't follow the session start/end protocols defined in Collaboration_Protocol.md.
[2026-05-24] [A-03] [ARCH] **CONFIRMED:** `quiz4win-app` container (port 5801) is the unified host for Universal Links and web fallbacks. A-01 correctly implemented it after initially deviating with static files proposal.
[2026-05-24] [A-01] [FIX] **Correcting `auth/forgot-password` flow:** Switched from built-in mailer to `generateLink` + `sendEmail` (Brevo) to ensure brand consistency (dark shell, logo, indigo CTA) across all transactional emails.

# Quiz4Win Backend — AI Agent Change Log

[2026-05-31] [A-01] [FIX] **validateJWT: switch from `getAnonClient(req)` to `getPublicClient()` in `_shared/auth.ts`.** Root cause of the "Invalid API key" / 401 on `POST /auth/update-password` (and every other endpoint that calls `validateJWT`): `getAnonClient(req)` puts the user's Bearer JWT into `global.headers.Authorization`, which overrides the SDK's default `Authorization: Bearer <ANON_KEY>` on all `/auth/v1/*` calls — gotrue then rejects the call with "Invalid API key". The token is passed *explicitly* to `supabase.auth.getUser(token)` so no header forwarding is needed. One-line change, same pattern as `408cd35`. Confirmed from production logs.

[2026-05-31] [A-01] [REVERT] **reset-password: reverted `3e4da41` + `59c562e`.** User reported signin and other endpoints regressed after the password-reset edits. Both `supabase/functions/auth/index.ts` and `app/public/auth/reset-password.html` restored to their state at `408cd35` (last known-good commit). Pushed as `535baf6`. The original password-reset "link expired" issue is back on the open-task list; **do not retry without explicit user approval and a clear diagnostic from the edge-function logs first.**

[2026-05-24] [A-01] [FIX] Email logo now served from app.quiz4win.com (public static container) instead of panel.quiz4win.com to avoid auth-middleware / image-proxy issues. Removed duplicate from admin/public/email/.
[2026-05-24] [A-01] [BUILD] **Branded customer password-recovery email.** Customer "forgot password" emails were previously sent by Supabase Auth's built-in mailer via `supabase.auth.resetPasswordForEmail()` — unbranded, no Q4W logo, no responsible-play footer. Replaced with the same Brevo + brand-shell pipeline used by every other transactional email. New `recoveryTemplate` in `supabase/functions/_shared/email.ts` (dark header + indigo CTA + optional 6-digit OTP box for the in-app code-entry flow). The `POST /auth/forgot-password` endpoint now: (1) calls `admin.auth.admin.generateLink({ type: "recovery", email, options: { redirectTo } })` to mint the action link and OTP without triggering Supabase's email; (2) looks up `profiles.full_name` for personalisation; (3) sends via Brevo `sendEmail()` fire-and-forget (never blocks the response, never leaks account existence). Endpoint still returns 200 unconditionally (anti-enumeration). The existing `/auth/verify-otp` and `/auth/update-password` endpoints continue to work unchanged — the OTP and access token are the same ones Supabase would have issued.

[2026-05-24] [A-01] [FIX] **`_shared/auth.ts` validateJWT — pass JWT explicitly to `supabase.auth.getUser()`.** Previously called `supabase.auth.getUser()` with no argument; in an Edge Function context (`persistSession: false`) the SDK has no internal session, so the call returned `AuthSessionMissingError` regardless of the forwarded `Authorization` header. The forwarded header was used by PostgREST sub-requests but **not** by the auth client's `getUser()` path. Fix: extract the Bearer token and pass it as `supabase.auth.getUser(token)`. This is the canonical Supabase Edge-Function pattern. Repairs every endpoint that uses `validateJWT` (settings, admin-auth, admin-analytics, admin-questions, topup, kyc, auth/update-password, referrals, and more — 13+ endpoints). Visible symptom this fixes: the customer password-reset web fallback (`https://app.quiz4win.com/auth/reset-password#access_token=…&type=recovery`) was POSTing to `api.quiz4win.com/auth/update-password`, the server returned 401, and the static page rendered "Link invalid or expired".

[2026-05-24] [A-01] [ARCH] **New `app/` module — app.quiz4win.com static container.** Created top-level `app/` directory hosting a minimal `nginx:alpine` static container that serves both the Universal Link / App Link deep-link manifests AND the branded password-reset web fallback from a single image. Files: `app/Dockerfile`, `app/nginx.conf`, `app/.dockerignore`, `app/public/index.html` (landing → app stores), `app/public/auth/reset-password.html` (reads `#access_token=…&type=recovery` from URL hash, POSTs to `https://api.quiz4win.com/auth/update-password` with Bearer token — same endpoint the Expo app uses per MOBILE_TEAM_PROMPT.md §2), `app/public/.well-known/apple-app-site-association` (iOS — TEAMID placeholder), `app/public/.well-known/assetlinks.json` (Android — SHA-256 placeholder), `app/public/q4w_text_logo.png`. Updated `docker-compose.yml` to add the `app` service bound to `127.0.0.1:5801:8080`. Simplified `deploy/nginx/app.quiz4win.com.conf` — removed the local `/var/www/app.quiz4win.com/.well-known/` static block; everything (including manifests) now flows through the container so there is one source of truth. Updated `deploy/nginx/setup.sh` to drop the `APP_WEBROOT` mkdir step. **Root cause this fixes:** nginx was proxying `app.quiz4win.com → 127.0.0.1:5801` but no docker service was bound to that port, so password-reset recovery links opened in a browser returned 502.

[2026-05-24] [A-01] [BUILD] **Phase 1-6 Email System Implementation.**
- [Phase 1] Premium brand shell for all transactional emails (admin/src/lib/admin-auth/email-brand.ts).
- [Phase 2] Welcome email on customer signup (supabase/functions/_shared/email.ts + supabase/functions/auth/index.ts).
- [Phase 3] Admin-initiated custom email UI (admin/src/app/(app)/users/[id]/custom-email-action.tsx) and server action (sendCustomEmail).
- [Phase 4] Voucher notification emails with UI checkbox in voucher-actions.
- [Phase 5] Win / prize notification email triggered on successful claim-prize in supabase/functions/games/index.ts.
- [Phase 6] Bulk Email Broadcast system: new tables email_broadcasts, email_messages; new admin UI under /email-broadcasts; server action createEmailBroadcast; Edge Function worker email-broadcast.

[2026-05-23] [A-01] [BUILD] Created MOBILE_TEAM_PROMPT.md — a detailed handoff document for the mobile team explaining how to implement Universal Links and password reset without the Supabase SDK.
[2026-05-23] [A-01] [FIX] /auth/update-password edge function now uses admin.auth.admin.updateUserById() instead of anon supabase.auth.updateUser(). The previous implementation silently failed with "Auth session missing" because the anon client only had the Bearer token forwarded as a header — no internal GoTrue session was established via setSession(). validateJWT() still verifies the token via getUser() before the admin client performs the actual write. Affects both the in-app OTP flow and the new Universal Link magic-link flow.
[2026-05-23] [A-01] [BUILD] /auth/forgot-password now passes redirectTo=https://app.quiz4win.com/auth/reset-password (configurable via APP_URL env var) so end-user-initiated recovery emails open in the Expo app via Universal Link. Added APP_URL to the api service in docker-compose.yml.
[2026-05-23] [A-01] [BUILD] Added deploy/nginx/app.quiz4win.com.conf — TLS reverse-proxy config for app.quiz4win.com (Universal Link host). Serves .well-known/ from /var/www/app.quiz4win.com (apple-app-site-association, assetlinks.json) and proxies everything else to 127.0.0.1:5801. Updated deploy/nginx/setup.sh to issue a Let's Encrypt cert for app.quiz4win.com and create the static webroot.
[2026-05-23] [A-01] [BUILD] Recovery/invite redirectTo now points at https://app.quiz4win.com/auth/reset-password so password-reset emails open in the Expo mobile app via Universal Link. Added appUrl() helper in auth-users.ts reading APP_URL env var (server-only, default https://app.quiz4win.com). Added APP_URL to docker-compose.yml admin service. Magic-link redirectTo unchanged (stays on admin panel for staff).
[2026-05-24] [A-01] [AUDIT] Re-read updated Google Sheet (15gjlI2Wgh7G21dZG-WKw9Nu8r5imHpmxYHvnRHL2-_Q). Cross-referenced all 200+ rows in "Quiz4Win - APIs" tab against codebase. All 56 customer APIs (#1-55 + vouchers #155-158) and all admin APIs (admin-analytics, admin-audit, admin-auth, admin-config, admin-dashboard, admin-finance, admin-games, admin-kyc, admin-livekit, admin-notifications, admin-questions, admin-referrals, admin-reports, admin-shows, admin-support, admin-users, admin-vouchers) are implemented. Found 2 missing routes: PATCH /admin/questions/categories/:name and DELETE /admin/questions/categories/:name (sheet rows 81-82). Both implemented in admin-questions/index.ts.
[2026-05-24] [A-01] [BUILD] admin-questions/index.ts — Added PATCH /admin/questions/categories/:name (update category name/description, row 81) and DELETE /admin/questions/categories/:name (remove category, row 82). Both operate on the app_config key "question_categories", audit-logged, and guard DELETE against non-empty categories.
[2026-05-24] [A-01] [ARCH] Separated admin auth completely from Supabase Auth. Customers continue to use Supabase Auth (auth.users). Admins now use a native system: bcrypt password_hash in admin_users, TOTP secret stored in admin_users.mfa_secret, sessions in admin_sessions (cookie: q4w_admin_session), password-reset tokens in admin_password_reset_tokens, MFA challenges in admin_mfa_challenges. All emails sent via Brevo from noreply@quiz4win.com.
[2026-05-24] [A-01] [BUILD] Created admin/src/lib/admin-auth/* — shared auth utilities: passwords.ts (bcrypt), tokens.ts (SHA-256 token hash, recovery codes), totp.ts (otplib TOTP), sessions.ts (session CRUD + cookie helpers), session-validator.ts (DB-validated session lookup), email.ts (Brevo client), email-templates.ts (password-reset, invite, confirmation), audit.ts (audit log helper).
[2026-05-24] [A-01] [BUILD] Replaced Supabase-based middleware with native cookie check (admin/src/middleware.ts). Protected routes require q4w_admin_session cookie; full cryptographic validation deferred to RSC layout (getCurrentAdmin).
[2026-05-24] [A-01] [BUILD] Replaced Supabase auth in admin/src/lib/auth.ts. requireAdmin() and getCurrentAdmin() now validate via validateSessionToken() (DB session lookup + bcrypt) instead of supabase.auth.getUser().
[2026-05-24] [A-01] [BUILD] Created API routes: POST /api/admin/auth/login (phase-1 password check, returns challenge token), POST /api/admin/auth/mfa/verify (TOTP verify → aal2 session), GET+POST /api/admin/auth/mfa/setup (first-login MFA enrollment + QR), POST /api/admin/auth/logout (revoke session + clear cookies), POST /api/admin/auth/forgot-password (Brevo reset email), POST /api/admin/auth/reset-password (token-validated password change).
[2026-05-24] [A-01] [BUILD] Updated admin/src/app/login/login-form.tsx — removed all Supabase Auth calls, now uses native API routes. Added Forgot password? link. Login flow: credentials → challenge token → TOTP verify or first-time MFA setup.
[2026-05-24] [A-01] [BUILD] Updated admin/src/app/auth/reset-password/page.tsx — removed Supabase hash-token flow, uses ?token= query param with POST /api/admin/auth/reset-password. Added admin/src/app/auth/forgot-password/page.tsx.
[2026-05-24] [A-01] [ARCH] Replaced Supabase Storage with S3-compatible object storage (DigitalOcean Spaces, region fra1, bucket wingobingo) for ALL file uploads (KYC, avatars, future attachments). Credentials sourced from S3_BUCKET/S3_REGION/S3_ACCESS_KEY/S3_SECRET/S3_ENDPOINT env vars (R-01). Buckets `kyc-documents` and `user-avatars` are no longer referenced. Key layout: kyc/<uid>/<doc>.<ext> (private, 10-min presigned GET) and avatars/<uid>/avatar.<ext> (public-read).
[2026-05-24] [A-01] [BUILD] Added supabase/functions/_shared/s3.ts (Deno) and admin/src/lib/s3.ts (Node) — symmetric helpers: uploadObject(key, body, contentType, visibility), presignGet(key, expiresSec), deleteObject(key), buildPublicUrl(key). Both use @aws-sdk/client-s3 + @aws-sdk/s3-request-presigner. Added the packages to admin/package.json and supabase/functions/deno.json imports.
[2026-05-24] [A-01] [BUILD] Migrated supabase/functions/kyc/index.ts: id_front / id_back / selfie now uploaded to S3 as private objects via uploadObject(); only the object key is persisted in kyc_requests.
[2026-05-24] [A-01] [BUILD] Migrated supabase/functions/profile/index.ts (POST /profile/avatar): avatar uploaded to S3 as public-read; canonical virtual-hosted URL (https://wingobingo.fra1.digitaloceanspaces.com/avatars/<uid>/avatar.<ext>) saved to profiles.avatar_url.
[2026-05-24] [A-01] [BUILD] Migrated admin/src/lib/actions/kyc.ts signKycDocumentUrl(): now calls presignGet() against S3 (600 s) instead of Supabase storage.createSignedUrl. Existing kyc[id] review page calls remain unchanged.
[2026-05-24] [A-01] [BUILD] docker-compose.yml: added S3_BUCKET / S3_REGION / S3_ACCESS_KEY / S3_SECRET / S3_ENDPOINT to both admin and api services; added BREVO_API_KEY to admin (was missing).

Last updated: 2026-05-22
Owner: A-02 (Project Memory Guardian)

> **APPEND-ONLY.** Never edit or delete existing entries.
> Format: `[YYYY-MM-DD] [AGENT-ID] [PREFIX] Description`
> Newest entries go at the TOP of this file.

---

[2026-05-23] [A-01] [BUILD] **Password recovery flow: public reset-password page + correct redirectTo.** Created `admin/src/app/auth/reset-password/page.tsx` — a public client-side page (no auth required; route is in the `/auth/*` namespace already whitelisted by middleware) that reads `access_token`/`refresh_token`/`type` from the URL hash, calls `supabase.auth.setSession` to establish the recovery session, presents a confirm-password form, calls `supabase.auth.updateUser({password})`, then signs the user out and shows a success screen. Handles all error states (expired token, wrong type, Supabase error). Updated `sendAuthLink` in `auth-users.ts` to pass `options.redirectTo` pointing at `/auth/reset-password` for recovery links and `/login` for magic-link. Updated `inviteUserByEmail` to also redirect invited users to `/auth/reset-password` so they land on the password-set form. Added `NEXT_PUBLIC_ADMIN_URL` build-arg + env to `admin/Dockerfile` and `docker-compose.yml`. Set `NEXT_PUBLIC_ADMIN_URL` in `.env` to the public-facing admin URL (e.g. `https://panel.quiz4win.com`) and add that URL to the Supabase Auth allowed redirect URLs list.\n\n[2026-05-23] [A-01] [BUILD] **Supabase Auth admin management in admin panel.** New server-action module `admin/src/lib/actions/auth-users.ts` exposes: `createAuthUser`, `deleteAuthUser`, `setUserPassword`, `updateUserEmail`, `setEmailConfirmation`, `banUser`, `unbanUser`, `revokeAllSessions`, `sendAuthLink` (recovery/magiclink), `inviteUserByEmail` — all routed through `supabase.auth.admin.*` using the service-role client, gated by `requireAdmin` role checks, with zod validation and `admin_audit_log` entries (using correct `entity_type`/`entity_id` columns). UI additions: (1) new "Authentication" card on user detail page showing `email_confirmed_at`, `last_sign_in_at`, `banned_until`, phone, identity providers, raw user metadata; (2) `auth-actions.tsx` component on user detail page providing "Set password" dialog plus a dropdown menu for recovery/magic link, confirm email, change email, revoke sessions, ban (1h/24h/7d/30d/365d/permanent) / unban, delete user — each wrapped in AlertDialog confirmation; (3) `create-user-dialog.tsx` on users list page with tabs for "Create user" (email/password/full_name/auto-confirm) and "Send invite". Brings the admin panel to feature-parity with the relevant parts of Supabase's hosted auth dashboard.

[2026-05-23] [A-01] [FIX] **Auto-create public.profiles row on auth.users insert.** New migration `20260523220000_auto_create_profile_on_signup.sql` adds `public.handle_new_auth_user()` SECURITY DEFINER trigger function and `on_auth_user_created` AFTER INSERT trigger on `auth.users`. Mirrors id/email/full_name/referral_code from `raw_user_meta_data` into `public.profiles` on every signup. Includes a one-time backfill for existing auth.users without a profile. Resolves admin panel showing 0 users despite successful signups (root cause: signup function only called `supabase.auth.signUp()` which never touches public.profiles, and no DB trigger existed to bridge them).

[2026-05-23] [A-01] [FIX] **Compatibility fix for Nginx http2 directive.** Modified `deploy/nginx/panel.quiz4win.com.conf` and `deploy/nginx/api.quiz4win.com.conf` to move `http2` into the `listen` line (e.g., `listen 443 ssl http2;`). This resolves the `unknown directive "http2"` error on Nginx versions older than 1.25.1 where `http2 on;` is not supported as a standalone directive.


[2026-05-23] [A-01] [BUILD] **Nginx deploy configs and Let's Encrypt setup script.** Created `deploy/nginx/` folder with: `panel.quiz4win.com.conf` (HTTP→HTTPS redirect + TLS proxy to 127.0.0.1:5800, security headers, streaming support), `api.quiz4win.com.conf` (same pattern proxying to 127.0.0.1:5802, CORS OPTIONS fast-path, 50 MB body limit, `/health` passthrough), and `setup.sh` — a root-level bash script that installs bootstrap HTTP configs, runs `certbot certonly --webroot` for each domain, installs the final TLS configs, reloads nginx, and registers a daily cron for auto-renewal. Script is idempotent (skips cert issuance if certs already exist, skips cron if already registered).

[2026-05-23] [A-01] [BUILD] **Dockerized admin panel and edge-functions API.** Added `admin/Dockerfile` (multi-stage Next.js standalone, node:22-alpine), `admin/.dockerignore`, and enabled `output: "standalone"` in `admin/next.config.ts`. Added `supabase/functions/_server.ts` — a Deno entrypoint that shims `Deno.serve` to capture each function's handler, dynamically imports every `<name>/index.ts` under `supabase/functions/` (skipping `_*` dirs), then runs a single dispatcher that routes `/admin/<x>/...` → handler `admin-<x>` and `/<x>/...` → handler `<x>`. Added `supabase/functions/Dockerfile` (denoland/deno:alpine, port 8000) and `.dockerignore`. Created root `docker-compose.yml` binding admin to `127.0.0.1:5800` and api to `127.0.0.1:5802` (host nginx terminates TLS for panel.quiz4win.com and api.quiz4win.com). Env wiring: `NEXT_PUBLIC_*` passed as build args + runtime env; API service maps `NEXT_PUBLIC_SUPABASE_URL` → `SUPABASE_URL` for the edge-function client. Created `.env.docker.example` and root `.dockerignore`. No new function code or schema changes.

[2026-05-22] [A-01] [BUILD] **Drizzle ORM scaffolding complete.** Installed `drizzle-orm@^0.45.2`, `postgres@^3.4.9` (runtime) and `drizzle-kit@^0.31.10`, `dotenv`, `tsx` (dev). Created `drizzle.config.ts` (reads DB URL from `.env`, targets Supavisor pooler). Ran `drizzle-kit pull` against the live DB to generate type-safe schema — output moved to `supabase/drizzle/` (schema.ts 934 lines, relations.ts 401 lines, 0000_eminent_dorian_gray.sql). Created Deno import map at `supabase/functions/deno.json`. Created `supabase/functions/_shared/db/client.ts` (Drizzle + postgres.js connection, max 1 conn, prepare=false for Supavisor) and `supabase/functions/_shared/db/index.ts` (barrel re-exporting db client + all 31 table definitions + relations). Edge Functions can now import via `import { db, profiles, games } from "../_shared/db/index.ts"`.

[2026-05-22] [A-01] [BUILD] **Initial schema migration APPLIED to remote Supabase Postgres** (project `xcvzagyszaxnnjojnlep`, region eu-west-1, PostgreSQL 17.6). 31 tables created in `public` schema, all with `ROW LEVEL SECURITY ENABLED`. Direct connection (`db.<ref>.supabase.co`) failed due to IPv6-only resolution from local network; fell back to Supavisor session-mode pooler (`aws-0-eu-west-1.pooler.supabase.com:5432`, user `postgres.<ref>`). Migration committed in a single transaction. Used temporary script `.tmp_apply_migration.py` (gitignored, reads URL from `.env`, no secrets embedded). Note: actual table count is 31, not 30 — earlier docs undercounted by one; doc updates pending.

[2026-05-22] [A-02] [RULE] R-01 VIOLATION FLAGGED: `.tmp_run.py` in repo root contains a hardcoded Google service-account private key. This file must be deleted immediately and must not be committed to git. `.gitignore` created to prevent accidental commit. Action required: run `rm .tmp_run.py .tmp_fetch.py .tmp_read_sheet.py .tmp_sheet_result.txt` and verify `git status` shows no sensitive files staged.

[2026-05-22] [A-01] [BUILD] Created `.gitignore` covering: `.env`, `.env.*`, `.tmp_*.py`, `.tmp_*.txt`, `.tmp_*.json`, Supabase temp dirs, Deno cache, Node modules, macOS/IDE artifacts.

[2026-05-22] [HUMAN] [BUILD] Supabase project credentials added to `.env` in repo root. P0 blocker "Confirm Supabase project credentials" resolved. Reminder: `.env` must be listed in `.gitignore` before any git commit (R-01 — secrets must never be committed to version control).

[2026-05-22] [A-01] [AUDIT] Synced all project-brain/ and agents/ documentation with the completed 30-table initial schema. Changes: Architecture_Map.md §2 (repo structure with actual files), §4 (full 30-table canonical list replacing placeholder 10-table list), §5 (3 new architectural decisions); Domain_Knowledge.md §2 (corrected wallet model — balance in profiles, no separate wallets table; added show hosts §2.6, vouchers §2.9; updated all game mode/status values); Domain_Knowledge.md §3 (INV-01–10 updated with correct column names; added INV-11 KYC attempt limit, INV-12 AML threshold, INV-13 voucher fraud); Coding_Standards.md (money-field conflict notice, database.types.ts import guidance, updated column conventions); Rules.md R-04 (added note on current RLS policy gap); agents/architecture_agent.md (existing 30-table list, updated checklists); agents/augment_code_agent_integration.md (added responsibility 7 for types sync, added current schema state table); agents/domain_validation_agent.md (INV count updated to 13, corrected table references, expanded red flags); agents/project_memory_guardian.md (noted known R-02 and R-04 gaps).

[2026-05-22] [A-01] [BUILD] Created supabase/functions/_shared/database.types.ts with TypeScript interfaces for all 30 tables, including string-union enum types for all CHECK-constrained columns, plus an aggregate `Database` type compatible with the Supabase client. Money fields typed as `number` to match underlying NUMERIC columns (pending R-02 conflict resolution).

[2026-05-22] [A-01] [RULE] Conflict flagged between Data Schema (Google Sheet) and Rule R-02: the sheet defines monetary columns as NUMERIC(10,2) / NUMERIC(12,2) (decimal dollars), while R-02 mandates integer cents. Initial migration follows the sheet as the immediate source of truth; final decision deferred to human + A-05 (Domain Validation). Logged as a P0 blocker in Open_Tasks_AI.md.

[2026-05-22] [A-01] [BUILD] Created supabase/migrations/20260522120000_initial_schema.sql (~746 lines, 30 tables) covering identity, admin, games/content, finance, referrals, vouchers, communications, and content/config. Every table has primary keys, FKs, CHECK constraints, indexes, and ROW LEVEL SECURITY ENABLED. RLS policies themselves are deferred to a follow-up migration.

[2026-05-22] [A-01] [ARCH] Created supabase/migrations/ and supabase/functions/_shared/ directories per Architecture_Map.md §1. No structural deviation from canonical layout.

[2026-05-22] [A-01] [BUILD] Created /agents/ directory with five agent definition files: augment_code_agent_integration.md, project_memory_guardian.md, architecture_agent.md, refactor_agent.md, domain_validation_agent.md.

[2026-05-22] [A-03] [ARCH] Created project-brain/Architecture_Map.md defining repository structure, module boundaries, import directions, canonical table list, and initial architectural decisions log.

[2026-05-22] [A-05] [DOMAIN] Created project-brain/Domain_Knowledge.md capturing 10 business invariants (INV-01 through INV-10), game lifecycle, domain concepts, and regulatory placeholders. Human input required for INV-06, INV-07, INV-08, and compliance jurisdiction.

[2026-05-22] [A-02] [RULE] Created project-brain/Rules.md with R-01 through R-10 covering secrets safety, integer cents, JWT validation, RLS enforcement, append-only transactions, import boundaries, change logging, KYC gating, atomic wallet+join, and agent authority.

[2026-05-22] [A-01] [BUILD] Created project-brain/Coding_Standards.md covering TypeScript/Deno style, Edge Function template, DB migration conventions, RLS template, error handling, security patterns, testing conventions, and version control rules.

[2026-05-22] [A-02] [BUILD] Created project-brain/Collaboration_Protocol.md defining message format, prefix semantics, handoff flows, blocking/escalation protocol, and session start/end procedures.

[2026-05-22] [A-02] [BUILD] Created project-brain/Open_Tasks_AI.md with initial priority task queue derived from framework setup, domain placeholders, and pending backend implementation work.

[2026-05-22] [A-02] [AUDIT] Multi-agent coordination framework bootstrapped for Quiz4Win backend repository. All /project-brain/ files created. All /agents/ definition files created. agents.md root registry created. Fourteen placeholders marked <<TBD by Human>> require human input before implementation can proceed. See Open_Tasks_AI.md for prioritised task list.

[2026-05-28] [A-01] [BUILD] public-winners: added GET /public-winners endpoint (supabase/functions/public-winners/index.ts) returning aggregate results per completed game run plus rolling totals. Query params: limit (1-100, default 20), game_id (UUID or show_id slug), language (en|ar|fa|tr). Uses anon client + RLS (R-04), no PII (R-01). Documented in docs/public-api-reference.md.

[2026-05-28] [A-01] [BUILD] public-leaderboard: added GET /public-leaderboard endpoint (supabase/functions/public-leaderboard/index.ts) ranking players by survivor finishes + credits earned over a window. New migration 20260528200000_public_leaderboard_rpc.sql adds SECURITY DEFINER function public.get_public_leaderboard granted to anon — the function defines the curated exposed surface (R-04, no service-role bypass in app code). Player names rendered server-side as "<First> <Initial>." — no full names, emails, wallets, or user IDs exposed (R-01). Query params: period (required: weekly|all_time), limit (1-100, default 20), language (en|ar|fa|tr). Documented in docs/public-api-reference.md.

[2026-05-28] [A-01] [BUILD] public-featured-host: added GET /public-featured-host endpoint (supabase/functions/public-featured-host/index.ts) returning the host of the closest active featured game (is_featured=TRUE AND status IN upcoming/open/live, ordered by scheduled_at ASC). Returns { host: null } when no qualifying game / no linked host. New migration 20260528300000_show_hosts_public_access.sql adds show_hosts.years_on_air INTEGER column and an anon-readable SELECT RLS policy restricted to status='active' rows (all show_hosts columns are public presenter data — no PII). Documented in docs/public-api-reference.md.

[2026-05-28] [A-01] [BUILD] public-host-applications: Added POST /public-host-applications public endpoint (supabase/functions/public-host-applications/index.ts). Accepts name, email, country, instagram, followers. IP-based rate limiting: 3/10min and 5/hr via SECURITY DEFINER RPC check_host_application_rate_limit(). Returns {ok:true,application_id}. R-01: ip_address never returned publicly. R-04: anon client only.

[2026-05-28] [A-01] [BUILD] admin-host-applications: Added admin Edge Function (supabase/functions/admin-host-applications/index.ts). GET list with pagination+status filter, PATCH status+notes, POST custom email via Brevo. JWT+admin-role guarded (R-03). Audit-logged.

[2026-05-28] [A-01] [BUILD] host_applications table: Added migration 20260528310000_host_applications.sql. Table with status CHECK (pending|accepted|rejected|info_requested), RLS anon INSERT only, SECURITY DEFINER rate-limit RPC, indexes on ip+created_at, status+created_at, email.

[2026-05-28] [A-01] [BUILD] admin host-applications UI: Added admin/src/app/(app)/host-applications/ pages (list + detail) and admin/src/lib/actions/host-applications.ts server actions (updateApplicationStatus, sendCustomEmailToApplicant). Nav item added under Content section.

[2026-05-28] [A-01] [BUILD] public-early-birds: Added POST /public-early-birds public endpoint (supabase/functions/public-early-birds/index.ts) for mobile app early-access sign-ups (iOS + Android). Body: {platform, name, email}. For iOS, email is the Apple ID identifier from Sign in with Apple. IP-based rate limiting: 5/10min and 10/hr via SECURITY DEFINER RPC check_early_bird_rate_limit(). Unique on (platform, lower(email)). Sends branded welcome email asynchronously (best-effort). R-01: ip_address never returned. R-04: anon client only.

[2026-05-28] [A-01] [BUILD] early_birds table: Added migration 20260528320000_early_birds.sql. Table with platform CHECK (ios|android), unique partial index on (platform, lower(email)), RLS anon INSERT only, SECURITY DEFINER rate-limit RPC, indexes on ip+created_at and platform+created_at.

[2026-05-28] [A-01] [BUILD] Email templates: Added earlyBirdWelcomeTemplate(name, platform) and hostApplicationReceivedTemplate(name) to supabase/functions/_shared/email.ts. Both use the existing brand shell (logo, indigo CTA, responsible-play footer). Early-bird email lists launch perks; host-application email outlines the 3-step review flow.

[2026-05-28] [A-01] [BUILD] public-host-applications: Now sends a branded "Thanks for applying" confirmation email to the applicant immediately after a successful submission (best-effort, async — does not block the HTTP response). Uses hostApplicationReceivedTemplate.

[2026-05-28] [A-01] [BUILD] Admin panel - Early Birds: Added full management section for mobile early-access sign-ups.
- admin/src/app/(app)/early-birds/page.tsx: List view with platform, country, and email-sent status.
- admin/src/lib/actions/early-birds.ts & resend-button.tsx: Admin action to resend the branded welcome email.
- admin/src/lib/nav.ts: Added "Early Birds" (Smartphone icon) to the Content section.
- supabase/functions/admin-early-birds/index.ts: New admin Edge Function for listing, filtering, and resending emails. Guarded by requireAdminRole.

[2026-05-28] [A-01] [FIX] public-host-applications + public-early-birds: RLS rejection on INSERT.
- Root cause: Edge Functions used .insert(...).select("id").single() which compiles to INSERT ... RETURNING id. PostgreSQL requires the inserted row to pass a SELECT policy for the RETURNING clause; we only granted INSERT to anon, so every submission was rejected with "new row violates row-level security policy for table".
- Fix:
  - supabase/migrations/20260528340000_public_inserts_rls_fix.sql: recreates both INSERT policies as TO anon, authenticated and adds explicit GRANT INSERT.
  - supabase/functions/public-host-applications/index.ts & public-early-birds/index.ts: generate the row UUID client-side via crypto.randomUUID() and drop the .select(...).single() chain - no RETURNING means no SELECT policy needed. The returned application_id / early_bird_id matches the inserted row.

[2026-05-28] [A-01] [BUILD] Payments API v1: Added top-up flow for MasterCard (via Remitation), with placeholders for Apple Pay and Crypto.
- supabase/migrations/20260528350000_payments_v1.sql: New payments table tracking the gateway lifecycle (init|pending|succeeded|failed|cancelled|expired) keyed by a client-generated UUID. amount_cents BIGINT per R-02. RLS: users SELECT their own rows; service_role bypasses for writes. SECURITY DEFINER RPC complete_payment(p_payment_id, p_provider_response) locks the payment row, inserts an immutable transaction (R-05), credits profiles.wallet_balance + total_deposited, and flips payments.status to succeeded - all atomically (R-09).
- supabase/functions/payments/index.ts: New Edge Function. POST /payments initiates a payment (JWT required) - calls Remitation /generate with x-access-key / x-secret-key, stores provider _id / shortId / paymentLink, returns redirect_url. GET /payments/:id returns status (JWT, scoped to user). POST /payments/:id/verify (no auth - called by the return page) re-queries Remitation, normalises state (successful|success|paid|completed -> succeeded; failed|cancelled|expired -> failed), and on success invokes complete_payment RPC.
- app/public/pay/return.html: Branded landing page (Quiz4Win shell). Reads ?id=, calls /payments/:id/verify, shows success/pending/failure UI, deep-links back to mobile app at quiz4win://payment/return?id=<id>&status=<status>. Auto-redirects on success after 3s.
- app/nginx.conf: Added location = /pay/return -> /pay/return.html.
- docs/payments-api-reference.md: Endpoints, request/response schemas, error codes, env vars, redirect flow.
- Env vars required: REMITATION_ACCESS_KEY, REMITATION_SECRET_KEY, REMITATION_BASE_URL (optional, defaults to https://api.merchant.remitation.com/api/plugin/payment-gateway), APP_URL (used to build the redirect URL).

[2026-05-30] [A-01] [BUILD] Firebase Cloud Messaging push notifications — per-user and broadcast fan-out
- admin/src/lib/fcm.ts: new server-only helper. RS256 JWT signing via Web Crypto API, OAuth2 access-token cache, sendFcmToTokens() with bounded concurrency (20 parallel), invalid-token pruning. isFcmConfigured() guard. R-01: private key never logged.
- admin/src/lib/actions/users.ts sendNotification: fetches push_tokens for the target user, calls sendFcmToTokens, prunes invalid tokens from push_tokens table, inserts in-app inbox row (notifications, type=system, read=false, sent_via_push flag). Returns human-friendly message with delivered/failed counts.
- admin/src/lib/actions/broadcasts.ts sendBroadcast: resolved target user IDs once; inserts inbox rows in 500-row batches into notifications (fixing previous admin_notifications schema mismatch); fetches all push tokens for segment, FCM fan-out via sendFcmToTokens; updates notification_broadcasts.delivered_count / failed_count; prunes invalid tokens.
- docker-compose.yml: added FIREBASE_PROJECT_ID, FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY to admin container environment block.
- .env.docker.example: documented Firebase env vars with instructions for extracting from service-account JSON.
- admin/package.json: added server-only package (^0.0.1).
- .gitignore: already blocks configs/*.json (service-account JSON).

[2026-05-30] [A-01] [BUILD] Payments API v1 — Crypto extension & webhook
- supabase/migrations/20260528360000_payments_crypto.sql: Adds pay_address, pay_amount, pay_currency, qr_url, expires_at columns to payments table for crypto-specific gateway data.
- supabase/functions/payments/index.ts: Refactored — separate MasterCard and Crypto gateway helpers, added initiateCrypto (POST crypto-payment-gateway with REMITATION_CRYPTO_ACCESS_KEY / REMITATION_CRYPTO_SECRET_KEY), webhook handler (POST /payments/webhook/:method), enriched verify response with crypto fields (address/QR for pending display). applyOutcome shared by verify and webhook.
- app/public/pay/return.html: Added showCryptoPending() — renders QR image, pay address, coin amount, expiry when verify returns status=pending + method=crypto.
- Env vars added: REMITATION_CRYPTO_ACCESS_KEY, REMITATION_CRYPTO_SECRET_KEY, API_URL.

[2026-05-30] [A-03] [ARCH] Game Templates Engine v1 — module boundary approved
- New top-level module: Game Template Engine. Spans schema (game_templates), Edge Functions (admin-game-templates, admin-liveavatar), shared helper (_shared/rabbitmq.ts), Docker service (deploy/template-generator), admin UI (admin/src/app/(app)/templates/).
- Import direction: template-generator → Postgres RPCs only (no Edge Function dependency). admin-games → _shared/rabbitmq.ts (best-effort publish, no hard dep on broker).
- Mutual FK between games.template_id and game_templates.current_game_id is acceptable because both sides use ON DELETE SET NULL.

[2026-05-30] [A-01] [BUILD] Game Templates Engine v1 — schema + RPCs
- supabase/migrations/20260530100000_game_templates_v1.sql: game_templates table (mirrors games configurable fields + cron + AI presenter + question filters + lifecycle tracking), games.template_id FK, updated_at trigger, RLS enabled (service-role only).
- supabase/migrations/20260530100001_game_templates_rpcs.sql: match_cron_expression(expr text, ts timestamptz) — 5-field UTC cron matcher with ranges/steps/lists/named-DoW. generate_game_from_template(p_template_id, p_skip_overlap) SECURITY DEFINER — overlap guard, filter-based question selection, insert games row with status=upcoming + mode=live + scheduled_at = NOW() + start_buffer_seconds, populate game_questions, advance template tracking columns.
- supabase/migrations/20260530100002_game_templates_generator.sql: refined generator (LiveKit room name, AI/avatar field propagation onto games row).
- supabase/migrations/20260530100003_game_templates_cron_tick.sql: generate_games_from_active_templates() — looped tick RPC called by the Docker cron service every 60 s. Returns per-template status summary (generated|no_match|skipped_recent|overlap_skipped|error).

[2026-05-30] [A-01] [BUILD] Game Templates Engine v1 — admin Edge Function
- supabase/functions/admin-game-templates/index.ts: full CRUD (GET list/detail, POST create, PATCH update, DELETE soft-delete) plus PATCH activate / deactivate, POST generate-now, GET current-game / last-game / history, POST asset (icon/thumbnail/poster/host_avatar — multipart, ≤10 MB, S3 upload). JWT validation + admin role gate + admin_audit_log entries. Server-side invariant: ai_enabled = TRUE forces enable_streaming = TRUE.

[2026-05-30] [A-01] [BUILD] Game Templates Engine v1 — Docker template-generator
- deploy/template-generator/generator.ts: Deno loop that wakes every TEMPLATE_GEN_INTERVAL_MS (default 60 000 ms) and POSTs the tick RPC via PostgREST with the service-role key. Summarises per-tick result; logs "GENERATED template=… game=…" lines for ops visibility. Graceful SIGTERM/SIGINT handling via tini.
- deploy/template-generator/Dockerfile: denoland/deno:alpine-1.46.3 + tini, runs with --allow-env --allow-net only.
- docker-compose.yml: new "template-generator" service depends_on db-maintainer (healthy), inherits SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY / TZ=UTC.

[2026-05-30] [A-01] [BUILD] Game Templates Engine v1 — LiveAvatar + RabbitMQ
- supabase/functions/_shared/rabbitmq.ts: publish() via RabbitMQ Management HTTP API (Basic auth, persistent delivery mode 2). publishQuizShowStart() composes the quiz.show.start payload (gameId QUIZ:<uuid>, templateId, language, avatarId, voiceId, durationSeconds, totalPrize/currency, liveKit.url/roomName). Best-effort — returns { ok:false, error } instead of throwing; isConfigured() guard for caller short-circuit.
- supabase/functions/admin-liveavatar/index.ts: thin proxy over LIVEAVATAR_API_URL (avatars, public avatars, voices, voice preview, credits). Forwards X-Api-Key + Bearer auth. Returns 503 if not configured so the UI can fall back to free-form ID entry.
- supabase/functions/admin-games/index.ts: start endpoint now reads game.template_id + game_templates AI fields; if ai_enabled && rabbitmqConfigured() it publishes quiz.show.start and records the outcome in admin_audit_log details ({ ai: "ai_show_dispatched" | "ai_show_dispatch_failed:*" }). Failure never rolls the start transition back.
- docker-compose.yml: new env vars on the api service — LIVEAVATAR_API_URL/KEY, RABBITMQ_MGMT_URL/USER/PASSWORD/VHOST, MQ_COMMAND_EXCHANGE, MQ_COMMAND_QUEUE (default "quiz.show.start").

[2026-05-30] [A-01] [BUILD] Game Templates Engine v1 — admin UI
- admin/src/lib/actions/templates.ts: server actions (createTemplate, updateTemplate, setTemplateActive, deleteTemplate, generateNow) with Zod validation matching the table CHECK constraints (mode/language/difficulty enums, hex colours, AI duration 60..1800).
- admin/src/app/(app)/templates/page.tsx: list page with status tabs (All/Active/Inactive), cron preview, AI badge, total generated, last run, pagination.
- admin/src/app/(app)/templates/create-template-dialog.tsx: 4-tab dialog (Basic / Schedule / Questions / AI Presenter) with cron presets, filter-based question fields, AI avatar/voice ID inputs (disabled when ai_enabled=false).
- admin/src/app/(app)/templates/[id]/page.tsx: detail page — schedule/config card, AI presenter card, current/last game cards, history table (linked to /games/<id>).
- admin/src/app/(app)/templates/[id]/template-actions.tsx: client component for Generate-now / Activate-Deactivate / Delete.
- admin/src/lib/nav.ts: new "Game Templates" entry (CalendarClock icon) in Content section.

[2026-05-30] [A-01] [TASK] Game Templates Engine v1 — operational notes
- Cron expressions are UTC only. Admin UI hints this on the schedule tab.
- Generated games always start as status=upcoming, mode=live (R-D-LiveOnly invariant for templated games).
- Question selection is filter-based: question_category / question_difficulty / question_language on the template are matched against the questions table at generation time. NULL means "any value".
- Overlap guard: a template with a non-completed current_game_id will not generate another game unless POST /generate-now { skip_overlap: true }.

[2026-05-30] [A-01] [BUILD] Game Template Engine — test suite + UI completion
  • admin/src/app/(app)/templates/[id]/edit/page.tsx — new edit page (RSC)
  • admin/src/app/(app)/templates/[id]/edit/edit-template-form.tsx — full edit form (basic info, schedule, gameplay, question filters, AI presenter)
  • admin/src/app/(app)/templates/[id]/page.tsx — added Edit button (Pencil icon → /templates/:id/edit)
  • .env.docker.example — added LIVEAVATAR_API_URL/KEY, RABBITMQ_MGMT_URL/USER/PASSWORD/VHOST, MQ_COMMAND_EXCHANGE/QUEUE, TEMPLATE_GEN_INTERVAL_MS
  • supabase/functions/_shared/rabbitmq_test.ts — Deno unit tests: isConfigured, publish (ok/error/network-fail), publishQuizShowStart payload composition
  • supabase/migrations/tests/game_templates_test.sql — SQL tests: match_cron_expression (wildcard/step/range/list/DOW/malformed/NULL), generate_game_from_template (happy path, overlap guard, skip_overlap, AI streaming)
  • supabase/migrations/tests/template_generator_integration_test.sql — Integration test: cron-tick RPC generates game for matching template, skipped_recent on second tick, no_match for impossible cron, inactive template ignored
  • admin/playwright.config.ts — Playwright config (auth setup project + chromium runner)
  • admin/e2e/auth.setup.ts — login helper saving session to .auth/admin.json
  • admin/e2e/templates.spec.ts — 7-step smoke test: list/create/detail/edit/  • admin/e2e/templates.spec.ts — 7-step smoke test: list/create/detail/edit/  • admin/e2e/templates.spec.ts — 7-step smoke test: list/createtalled)

[2026-05-30] [A-01] [BUILD] LiveAvatar catalog proxied through backend; provider key removed from admin
  • supabase/functions/_shared/auth.ts — added validateAdminSessionToken() that re-hashes the panel's q4w_admin_session cookie token against admin_sessions.session_token_hash and resolves to an active admin_users row.
  • supabase/functions/admin-liveavatar/index.ts — accepts X-Admin-Session-Token (preferred) with Authorization: Bearer JWT as fallback; same allowed roles (super_admin/admin/moderator).
  • admin/src/lib/actions/liveavatar.ts — server actions no longer call api.liveavatar.com directly; they fetch ${API_URL}/admin/liveavatar/avatars(+/public), /voices, /voices/:id/preview and forward the admin session cookie token via X-Admin-Session-Token.
  • docker-compose.yml — dropped LIVEAVATAR_API_URL/KEY from admin service (now only the api container needs them); added API_URL with internal default http://api:8000.
  • .env.docker.example — documented API_URL near the top.
  • Rule compliance: R-01 (provider key never leaves api container), R-03/R-04 (backend re-validates admin session before contacting provider).


[2026-05-30] [A-01] [FIX] admin-liveavatar: align with provider's real /v1 paths and response shape
  • supabase/functions/admin-liveavatar/index.ts — paths are now /v1/avatars, /v1/avatars/public, /v1/avatars/:id, /v1/voices, /v1/voices/:id, /v1/voices/:id/preview, /v1/users/me/credits (LiveAvatar OpenAPI: docs.liveavatar.com/openapi.json). Auth header normalised to single X-API-KEY (dropped duplicate Authorization: Bearer).
  • admin/src/lib/actions/liveavatar.ts — provider envelope is { code, data: { count, results: [...] }, message }, not { data: { avatars/voices: [...] } }. Added mapAvatar() (id→avatar_id, name→avatar_name, preview_url→preview_image_url/preview_video_url heuristic) and mapVoice() (id→voice_id). fetchVoices now defaults to merging public+private (capped at 100/page per provider). LiveAvatarVoice type slimmed (dropped unused support_pause/emotion_support).
  • Root cause: previous build returned upstream 404 for every path; admin UI silently fell back to empty grid/list. Fixed by matching the provider's documented OpenAPI exactly.


[2026-05-28] [A-01] [BUILD] Real-time quiz Phase 1: schema + atomic RPCs + game-session edge function.
  - Migrations 20260528360000 (schema columns + stable option_ids backfill) and 20260528360001 (join_game_session, start_game_question, close_game_question, submit_answer_v2 SECURITY DEFINER RPCs with FOR UPDATE locking).
  - submit_answer_v2 implements the §9.3 atomic validator: idempotency (attempt_id), time window with grace_period_ms, duplicate check, wrong/lives counters, elimination, full §16 audit row.
  - New edge function game-session exposes POST/GET join/state/answer/advance/close. Lifecycle routes (advance/close) gated by requireAdminRole.
  - Legacy /games/* routes left untouched for backwards-compat.
  - Redis/LiveKit/RabbitMQ/LLM deferred to Phase 2+ (see docs/wingobingo_quiz_backend_phase1_implementation.md).

[2026-05-31] [A-01] [BUILD] Real-time Quiz Phase 2: exact-technology implementation per docs/wingobingo_quiz_backend_architecture_complete.md

Architecture (§2.1 compliant — Redis source of truth, not Postgres FOR UPDATE):
- supabase/migrations/20260528360001: replaced wrong Postgres RPCs with Redis reference columns on games (redis_namespace, redis_cluster_id, redis_started_at, redis_expires_at per §3.8)
- supabase/functions/_shared/redis.ts: Redis client singleton (npm:redis@4)
- supabase/functions/_shared/redis_keys.ts: §6 key schema builders (game/question/user-level)
- supabase/functions/_shared/redis_scripts.ts: atomic Lua scripts — JOIN_GAME (§7), SUBMIT_ANSWER (§9.3), PREPARE_QUESTION (§8.2), CLOSE_QUESTION (§18.5)
- supabase/functions/_shared/livekit.ts: LiveKit broadcaster — HS256 JWT signing + RoomService.SendData REST (§3.5)
- supabase/functions/_shared/llm.ts: OpenAI gpt-4o-mini question generator — multilingual with stable option IDs per §5.5
- supabase/functions/game-session/index.ts: rewritten — /join uses Redis Lua + DB upsert + LiveKit token; /state reads Redis HGETALL; /answer runs atomic Lua + publishes ANSWER_PERSIST_REQUESTED to RabbitMQ (§9.5)
- deploy/game-orchestrator/orchestrator.ts: new AMQP consumer (npm:amqplib) — StartGame→Redis init→question loop; CloseQuestion→no-answer processing; PersistAnswer→DB insert; FinalizeGame; §15.2 recovery
- deploy/game-orchestrator/Dockerfile: same Deno pattern as template-generator
- deploy/template-generator/generator.ts: extended with §3.1 Game Scheduler — detects upcoming games, flips status to open, publishes StartGame to RabbitMQ
- docker-compose.yml: added redis:7-alpine service + game-orchestrator service; api depends on redis
- .env.docker.example: added REDIS_PASSWORD, REDIS_URL, OPENAI_API_KEY, OPENAI_MODEL, MQ_ORCHESTRATOR_QUEUE

[2026-05-31] [A-01] [BUILD] Presenter command handling — implemented PrepareQuestion, StartQuestion, CloseQuestion, AdvanceQuestion in deploy/game-orchestrator/orchestrator.ts. Added STAGE_Q and ACTIVATE_Q Lua scripts for two-phase question flow (staged→active). Added broadcastToIdentity() for private QUESTION_PREPARED events. Added PresenterGameConfig, presenterGames map, stagedQuestions map, module-level prefillQueue(). Updated closeQuestion() to skip auto-advance in presenter mode. Updated handleStartGame() to set up presenter config + store presenterIdentity in Redis. Updated recoverRunningGames() to rebuild presenter state from Redis on restart. Updated docs/game-commands-protocol.md — all six commands marked implemented, presenter identity convention documented.

[2026-05-31] [A-01] [BUILD] Template-generator start reminders — T-60min / T-10min / T-1min FCM pushes:
- supabase/migrations/20260531180000_games_start_reminders.sql: added reminder_60m_sent_at / reminder_10m_sent_at / reminder_1m_sent_at columns + 3 partial indexes for pending-only scans; new SECURITY DEFINER RPC get_game_reminder_push_tokens() returning opted-in tokens (game_reminders default TRUE).
- deploy/template-generator/fcm.ts: new Deno-native FCM v1 sender (mirrors admin/src/lib/fcm.ts) — service-account JSON, RS256 JWT via WebCrypto, OAuth2 token cache, concurrency=20 fan-out, invalid-token detection.
- deploy/template-generator/generator.ts: added reminderTick() that runs each tick, scans 3 windows, atomic claim via PATCH (column IS NULL), per-game FCM fan-out, bulk insert into public.notifications (type='show_reminder', sent_via_push=true) carrying game_id/title/prize_pool/accent_color/glow_color/gradient_colors/minutes_until_start/scheduled_at, then deletes invalid tokens via PostgREST. Token list cached 60s. fullTick() now awaits reminderTick() in parallel with tick/schedulerTick.
- deploy/template-generator/Dockerfile: copies fcm.ts and adds --allow-read; documents FCM_SERVICE_ACCOUNT_PATH env var.
- docker-compose.yml: template-generator now mounts ./configs:/app/configs:ro (shared with admin) and exposes FCM_SERVICE_ACCOUNT_PATH (default points to existing firebase-adminsdk JSON).
- Real-time strategy preserved: still push-based; reminders are minute-granular by design, not polling for state.
- R-01 compliant: service-account loaded but never logged.
[2026-05-31] [A-01] [BUILD] Redesigned admin template detail page (/templates/[id]) in ERP style — KPI strip (status/mode/generated/last-run/prize/fee), 3-column grid with Schedule + Activity sidebar, Gameplay + Question Filters, AI Presenter + Branding, Identifiers full-width, Generated games table. Reused DetailRow pattern from /finance/payments/[id]. No data-shape or action changes; pure UI density and IA improvement.
[2026-05-31] [A-01] [BUILD] Templates ERP polish, round 2 — (1) Added admin/src/lib/cron.ts wrapping cron-parser (next runs, validation). (2) Detail page now shows a Next-run KPI + an "Upcoming runs (UTC)" chip row in Schedule. (3) Edit form rewritten as sticky save bar + 4-tab layout (Basic / Schedule / Gameplay / AI Presenter) with live cron next-3-runs preview and inline validity indicator. (4) Templates list: KPI strip (Templates / Active / Games generated / AI-enabled), name search, mode + AI filter chips, new "Next run" column, "Clear filters" link, ERP-style chip filters. Dependency: cron-parser ^5.5.0.
[2026-05-31] [A-01] [FIX] deploy/template-generator/fcm.ts: surface FCM error.status code (e.g. INVALID_ARGUMENT, SENDER_ID_MISMATCH, UNREGISTERED) in warn log without ever logging the device token (R-01 compliant). Log line now reads: [fcm] send failed status=<http> code=<FCM_CODE> remove=<bool>.
[2026-05-31] [A-01] [BUILD] Game lifecycle alignment with Template spec
- supabase/migrations/20260531200000_game_lifecycle.sql:
  • join_game now allows status IN ('upcoming','open') AND scheduled_at > NOW().
    Time is the registration gate (raises 'registration_closed' after start).
  • generate_game_from_template overlap now blocks only on current_game_id
    status = 'upcoming'. Dropped 'open','live','paused' from the block-list,
    enabling "one running + one upcoming" per spec.
    ('paused' was never a valid games.status — also dead-code removed.)
- supabase/functions/games/index.ts: matching fast-path check on POST /games/:id/join.
- Migration applied to live DB (BEGIN/CREATE/CREATE/REVOKE/GRANT/COMMIT).
- Pre-existing bugs flagged but NOT touched in this commit (require coordinated
  orchestrator change, awaiting sign-off):
  • orchestrator.ts:579 writes status='finished', finished_at — invalid (enum is
    'completed', column is ended_at).
  • No scheduled job to finalize games whose duration_minutes has elapsed
    when the orchestrator misses the GAME_ENDED path.
[2026-05-31] [A-01] [BUILD] Game lifecycle, round 2 — orchestrator finalize fix + watchdog + event-driven next-game generation
- deploy/game-orchestrator/orchestrator.ts:579 — fix invalid finalize: status:'finished'/finished_at -> status:'completed'/ended_at (matches schema CHECK constraint and actual column name). Every game finalize was failing silently before this.
- supabase/migrations/20260531210000_force_finalize_stuck_games.sql — new force_finalize_stuck_games(grace_minutes INT DEFAULT 5) RPC. Selects games with status='live' AND started_at + template.duration_minutes + grace_minutes <= NOW(), atomically flips to status='completed', ended_at=NOW(). Race-guarded by WHERE g.status='live' in the UPDATE. Returns one row per game flipped (game_id, template_id, started_at, expected_end) for caller logging. Prize distribution NOT touched — lifecycle recovery only.
- deploy/template-generator/generator.ts:
  - watchdogTick(): added to fullTick(); calls force_finalize_stuck_games via PostgREST RPC every interval (default 60s). Grace via WATCHDOG_GRACE_MINUTES env var (default 5).
  - schedulerTick(): after flipping a game upcoming->open and publishing StartGame, now also calls generate_game_from_template(template_id) so the next upcoming game is created at the moment the current one starts — event-driven, not cron-driven (spec: "create next game when current Upcoming Game changes to Running"). Made possible by the relaxed overlap rule in 20260531200000_game_lifecycle.sql.
- Migration applied to live DB. Container rebuild required: docker compose up -d --build --force-recreate template-generator game-orchestrator

[2026-05-31] [A-01] [BUILD] Prize distribution pipeline + admin visibility + notification sweep
- supabase/migrations/20260531220000_distribute_prizes.sql:
  - ALTER public.games ADD COLUMN prizes_distributed_at TIMESTAMPTZ, prize_notifications_sent_at TIMESTAMPTZ. Partial indexes for both ("status='completed' AND prizes_distributed_at IS NULL", and "prizes_distributed_at IS NOT NULL AND prize_notifications_sent_at IS NULL") so the orchestrator + generator sweeps stay O(k).
  - compute_game_ranks(p_game_id) — DENSE_RANK by score DESC, correct_answers DESC, joined_at ASC; updates game_participants.rank in place.
  - get_game_winners(p_game_id) — SECURITY DEFINER view function returning (rank, user_id, full_name, prize_amount, currency) for admin UI.
  - get_winner_push_tokens(p_game_id) — returns (user_id, full_name, prize_amount, currency, device_token) for FCM, joining push_tokens + notification_preferences (respects user opt-out).
  - distribute_prizes(p_game_id UUID) — main RPC. SECURITY DEFINER. Locks games row FOR UPDATE; short-circuits when prizes_distributed_at IS NOT NULL (returns already_distributed:true); calls compute_game_ranks; parses prize_breakdown JSONB tiers (supports {rank,amount}, {rank,percent}, {rank_from,rank_to,amount|percent}); falls back to winner-takes-all when breakdown is NULL/empty; for each winner inlines: UPDATE profiles SET wallet_balance=wallet_balance+amount, total_prizes_won=total_prizes_won+amount; INSERT into transactions (type='prize', status='completed', game_id, user_id, amount, currency, description); UPDATE game_participants SET prize_earned=amount. Stamps games.prizes_distributed_at + total_winners + total_prizes_paid. Returns JSON {distributed, already_distributed, total_winners, total_paid, currency}.
- deploy/game-orchestrator/orchestrator.ts: after broadcasting GAME_ENDED in finalizeGame(), now calls admin.rpc('distribute_prizes', { p_game_id: gameId }) and logs the result. Errors are non-fatal — the template-generator safety-net sweep picks up any missed game.
- deploy/template-generator/generator.ts: added prizeNotificationTick() to fullTick(). Selects games with prizes_distributed_at IS NOT NULL AND prize_notifications_sent_at IS NULL, calls get_winner_push_tokens, sends FCM via sendFcmToTokens (existing helper), stamps prize_notifications_sent_at. Per-game error-isolated so one bad row does not stop the sweep. Tokens never logged (R-01).
- supabase/functions/admin-games/index.ts:
  - GET /admin/games/:id/result: SELECT updated to use prize_earned (was non-existent prize_amount/prize_credited); also surfaces prizes_distributed_at, prize_notifications_sent_at, total_winners.
  - GET /admin/games/:id/export (CSV): column rename prize_amount->prize_earned, profile join name->full_name.
  - POST /admin/games/:id/end: status now 'completed' (was invalid 'ended'), now calls distribute_prizes RPC and logs distribution result in admin_audit_log.details.
- admin/src/app/(app)/games/[id]/page.tsx: added "Prize distribution" card (status / total_winners / prize_pool / distributed_at / notifications_sent_at) and Winners table (rank, user, prize_amount, currency) sourced from get_game_winners RPC.
- admin/src/app/(app)/users/[id]/page.tsx: added "Wins" card listing the games where this user earned a prize (game title, rank, prize_earned, currency, ended_at). Filtered by prize_earned > 0 on game_participants joined with games.
- Verified end-to-end on live DB inside a ROLLBACK transaction with 4 synthetic users + a mixed prize_breakdown ({rank:1,amount:50}, {rank:2,percent:30}, {rank_from:3,rank_to:4,amount:5}, pool=100 USD): ranks/prize_earned/wallet_balance/total_prizes_won all correct, 4 transactions inserted (type='prize', status='completed'), 2nd call idempotent, total_winners=4, prizes_distributed_at set. No DB pollution after ROLLBACK.
- Migration applied to live DB. Container rebuild required: docker compose up -d --build --force-recreate template-generator game-orchestrator

[2026-06-03] [A-01] [BUILD] At-least-once StartGame (fixes games stranded in 'open' with 0:00 countdown). schedulerTick still fires StartGame once on upcoming→open; added startRedriveTick() to template-generator that re-publishes StartGame for games stuck status='open' AND started_at IS NULL AND scheduled_at in [now-grace, now] (grace = RULES_AUDIT_ORPHAN_GRACE_MINUTES, default 15m). Re-drive (no grace) wins the race against rules-audit §3 orphan-cancel, so a missed game is started rather than killed. Hardened orchestrator handleStartGame with an idempotency guard: no-op when status has already advanced past 'open' (live/completed/cancelled), preventing a duplicate question loop / GAME_STARTED if a re-drive races a slow first attempt. Files: deploy/template-generator/generator.ts, deploy/game-orchestrator/orchestrator.ts.

[2026-06-03] [A-01] [FIX] Recover games stranded in status='live' with no question broadcast (frozen 0:00). Root cause: handleStartGame commits status='live'+started_at BEFORE question 0 is confirmed broadcast, so any failure after the flip (prepare_question Lua non-ok, or a throw in the loop) leaves the game live-at-zero; recoverRunningGames only re-armed a timer when currentQuestionStatus='active', so a never-started live game was ignored on restart, and the idempotency guard (status!=open) blocked any re-drive. Fixes: (1) orchestrator handleStartGame guard is now Redis-aware — completed/cancelled are terminal, but a 'live' game is only skipped when Redis currentQuestionStatus is present; live-but-never-started falls through and (re)starts the loop. (2) prepare_question non-ok now throws (was a silent return) so the failure surfaces and is re-driven. (3) recoverRunningGames auto-mode adds a branch: live + currentQuestionIndex absent → startQuestionLoop({questionIndex:0}). (4) startRedriveTick (template-:
docker compose logs game-orchestrator | grep "5677c39b-4398-4778-bf8d-7dd5359c09c3" | tail -n 50
:
docker exec -it backend-redis-1 redis-cli HGETALL q4w:game:5677c39b-4398-4778-bf8d-7dd5359c09c3:state
:
docker ps --format "{{.Names}}"
:
docker ps
:
docker compose ps
:
docker container ls -a
:
ls -F
:
pwd
:
echo "hello"
:
head -n 5 package.json
:
test -f .env && echo ".env: present" || echo ".env: MISSING"; grep -c -E "^RABBITMQ_URL=" .env 2>/dev/null | sed 's/^/RABBITMQ_URL lines: /'; grep -E "^(MQ_COMMAND_EXCHANGE|MQ_ORCHESTRATOR_QUEUE|RABBITMQ_VHOST|DEBUG_LOG)=" .env 2>/dev/null | sed -E 's/=.*/=<set>/'
:
if [ -f .env ]; then echo "env_present=yes"; else echo "env_present=no"; fi
grep -q "^RABBITMQ_URL=" .env && echo "rabbitmq_url=set" || echo "rabbitmq_url=missing"
grep -qE "^RABBITMQ_URL=amqps" .env && echo "scheme=amqps_external" || echo "scheme=other"
'
"
:
bash -c 'test -f .env && echo env_present=yes || echo env_present=no; grep -q "^RABBITMQ_URL=" .env && echo rabbitmq_url=set || echo rabbitmq_url=missing; grep -qE "^RABBITMQ_URL=amqps" .env && echo scheme=amqps_external || echo scheme=other'



:
printf 'env_present=%s\n' "$([ -f .env ] && echo yes || echo no)"
:
echo hello123


[2026-06-05] [A-01] [BUILD] **Reliable LiveKit delivery + full game-state snapshot on (re)connect — fixes "joined after GAME_STARTED / QUESTION_PREPARED never arrived".** Root cause: (a) the orchestrator broadcast both via LiveKit `kind:1` (LOSSY), so a one-shot event (`GAME_STARTED`, and any other) could be silently dropped for a participant whose data channel was still settling; (b) even with reliable delivery, a client that connects AFTER an event was emitted can never receive that past event — there was no catch-up path carrying the static `GAME_STARTED` fields. Fixes: (1) `deploy/game-orchestrator/orchestrator.ts` — `broadcast()` and `broadcastToIdentity()` now send `kind:0` (RELIABLE) so game-state events are not dropped. (2) `handleStartGame` now persists the static `GAME_STARTED` fields into the Redis game hash (`languages` as JSON, `category`, `questionsCount`, `prizePoolCurrency`, `title`, `pregameDurationMs`, `firstQuestionStartsAt`) alongside the existing `gameMode`/`prizePool` so the snapshot is reconstructable from one authoritative source; `prize_pool_currency` added to the `handleStartGame` and `recoverRunningGames` DB selects; `recoverRunningGames` back-fills the same static fields (keyed on a missing `languages`) for games started before this change or after the 24h TTL. (3) `supabase/functions/game-session/index.ts` — new `buildSnapshot(redis, gameId, userId, lang)` helper assembles a client-safe `snapshot`: `game` (all GAME_STARTED fields + `projectedPrizePerSurvivor`), `stats` (participant/spectator/eliminated/activeSurvivor counts), `currentQuestion` (localized, `correctOptionId` stripped per §13.4, `null` between questions), and `me` (userStatus/wrong/correct/lives/eliminated/canSubmitAnswer). `projectedPrizePerSurvivor` mirrors the orchestrator `QUESTION_CLOSED` rule (null when no pool, 0 when pool but no survivors). The snapshot is returned by BOTH `POST /game-session/:id/join` (new `snapshot` field; all prior top-level fields kept for back-compat) and `GET /game-session/:id/state` (added `snapshot`; existing flat fields kept; the inner `lang` lookup hoisted to a single declaration). (4) `docs/livekit-events.md` — documented RELIABLE delivery, the "events are real-time only / use the snapshot to catch up" contract, and a full `snapshot` example for `/join` + `/state`. No DB migration (all `games` columns already exist). R-11 frozen surfaces (auth, Redis wiring, `.env`, `docker-compose.yml`) untouched; R-12 honoured (no manual SQL). IDE Deno LSP reports no diagnostics. Deploy: `docker compose up -d --build --force-recreate game-orchestrator api`.


[2026-06-05] [A-01] [BUILD] **Game logic and event-timing reference — `docs/game-logic.md`.** Created a single authoritative reference document for the backend game flow covering: (1) Game status lifecycle (`upcoming → open → live → completed/cancelled`). (2) Run modes (`auto` vs `presenter`) and which events each mode emits. (3) Auto-mode full event timeline with exact ms-level timing formulas: `firstQuestionStartsAt = T_start + PREGAME_WARMUP_MS`; close timer fires at `timeLimitSeconds×1000 + gracePeriodMs + 50 ms` after `startsAt`; inter-question pause = 3 000 ms; `GAME_ENDED → GAME_RESULT` sequence. (4) Presenter-mode command flow (`PrepareQuestion → QUESTION_PREPARED → StartQuestion → QUESTION_STARTED → CloseQuestion / auto-close → QUESTION_CLOSED`). (5) Answer window submission rules (all checks inside atomic `SUBMIT_ANSWER_SCRIPT` Lua) and scoring formula (`max(10, 100 − floor(responseTimeMs / 100))`). (6) Wrong-answer and elimination rules (INV-14): life-based vs count-based, all four trigger paths (wrong submission, no-answer sweep, ghost-participant sweep, late-join demotion), and exact Redis + DB + LiveKit side-effects. (7) Ghost-participant sweep algorithm (§10.3). (8) Late-join logic for `first_question_only` vs `any_time` vs `closed` policies including the ghost-sweep pre-charged path (ARGV[6]/ARGV[7]). (9) State recovery: orchestrator restart (`recoverRunningGames`) + client snapshot (`POST /join` / `GET /state`). (10) Prize distribution timing: `GAME_ENDED → distribute_prizes → GAME_RESULT`; `projectedPrizePerSurvivor` rules. (11) Redis key lifecycle and TTLs: game hash 24 h (1 h post-game), question hash 24 h, idempotency cache 300 s. (12) Full configuration reference table (env vars + DB columns). (13) Clock-synchronization recipe (`clockOffset = serverTime − Date.now()`). Sourced exhaustively from `deploy/game-orchestrator/orchestrator.ts` and `supabase/functions/_shared/redis_scripts.ts` — no values guessed. AGENTS.md onboarding checklist updated with step 3a pointing to this file. `project-brain/Domain_Knowledge.md` §2.3 annotated with a pointer. No code changed; pure documentation.



[2026-06-07] [A-01] [BUILD] **Editable LLM prompt/model templates (3-tier cascade) + Redis-backed per-game no-repeat dedup.** Adds an admin-managed store for the OpenAI question-generation prompt/model and moves within-game duplicate prevention from in-memory maps into Redis. (1) Migration `20260607120000_llm_prompt_templates.sql` — new `public.llm_prompt_templates` table (`name, description, provider['openai'], model, system_prompt, temperature[0–2], max_tokens[256–8192], is_active, created_by/updated_by, timestamps, deleted_at`), partial-unique index enforcing exactly one active default, `updated_at` trigger, RLS enabled (service-role only), nullable `llm_template_id` FK added to `game_templates` and `games` (ON DELETE SET NULL), `set_active_llm_template(p_id)` SECURITY DEFINER RPC, and a seed of the built-in default mirroring the orchestrator guidance. No API key stored (R-01). (2) Migration `20260607120001_generate_copies_llm_template.sql` — re-installs the canonical `generate_game_from_template` body (overlap=('upcoming','open'); cron-aligned scheduled_at; cooldown-aware picker; target_languages copy) and additionally copies `llm_template_id` template→game. (3) `deploy/game-orchestrator/orchestrator.ts` — new `LlmConfig` interface + `resolveLlmConfig(g)` cascade (game.llm_template_id → game_templates.llm_template_id → global active → hardcoded `DEFAULT_GEN_GUIDANCE`/OPENAI_MODEL); `generateQuestion` now accepts `systemPrompt`/`model`/`maxTokens` and the guidance half is overridable while the JSON-output schema is ALWAYS appended (edited prompts can't break parsing); cascade resolved once at game start and on recovery, threaded through `prefillQueue`/`resolveUniqueQuestion`/`startQuestionLoop`/`PresenterGameConfig`; `handleStartGame` and `recoverRunningGames` DB selects extended with `template_id,llm_template_id`. Per-game dedup now uses Redis sets `q4w:game:{id}:asked_ids` and `:asked_hashes` (helpers `getAskedTexts`/`isAskedId`/`markAsked`, 24 h TTL → 1 h on game end) replacing the `askedQuestionIds`/`askedQuestionTexts` maps; best-effort (Redis outage degrades to platform-cooldown-only dedup). (4) Admin panel — new `/llm-templates` page (list + create/edit dialog + set-default/delete row actions) via `admin/src/lib/actions/llm-templates.ts` (create/update/delete + `set_active_llm_template` RPC, audit-logged) and nav entry (super_admin/admin). (5) `docs/game-logic.md` §14 documents the cascade + Redis dedup. R-12: both migrations applied by db-maintainer only (NOT run locally). R-05/A-05: game-logic change — financial columns untouched. IDE reports no diagnostics (orchestrator Deno LSP + admin TS). Deploy after migration: `docker compose up -d --force-recreate db-maintainer` then `docker compose up -d --build --force-recreate game-orchestrator` and rebuild the admin app.

[2026-06-07] [A-01] [FIX] **Forced native multi-language generation in LLM contract.** Updated the non-negotiable prompt contract to explicitly command the model to generate full question and option text for every requested language in `targetLanguages` within the same response. Added a critical instruction forbidding English placeholders in non-English payloads to ensure native localized content is produced in one pass.

[2026-06-07] [A-01] [FIX] **Per-game pregame warmup from start_buffer_seconds.** Replaced the fixed global `PREGAME_WARMUP_MS` (120 s) with the per-game `start_buffer_seconds` column. (1) Migration `20260607150000_games_start_buffer_seconds.sql` — adds `start_buffer_seconds INTEGER NOT NULL DEFAULT 120` to `public.games`, back-fills from linked templates, reinstalls `generate_game_from_template()` to copy `start_buffer_seconds` from template→game (title verbatim + `llm_template_id` copy preserved). (2) `orchestrator.ts` — removed dead `PREGAME_WARMUP_MS` constant; `handleStartGame` reads `start_buffer_seconds` from the DB row and computes `pregameWarmupMs = start_buffer_seconds * 1000` (fallback 120 s when NULL); `recoverRunningGames` does the same for all live games. Both `GAME_STARTED` broadcasts and Redis `pregameDurationMs` field now reflect the per-game value. R-12: migration applied by db-maintainer only.

[2026-06-07] [A-01] [BUILD] **Upgrade LLM to gpt-4o, temperature 0.4, stronger system prompt.** Confirmed by user. (1) `orchestrator.ts` — default model changed from `gpt-4o-mini` to `gpt-4o`; default temperature from `0.8` to `0.4`; `DEFAULT_GEN_GUIDANCE` rewritten into a world-class-author checklist (6-item pre-output quality gate: fact-check, anchored & self-contained, one right answer, tempting distractors, clear & concise, interesting & fresh). (2) Migration `20260607140000_upgrade_default_llm_template.sql` — UPDATEs all active `llm_prompt_templates` rows to model=`gpt-4o`, temperature=0.40, and the new system prompt so the DB-stored template matches the code default. R-12: applied by db-maintainer only. Deploy: `docker compose up -d --force-recreate db-maintainer` then `docker compose up -d --build --force-recreate game-orchestrator`.

[2026-06-07] [A-01] [FIX] **game title verbatim — no suffix appended when generating from template.** Migration `20260607120001` accidentally reintroduced the `_MMDD_HH24MI` timestamp suffix (overwriting the fix in `20260607110000`). New migration `20260607130000_fix_game_title_verbatim.sql` reinstalls `generate_game_from_template()` with `title = v_tpl.name` and removes the unused `v_title` variable. Full `llm_template_id` copy behaviour from `20260607120001` is preserved. R-12: applied by db-maintainer only.

[2026-06-07] [A-01] [FIX] **Levelled up question-generation prompt quality.** Rewrote `DEFAULT_GEN_GUIDANCE` into an expert-author QUALITY BAR (meaningful/self-contained questions, exactly one verifiable answer, plausible same-type distractors, concise wording, freshness) to stop nonsensical/filler questions. Strengthened the non-negotiable contract with: strict `category` confinement (no topic drift); precise `difficulty` calibration (easy=common knowledge, medium=real familiarity, hard=expert detail); a stronger NO-REPEATS rule (genuinely different fact from every avoid-list entry, not a reworded/same-answer variant); and detailed TRANSLATION QUALITY rules for every `targetLanguages` code (native fluent phrasing not machine word-for-word, correct script/direction, meaning preserved so the same option stays correct, proper-noun/transliteration handling, no baseLanguage placeholders, identical option IDs). `docs/game-logic.md` updated. Game-logic-only change — no schema/financial impact. Deploy: `docker compose up -d --build --force-recreate game-orchestrator`.

[2026-06-08] [A-01] [FIX] **Restore `distribute_prizes` rich result payload + `result_summary` persistence (GAME_RESULT showed totalPrize/prizePool/sharePerWinner=0, winnerUserIds/winners=[]).** Root cause: the three-bucket migration `20260607000000_three_bucket_financial_model.sql` correctly moved crediting to `earnings_balance` + `score_events` (INV-15) but regressed the RPC contract — it returned only `{distributed, already_distributed, total_winners, total_paid, currency}` and stopped writing `games.result_summary`. The orchestrator's `finalizeGame` GAME_RESULT broadcast reads `out.total_prize/prize_pool/share_per_winner/winner_user_ids/winners/distributed_at`, and both public/customer `GET /…/result` endpoints read `games.result_summary` — all absent, so they defaulted to `0`/`[]`/`null` even though winners were actually paid (`total_winners=1` was the only field that still matched). Fix: migration `20260608100000_fix_distribute_prizes_result_payload.sql` redefines `distribute_prizes` to KEEP three-bucket crediting (earnings_balance + total_prizes_won + score_events for game_winner/top3/prize_winner/participant) AND restore the full return shape (`total_prize`, `prize_pool`, `share_per_winner`, `winner_user_ids` ordered by rank, `winners` with per-winner rank+prize_amount, `distributed_at`) plus persistence of `games.result_summary`. Idempotent-replay path back-fills `result_summary` from `game_participants` for games distributed under the regressed version (e.g. `b14f75cd-0387-4403-9250-064e66178d6d`) so a re-invocation returns/persists correct numbers. R-02/R-05/R-09/INV-15 preserved; A-05 domain (prize/result contract). R-12: migration applied by db-maintainer only — `docker compose up -d --force-recreate db-maintainer`.


[2026-06-08] [A-01] [BUILD] **Added 'loser' sound usage.** (1) `admin/src/lib/sound-usages.ts` — added `"loser"` to `SoundUsage` type and `SOUND_USAGES` constants (label: "Loser") so it appears in the admin panel's usage dropdown. (2) `supabase/functions/admin-sounds/index.ts` — added `"loser"` to the allowed usage validation array in the upload/update Edge Function. (3) Migration `20260608200000_app_sounds_add_loser_usage.sql` — updates the `app_sounds_usage_check` constraint on the `public.app_sounds` table to include the new slot. Deployment: apply migration via `db-maintainer`, rebuild `admin` and `api` containers.

[2026-06-08] [A-01] [BUILD] **Producer/consumer question buffer (buffer depth 5 → 2).** Reworked the pre-resolved question buffer in `deploy/game-orchestrator/orchestrator.ts` from the fire-and-forget `prefillQueue` model into an explicit single-producer/single-consumer scheme per game. (1) `prefillQueue` removed; replaced by `produceOneQuestion` (generate → claim → dedup, returns one `ResolvedQuestion`), `startProducer` (idempotent per-game background loop that keeps `questionQueue` filled to `QUESTION_BUFFER_TARGET`, idling `PRODUCER_IDLE_MS` while full and backing off `PRODUCER_BACKOFF_MS` on a gen error), `stopProducer`, and `takeQuestion` (FIFO consumer that pops immediately when the buffer is non-empty, else waits for the producer up to `QUESTION_WAIT_TIMEOUT_MS`, returning null only on timeout → finalize). (2) `QUESTION_BUFFER_TARGET` default lowered 5 → 2; added env knobs `PRODUCER_IDLE_MS` (250), `PRODUCER_BACKOFF_MS` (750), `QUESTION_POLL_MS` (150), `QUESTION_WAIT_TIMEOUT_MS` (60 000). (3) Lifecycle wiring: `startProducer` replaces `void prefillQueue` at auto `handleStartGame`, presenter `handleStartGame`, both `recoverRunningGames` branches, the top of `startQuestionLoop`, and the top of `handlePrepareQuestion`; `stopProducer` added to `finalizeGame`. (4) Consumer change — `advanceQuestion` (auto) and `handlePrepareQuestion` (presenter) now call `takeQuestion` and only need ONE ready question, so a game starts the instant the first question is produced instead of blocking until the full buffer (TARGET) was generated; the old trailing `void prefill()` calls are gone (the always-on producer refills). Single producer per game ⇒ no overlapping prefills/races; mutual distinctness preserved (resolveUniqueQuestion still marks each question asked in Redis on enqueue). Starvation behaviour preserved (timeout → finalize). No DB/migration/financial impact; no R-11 surfaces touched. IDE Deno LSP reports no diagnostics (deno binary unavailable in this env for `deno check`). `docs/game-logic.md` §3/§4/§5.7/§12 updated. Deploy: `docker compose up -d --build --force-recreate game-orchestrator`.

[2026-06-08] [A-01] [BUILD] **Creative facet rotation (Phase 1) — kills cross-game repeats at the source.** Diagnosed why a model that "knows" millions of sport facts kept emitting the same five: a bare `category`+`difficulty` prompt at `temperature=0.4` collapses onto the highest-probability (most-famous) facts, so `claim_question` collisions + the 5-retry loop fired constantly. Fix is entirely in `deploy/game-orchestrator/orchestrator.ts` (no migration, no new infra): (1) every `generateQuestion` call is now steered into a randomly chosen **facet** (sub-topic) + **angle** (question style); the output contract explicitly forbids the single most-famous fact about that facet and demands a specific, lesser-known but 100%-verifiable detail. (2) Facets are **self-expanding** — `enumerateFacets` makes one cheap LLM call to list ~`FACET_COUNT` (60) diverse sub-topics per category, cached in Redis (`q4w:facets:{category}`, `FACET_CACHE_TTL_SECONDS`=7d via new `K.facets`) and in-process (`facetMemoryCache`); a static `GENERIC_FACET_FALLBACK` is used on any failure so generation never blocks. `pickFacetAngle` selects a facet + one of `QUESTION_ANGLES` per call. (3) Wired into `produceOneQuestion` (initial gen) and the `resolveUniqueQuestion` dedup loop (each retry re-rolls a FRESH facet/angle — a new corner of the category dodges collisions far better than re-asking the same one). (4) Base sampling raised: `temperature` default 0.4 → `QUESTION_GEN_TEMPERATURE` (0.9) + `top_p`=`QUESTION_GEN_TOP_P` (0.95); the LLM-cascade temperature still overrides the base when set. New env knobs: `QUESTION_GEN_TEMPERATURE`, `QUESTION_GEN_TOP_P`, `FACET_CACHE_TTL_SECONDS`, `FACET_COUNT`. Also corrected a stale doc line (consumer is LIFO `queue.pop()`, not FIFO). No DB/migration/financial impact; no R-11 surfaces touched. IDE reports no diagnostics (deno binary unavailable for `deno check`). `docs/game-logic.md` §12 config table + new "Creative facet rotation" section updated. Phase 2 (dedicated `question-generator` Docker service) still pending design approval. Deploy: `docker compose up -d --build --force-recreate game-orchestrator`.

[2026-06-08] [A-01] [FIX] **No-show participant excluded from prize ranking (Option A).** Defect: in games with `allowed_wrong_answers = NULL` (unlimited lives / score-only), a registered player who never joined or submitted a single answer was never eliminated by the ghost sweep (`elim` always `false` when `maxWrong` is null). `compute_game_ranks` therefore ranked them as survivors with `score=0`, potentially placing them inside a prize tier. Fix is two coordinated changes: (1) **`deploy/game-orchestrator/orchestrator.ts`** — `handlePersistAnswer` wrong-answer branch now does a read-modify-write to increment `game_participants.wrong_answers` by 1 (same pattern as the correct-answer branch for `correct_answers`). The ghost-sweep no-answer path (increments only `wrong_count`) is intentionally NOT changed, so `wrong_answers` stays 0 for pure no-shows. (2) **Migration `20260608300000_compute_game_ranks_exclude_noshows.sql`** — rewrites `compute_game_ranks` to add `AND (correct_answers > 0 OR wrong_answers > 0)` to the survivor CTE, and adds the same inverse condition to the disqualified-update block so no-shows receive `rank=NULL`/`status='disqualified'`. Guarantees that a player who never submitted even one answer — correct or wrong — can never appear in prize tiers regardless of elimination config. R-02/R-05/R-06/R-12 preserved. Deploy: apply migration via `db-maintainer`, then rebuild `game-orchestrator`.

[2026-06-08] [A-01] [FIX] **`GET /public-sounds?usage=loser` returned no sounds.** The `loser` slot was added to the admin function (`admin-sounds`), the admin UI, and the `app_sounds_usage_check` DB constraint (migration `20260608200000`), but the public read endpoint `supabase/functions/public-sounds/index.ts` was missed — its `VALID_USAGES` set still excluded `"loser"`, so `?usage=loser` failed the early validation guard (line 34) and returned HTTP 400 instead of the loser sounds. Fix: added `"loser"` to `VALID_USAGES`, bringing the public endpoint in sync with `admin-sounds` and the DB constraint. No DB/migration/financial impact; no R-11 surfaces touched. Deploy: rebuild `api` container.

[2026-06-09] [A-01] [BUILD] **host-app — full 16-screen mobile-first PWA frontend.** Shipped the complete host dashboard at `host.quiz4win.com` on top of the scaffold from the previous commit. Stack: Next.js 15 (App Router, server actions), React 19, Tailwind 4, `@supabase/ssr` for cookie-based session, dark glassmorphism Apple-style. (1) **Shared infra:** `src/lib/supabase/{server,client,middleware}.ts` (createServerClient w/ cookie store, browser singleton, session-rotation middleware that redirects unauthed users on /(app)/* to /signin and authed users on /signin to /dashboard), `src/middleware.ts` (matcher excludes static + images), `src/lib/api.ts` (server-only fetch wrapper: pulls JWT from Supabase session, forwards Bearer to `api.quiz4win.com`, JSON+FormData support, returns ApiResult<T> union), `src/lib/utils.ts` (cn, formatRelative/DateTime/Money), 7 UI primitives in `components/ui/` (Button w/ variants + loading, Card+CardHeader/Title/Subtitle, Input/Textarea w/ labels+errors, StatusChip with 20+ color-mapped statuses, Skeleton), plus `components/bottom-nav.tsx` (5-tab safe-area-aware glass pill nav: Home/Games/Invites/Wallet/Profile) and `components/page-header.tsx` (sticky top bar with optional back button). (2) **Auth (`(auth)/`):** signin, signup, verify-otp pages each use server actions calling `supabase.auth.signInWithPassword` / `signUp` / `verifyOtp({type:'email'})` / `resend({type:'signup'})`. OTP page hardened with 6-digit `inputMode='numeric'` pattern, autocomplete='one-time-code', resend button. Error display via `?error=` searchParam round-trip; URLs preserved on failure. (3) **Onboarding (`onboarding/apply/`):** multi-section glass form (display name + country + phone, language multi-select 8 langs, tagline + bio, 6 social URLs). POSTs to `/host/apply`; redirects already-pending/approved hosts back to dashboard. (4) **App shell (`(app)/layout.tsx`):** server-side auth gate that calls `/host/me`; on `not_a_host` redirects to `/onboarding/apply`, on 401 to `/signin`. Renders bottom-nav fixed with safe-area-inset-bottom. (5) **Dashboard:** at-a-glance: host status banner (amber for non-approved), shows-hosted + total-earnings tiles, invitations badge with new-count, next-show card. (6) **Games:** unified tabs page `(/games?tab=available|upcoming|history)` plus dynamic detail `/games/[id]` (full info grid, status chip, conditional CTAs: 'Request to host' form for available games when host approved, 'Start hosting' shortcut for assigned upcoming/live games). (7) **Stream readiness wizard (`/games/[id]/stream/`):** server page fetches session+game, hands off to client component `stream-wizard.tsx` that runs in-browser `getUserMedia` for camera + mic tests, a HEAD-fetch latency check for connection, persists each ok-flag back to `POST /host/games/:id/stream-session`, then `Mark ready` (status->ready) and `Go live` (calls `/stream-session/live`, displays the issued LiveKit token; `End stream` calls `/stream-session/end`). Video preview rendered with `<video playsInline muted>`. (8) **Invitations:** pending cards with admin_message preview + expires_at + Accept/Reject server-action forms; history list below. INV-17 conflict-on-accept surfaces as a localized error. (9) **Wallet:** wallet_balance + pending-earnings tiles, per-status totals, full earnings ledger with status chip, currency-formatted amounts. (10) **Payment methods:** list with default + status pills + delete (non-active only) / set-default actions; rich add-method form supporting 7 method types (IBAN, bank_account, paypal, USDT TRC20/ERC20, BTC, other) — collects account_details fields into JSONB, defaults to pending_verification. (11) **Files:** type-tagged upload form (selfie, id_document, intro_video, screenshot, avatar, other) using server-action FormData → /host/me/files (multipart). Per-row status chip + preview link + delete-pending button + rejection_reason display. (12) **Notifications:** simple read-only feed against the existing customer `/notifications` endpoint (hosts ARE users so JWT works). (13) **Settings:** profile summary card, three quick links (Files / Payment methods / Notifications), full editable profile form (country/phone/languages/short_bio/bio/6 social URLs), and a sign-out server action that calls `supabase.auth.signOut()` then redirects to /signin. All 19 client/server files pass IDE diagnostics zero-error. **No new dependencies on the backend or rule changes:** the host-app talks ONLY to `api.quiz4win.com` (existing host/admin endpoints from commit 54e510f) plus Supabase Auth via `@supabase/ssr` — fully D-1 compliant. **Deploy:** `docker compose up -d --build host` (first build ~3–5 min for `npm install`).

[2026-06-09] [A-01] [BUILD] **Host Platform — Phases 1b through 9 (backend complete + frontend scaffold).** Following the D-1..D-6 decisions (entry below), shipped the end-to-end backend plus mobile-first frontend scaffold for `host.quiz4win.com`. (1) **Phase 1b RLS** — new migration `20260609000100_host_platform_rls_policies.sql`: owner-self policies on `show_hosts` (auth_user_id = auth.uid()) plus FOR-ALL ownership policies on `host_game_requests`, `host_invitations`, `host_stream_sessions`, `host_payment_methods`, `host_uploaded_files`; `host_earnings` is SELECT-only for owner (admin writes via service-role). (2) **`supabase/functions/host/index.ts`** (Phases 2–6, 435 lines): `POST /host/apply` (idempotent re-apply on rejected; rejects pending/approved/suspended), `GET/PATCH /host/me`, `POST/GET/DELETE /host/me/files` (S3 private upload, mime+size guards), `GET /host/games/available|upcoming|history|requests`, `POST /host/games/:id/request`, `DELETE /host/games/requests/:id`, `GET /host/invitations` + `POST /host/invitations/:id/accept|reject` (INV-17 conflict check via `check_host_schedule_conflict` RPC + assigns games.host_id on accept), `GET/POST /host/games/:id/stream-session` (state machine created→testing→ready), `POST /host/games/:id/stream-session/live` (HS256 LiveKit JWT minted pure Web Crypto with existing LIVEKIT_API_KEY/SECRET — no SDK), `POST /host/games/:id/stream-session/end` (failure_reason capture), `GET /host/earnings` (with per-status totals), `GET/POST/PATCH/DELETE /host/payment-methods`. INV-18 enforced via `requireApproved()` gate. (3) **`supabase/functions/admin-hosts/index.ts`** (~360 lines): `GET /admin/hosts` (paginated, application_status + q filters), `GET /admin/hosts/:id` (host + counts + earnings totals), `POST /admin/hosts/:id/(approve|reject|suspend|reactivate)` with admin_audit_log entries, `GET /admin/hosts/:id/files`, `POST /admin/hosts/files/:id/(approve|reject)`, `GET /admin/hosts/requests` + `POST /admin/hosts/requests/:id/(approve|reject)` (INV-17 conflict check at approve, assigns games.host_id), `GET/POST /admin/hosts/invitations` + `POST /admin/hosts/invitations/:id/cancel`, `GET/POST /admin/hosts/earnings` + **`POST /admin/hosts/earnings/:id/approve`** which performs the INV-16 atomic admin-side credit (insert transactions row type='host_earning' status='completed' → call `increment_wallet_balance` RPC → bump show_hosts.total_earnings → patch host_earnings.transaction_id+approved_at), `POST /admin/hosts/earnings/:id/cancel`, `GET /admin/hosts/payment-methods` + `POST /admin/hosts/payment-methods/:id/(verify|reject)`. All admin write paths emit `admin_audit_log` rows. (4) **Phase 8 frontend scaffold (`host-app/`)** — minimal but runnable Next.js 15 + Tailwind 4 project: `package.json` (next 15.5, tailwind 4.3, sonner, react-query, zod, zustand, supabase ssr/js, lucide), `Dockerfile` (mirror admin/Dockerfile multi-stage standalone build with NEXT_PUBLIC_* build args), `next.config.ts` (output: standalone), `tsconfig.json`, `postcss.config.mjs`, `.gitignore`, `.dockerignore`, `src/app/layout.tsx` (dark glassmorphism viewport theme #0B0D14 mobile-first), `src/app/page.tsx` (Apple-style dark landing with glass cards). (5) **Docker + nginx** — `docker-compose.yml`: header comment updated + new `host` service (image `quiz4win-host:latest`, port 5803:3000, build args for NEXT_PUBLIC_SUPABASE_URL/ANON_KEY/API_URL/HOST_URL/APP_NAME) inserted just before `monitor`; **R-11.2 frozen lines untouched** (verified: api at line 183, game-orchestrator at 286). `deploy/nginx/host.quiz4win.com.conf` created mirroring panel config (HTTP→HTTPS, TLS via certbot, security headers, proxy_pass to 127.0.0.1:5803, 50M body limit, _next/static long-cache). (6) **Phase 9 admin UI** — `admin/src/app/(app)/hosts/page.tsx` (server-component list with application_status filter chips + search), `admin/src/lib/nav.ts` (new "Hosts" entry under Content section with UserCog icon), `admin/src/lib/auth.ts` ROUTE_ROLES (`/hosts` → super_admin/admin/moderator/finance). The detail page + inline action UI is deferred (admin can drive everything via API for now). **Rule compliance:** R-01 (no secrets surfaced; admin endpoints log audit entries with reasons), R-02 (NUMERIC matches existing schema; awaits global R-02 sweep), R-03 (every host write validates JWT), R-04 (RLS policies on all new tables in 20260609000100; admin paths use service-role per existing pattern), R-05 (`host_earnings.transaction_id` set ONCE on approval; `transactions` rows never UPDATE/DELETE), R-06 (no reverse imports), R-08 (host payouts use existing withdrawals flow; KYC check unchanged), R-10 (D-1..D-6 human-approved; A-05 territory acknowledged in changelog), R-11 (no frozen surfaces touched — getPublicClient/REDIS_URL/.env hygiene all untouched), R-12 (migrations land via db-maintainer; HUMAN deploys), R-13 (no supabase CLI in any new instruction), R-14 (no Supabase SDK in any new docker-deployed service — host-app uses fetch to api). **Deploy sequence (HUMAN):** (a) `docker compose up -d --force-recreate db-maintainer` (applies both 20260609000000 + 20260609000100), (b) `docker compose up -d --build --force-recreate api admin`, (c) `docker compose up -d --build host` to bring `quiz4win-host` online (first build runs `npm install`; runs ~3–5 min), (d) on the VM `sudo bash deploy/nginx/setup.sh` to provision certbot for `host.quiz4win.com`. **Follow-up (not in this commit):** full 16-screen host-app UI (signin/signup/OTP/profile/games/invitations/streams wizard/wallet/payment-methods), admin host-detail action UI, host-side push notification integration, deletion of stale S3 objects on file-row delete.

[2026-06-09] [A-01] [ARCH] **Host Platform — Phase 0 decisions logged (human-approved).** New end-to-end host subsystem starts (self-service signup → application review → game request/invitation flow → stream readiness → earnings/payouts → mobile-first dashboard on `host.quiz4win.com`). Six architectural decisions confirmed by the human (replaces D-1..D-6 from agent proposal): **D-1** Reuse Supabase Auth (GoTrue) for host identity — no custom password/OTP/refresh infra; same code path as customers, R-11 surfaces untouched. **D-2** Hosts are an extension of an existing user (a player can become a host and vice versa) — one `auth.users.id` ↔ one `profiles` row ↔ optional `show_hosts` row with host-specific fields. **D-3** Extend existing `show_hosts` in place (keep `games.host_id` FK valid); add `auth_user_id`, `application_status`, `country`, `languages`, `phone`, social links (instagram/telegram/youtube/tiktok/twitter/website), application timestamps and admin-review fields. **D-4** Reuse `profiles.wallet_balance` + `transactions` ledger; add new `host_earnings` reservation table (`pending`→`approved`→`paid`) and new transaction types `host_earning`/`host_payout` — preserves R-05 (`transactions` stays append-only). Withdrawals reuse the existing `withdrawals` flow (R-08 KYC applies to hosts). **D-5** New subdomain `host.quiz4win.com` (port 5803) served by a new `quiz4win-host` Next.js container in `host-app/`. **D-6** Reuse existing LiveKit credentials (`LIVEKIT_API_KEY`/`LIVEKIT_API_SECRET`) for host stream tokens. Phase 0 artifacts: this entry + `Architecture_Map.md` host subsystem row + `Domain_Knowledge.md` INV-16 (host-earnings reservation), INV-17 (no overlapping host assignments), INV-18 (host application & status gates) + `Open_Tasks_AI.md` 10-task tree. No code, no schema change, no container change in this commit — Phase 1 (migration) lands next and is held pending human deploy via `db-maintainer` (R-12).


[2026-06-17] [A-01] [AUDIT] Session started. Onboarding checklist complete. All required documents read. Ready for task assignments.

[2026-06-17] [A-01] [BUILD] **host-app — Landing page redesign with nudge-marketing conversion copy.** Rewrote `host-app/src/app/page.tsx` from a minimal scaffold into a full high-conversion landing page. Key nudge-marketing elements: (1) live "accepting in your region" pulsing badge (in-group + availability signal), (2) gradient hero headline with gradient CTA button + pink glow shadow (visual salience), (3) social-proof stat strip (€850 avg/show, <1 hr approval, 10 k+ players/game), (4) numbered 3-step "How it works" path (reduces friction / cognitive load), (5) 2×2 benefit grid (Instant pay-out, Massive audience, Build fame, Your schedule — addresses objections), (6) amber scarcity strip ("host slots are capped per region — once capacity is reached, applications close" — FOMO), (7) closing bottom CTA glass card with soft repeat of value proposition. All built on existing design-system primitives (glass, Button gradient, color CSS vars, Lucide icons). Server component — no new dependencies. No backend/DB/R-11 impact. Deploy: `docker compose up -d --build host`.


[2026-06-17] [A-01] [FIX] **Resolved R-01 violation.** Deleted 12 temporary scripts and JSON files (starting with `.tmp_`) that contained or potentially contained hardcoded secrets and PII, as identified in `Open_Tasks_AI.md`.

[2026-06-17] [A-01] [FIX] **host-app — Notifications screen was missing a back button.** `app/(app)/notifications/page.tsx` rendered `<PageHeader title="Notifications" />` with no `back` prop, so the header showed no back affordance even though the screen is reached from the Settings/Profile quick links (like profile/social/files). Added `back="/settings"` to match the sibling settings inner pages; with the R-16-era `BackButton` this returns to the actual previous page via browser history and falls back to `/settings` only on a direct load/refresh. Frontend-only, no backend/DB/migration/R-11 impact. Deploy: `docker compose up -d --build host`.

[2026-06-17] [A-01] [RULE] **Added R-16 — Features Must Be End-to-End Complete & Backward-Compatible** (human-directed). New rule in `Rules.md` (rev 6) requiring any new feature implemented in one section to be propagated across every affected subdomain/layer (DB + RLS + RPC, all Edge Functions/API, admin/host-app/customer frontends, orchestrator/workers, shared types & docs) and to remain backward-compatible (additive-only fields/enums with safe defaults; no breaking changes to existing response shapes/contracts without migrating all callers in the same change). R-16.3 ties it to the codebase-wide downstream/mirror-sub completeness check before any feature is marked done; incomplete/inconsistent surfaces are treated as bugs and flagged `[RULE]`. Cites the 2026-06-08 `usage=loser` public-sounds miss as precedent. Also synced the rule-summary table in `AGENTS.md` (added the previously-missing R-15 row plus the new R-16 row). Rules.md is A-02's authority domain (R-10) — change made under explicit human direction. Docs-only; no code/DB/migration/R-11 impact.

[2026-06-17] [A-01] [FIX] **host — re-requesting a game after cancellation returned `already_requested`.** `host_game_requests` has a `UNIQUE(host_id, game_id)` constraint, so only one row per (host, game) pair can exist. Cancelling a request sets `status='cancelled'` on that row but leaves it in place. A subsequent `POST /host/games/:id/request` blindly inserted a new row, hit the unique constraint, and surfaced `already_requested` to the UI. Fix: before inserting, the handler now checks for an existing row with `maybeSingle()`. If one exists with `status='cancelled'` it UPDATEs it back to `pending` (resetting `admin_note`, `reviewed_by`, `reviewed_at`, updating `host_note` and `updated_at`). If one exists with any other status (`pending`/`approved`/`rejected`) it still returns `already_requested`. If no row exists the original INSERT path runs as before. No DB/migration change; no R-11 surfaces touched. Deploy: `docker compose up -d --force-recreate api`.

[2026-06-17] [A-01] [FIX] **host-app — back button now returns to the actual previous page.** `components/page-header.tsx` rendered the back affordance as a hard-coded `<Link href={back}>`, so inner pages reachable from more than one place (e.g. Profile → Payout wallets, which declared `back="/wallet"`) jumped to the fixed target instead of where the user came from. Extracted a small client component `components/back-button.tsx` (`"use client"`, `useRouter().back()`) that pops browser history and falls back to the declared `back` URL only when there is no history (direct load/refresh; `window.history.length <= 1`). `PageHeader` stays a server component and now renders `<BackButton fallback={back} />`. The `back` prop API is unchanged, so all call sites (settings/profile, settings/social, files, payment-methods, games/[id], games/[id]/stream) keep working and now behave correctly. Frontend-only, no backend/DB/migration/financial impact; no R-11 surfaces touched. Deploy: `docker compose up -d --build host`.
